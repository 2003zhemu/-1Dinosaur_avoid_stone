local module = {}

-- module.Modify = require(script.Modifier)
-- module.Buff = require(script.Buff)
-- module.Team = require(script.Team)
-- module.Unit = require(script.Unit)
module.Timer = require(script.Timer)
module.Signal = require(script.Signal)
return module
