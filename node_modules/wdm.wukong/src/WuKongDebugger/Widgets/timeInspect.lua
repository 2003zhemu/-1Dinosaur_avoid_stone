local formatTableModule = require(script.Parent.Parent.formatTable)
local formatTable = formatTableModule.formatTable

return function(plasma)
	local create = plasma.create
	return plasma.widget(function(debugger)
		local custom = debugger._customWidgets
		local timer = debugger.WKTimer
		local style = plasma.useStyle()

		local world = debugger.debugWorld

		local closed = plasma
			.window({
				title = "Time travel",
				closable = true,
			}, function()
				local fadeTime, setFadeTime = plasma.useState()
				local timeScale, setTimeScale = plasma.useState(1)
				local refs = plasma.useInstance(function(ref)
					return create("Frame", {
						BackgroundTransparency = 1,
						BackgroundColor3 = Color3.new(.2, .2, .2),
						AutomaticSize = Enum.AutomaticSize.Y,
						Size = UDim2.new(0, 200, 0, 0),

						create("UIListLayout", {
							HorizontalAlignment = Enum.HorizontalAlignment.Center,
							VerticalAlignment = Enum.VerticalAlignment.Center,
							FillDirection = Enum.FillDirection.Vertical,
							Padding = UDim.new(0, 0),
							SortOrder = Enum.SortOrder.LayoutOrder
						}),
						create("Frame", {
							BackgroundTransparency = .5,
							BackgroundColor3 = Color3.new(.2, .2, .2),
							AutomaticSize = Enum.AutomaticSize.Y,
							Size = UDim2.fromScale(1, 0),
							LayoutOrder = 0,
							create("UIPadding", {
								PaddingBottom = UDim.new(0, 8),
								PaddingLeft = UDim.new(0, 8),
								PaddingRight = UDim.new(0, 8),
								PaddingTop = UDim.new(0, 8),
							}),
							create("UICorner"),
	
							create("UIListLayout", {
								HorizontalAlignment = Enum.HorizontalAlignment.Center,
								VerticalAlignment = Enum.VerticalAlignment.Center,
								FillDirection = Enum.FillDirection.Vertical,
								Padding = UDim.new(0, 0),
								SortOrder = Enum.SortOrder.LayoutOrder
							}),
	
							create("Frame", {
								[ref] = "second",
								BackgroundTransparency = .5,
								BackgroundColor3 = Color3.new(.2, .2, .2),
								AutomaticSize = Enum.AutomaticSize.Y,
								Size = UDim2.fromScale(1, 0),
								LayoutOrder = 1,
								create("UIPadding", {
									PaddingBottom = UDim.new(0, 8),
									PaddingLeft = UDim.new(0, 8),
									PaddingRight = UDim.new(0, 8),
									PaddingTop = UDim.new(0, 8),
								}),
								create("UICorner"),
		
								create("UIListLayout", {
									HorizontalAlignment = Enum.HorizontalAlignment.Center,
									VerticalAlignment = Enum.VerticalAlignment.Center,
									FillDirection = Enum.FillDirection.Horizontal,
									Padding = UDim.new(0, 10),
									SortOrder = Enum.SortOrder.LayoutOrder
								}),
								create("TextButton", {
									Text = "◀",
									Size = UDim2.new(.2, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 0,
									create("UICorner"),

									Activated = function()
										setFadeTime(-1)
									end,
									
								}),
								create("TextButton", {
									[ref] = "secondBtn",
									Text = "Second",
									Size = UDim2.new(.6, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									BackgroundTransparency = 0,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 1,
									Font = Enum.Font.Gotham,
									create("UICorner"),

									Activated = function()
										setTimeScale(1)
									end,
								}),
								create("TextButton", {
									Text = "▶",
									Size = UDim2.new(.2, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 2,
									create("UICorner"),

									Activated = function()
										setFadeTime(1)
									end,
								})
							}),
							create("Frame", {
								[ref] = "minute",
								BackgroundTransparency = .5,
								BackgroundColor3 = Color3.new(.2, .2, .2),
								AutomaticSize = Enum.AutomaticSize.Y,
								Size = UDim2.fromScale(1, 0),
								LayoutOrder = 2,
								create("UIPadding", {
									PaddingBottom = UDim.new(0, 8),
									PaddingLeft = UDim.new(0, 8),
									PaddingRight = UDim.new(0, 8),
									PaddingTop = UDim.new(0, 8),
								}),
								create("UICorner"),
		
								create("UIListLayout", {
									HorizontalAlignment = Enum.HorizontalAlignment.Center,
									VerticalAlignment = Enum.VerticalAlignment.Center,
									FillDirection = Enum.FillDirection.Horizontal,
									Padding = UDim.new(0, 10),
									SortOrder = Enum.SortOrder.LayoutOrder
								}),
								create("TextButton", {
									Text = "◀",
									Size = UDim2.new(.2, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 0,
									create("UICorner"),

									Activated = function()
										setFadeTime(-60)
									end,
								}),
								create("TextButton", {
									[ref] = "minuteBtn",
									Text = "Minute",
									Size = UDim2.new(.6, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									BackgroundTransparency = 0,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 1,
									Font = Enum.Font.Gotham,
									create("UICorner"),
									Activated = function()
										setTimeScale(60)
									end,
								}),
								create("TextButton", {
									Text = "▶",
									Size = UDim2.new(.2, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 2,
									create("UICorner"),

									Activated = function()
										setFadeTime(60)
									end,
								})
							}),
							create("Frame", {
								[ref] = "hour",
								BackgroundTransparency = .5,
								BackgroundColor3 = Color3.new(.2, .2, .2),
								AutomaticSize = Enum.AutomaticSize.Y,
								Size = UDim2.fromScale(1, 0),
								LayoutOrder = 3,
								create("UIPadding", {
									PaddingBottom = UDim.new(0, 8),
									PaddingLeft = UDim.new(0, 8),
									PaddingRight = UDim.new(0, 8),
									PaddingTop = UDim.new(0, 8),
								}),
								create("UICorner"),
								create("UIListLayout", {
									HorizontalAlignment = Enum.HorizontalAlignment.Center,
									VerticalAlignment = Enum.VerticalAlignment.Center,
									FillDirection = Enum.FillDirection.Horizontal,
									Padding = UDim.new(0, 10),
									SortOrder = Enum.SortOrder.LayoutOrder
								}),
								create("TextButton", {
									Text = "◀",
									Size = UDim2.new(.2, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 0,
									
									create("UICorner"),

									Activated = function()
										setFadeTime(-3600)
									end,
								}),
								create("TextButton", {
									[ref] = "hourBtn",
									Text = "Hour",
									Size = UDim2.new(.6, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									BackgroundTransparency = 0,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 1,
									Font = Enum.Font.Gotham,
									create("UICorner"),
									Activated = function()
										setTimeScale(3600)
									end,
								}),
								create("TextButton", {
									Text = "▶",
									Size = UDim2.new(.2, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 2,
									create("UICorner"),

									Activated = function()
										setFadeTime(3600)
									end,
								})
							}),
							create("Frame", {
								[ref] = "day",
								BackgroundTransparency = .5,
								BackgroundColor3 = Color3.new(.2, .2, .2),
								AutomaticSize = Enum.AutomaticSize.Y,
								Size = UDim2.fromScale(1, 0),
								LayoutOrder = 3,
								create("UIPadding", {
									PaddingBottom = UDim.new(0, 8),
									PaddingLeft = UDim.new(0, 8),
									PaddingRight = UDim.new(0, 8),
									PaddingTop = UDim.new(0, 8),
								}),
								create("UICorner"),
								create("UIListLayout", {
									HorizontalAlignment = Enum.HorizontalAlignment.Center,
									VerticalAlignment = Enum.VerticalAlignment.Center,
									FillDirection = Enum.FillDirection.Horizontal,
									Padding = UDim.new(0, 10),
									SortOrder = Enum.SortOrder.LayoutOrder
								}),
								create("TextButton", {
									Text = "◀",
									Size = UDim2.new(.2, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 0,
									
									create("UICorner"),

									Activated = function()
										setFadeTime(-86400)
									end,
								}),
								create("TextButton", {
									[ref] = "dayBtn",
									Text = "Day",
									Size = UDim2.new(.6, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									BackgroundTransparency = 0,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 1,
									Font = Enum.Font.Gotham,
									create("UICorner"),
									Activated = function()
										setTimeScale(86400)
									end,
								}),

								create("TextButton", {
									Text = "▶",
									Size = UDim2.new(.2, 0, 0, 40),
									BackgroundColor3 = style.bg1,
									TextSize = 22,
									TextColor3 = Color3.new(1, 1, 1),
									LayoutOrder = 2,
									create("UICorner"),

									Activated = function()
										setFadeTime(86400)
									end,
								})
							})
	
						})
					})
				end)

				plasma.useEffect(function()
					timer:SetTimeScale(timeScale)
					refs.secondBtn.BackgroundColor3 = timeScale == 1 and style.primaryColor or style.bg1
					refs.minuteBtn.BackgroundColor3 = timeScale == 60 and style.primaryColor or style.bg1
					refs.hourBtn.BackgroundColor3 = timeScale == 3600 and style.primaryColor or style.bg1
					refs.dayBtn.BackgroundColor3 = timeScale == 86400 and style.primaryColor or style.bg1
				end, timeScale)

				plasma.space(10)
				if custom.button1(timer.Paused and "Resume" or "Pause", {bg = timer.Paused and style.primaryColor or style.bg3}):clicked() then
					timer.Toggle()
				end

				if fadeTime ~= 0 then
					timer:Fade(fadeTime)
					setFadeTime(0)
				end
			end)
			:closed()

		if closed then
			return closed
		end
		return nil
	end)
end
