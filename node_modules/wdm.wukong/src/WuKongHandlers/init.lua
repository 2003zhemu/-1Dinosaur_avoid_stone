-- 由于和服务端公用，不可以 require wukong

local priceProvide = require(script.ProductPriceProvider)

local module = {}

-- R币商品回调
-- 请提供回调函数，当需要玩家购买商品时，该函数会被调用，并提供Roblox商品Id作为参数
-- 回调中，需要提示玩家购买，并返回结果
-- 玩家购买成功后，返回true，玩家取消购买，返回false
-- 当玩家连续发送购买请求，第二次返回false
module.ProductPurchaseHandler = function(userId, productId)
	error("不再支持")
end

-- 获取R币价格回调 , 如果未获取到，返回-1
module.GetProductPrice = function(productId): number
	return priceProvide.GetProductPrice(productId)
end

return module
