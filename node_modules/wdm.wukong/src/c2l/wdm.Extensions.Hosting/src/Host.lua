-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmExtensionsHosting
local __importGlobal =function (out)
  wdmExtensionsHosting = out.wdm.Extensions.Hosting
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Hosting", function (namespace)
  -- <summary>
  -- Provides convenience methods for creating instances of <see cref="IHostBuilder"/> with pre-configured defaults.
  -- </summary>
  namespace.class("Host", function (namespace)
    local RequiresDynamicCodeMessage, CreateDefaultBuilder, CreateDefaultBuilder1
    RequiresDynamicCodeMessage = "Hosting uses wdm.Extensions.DependencyInjection, which may require generating code dynamically at runtime."
    -- <summary>
    -- Initializes a new instance of the <see cref="HostBuilder"/> class with pre-configured defaults.
    -- 
    -- </summary>
    -- <seealso href="https://learn.microsoft.com/en-us/dotnet/core/extensions/generic-host#default-builder-settings"/>
    -- <remarks>
    -- The following defaults are applied to the returned <see cref="HostBuilder"/>:
    -- <list type="bullet">
    -- <item><description>set the <see cref="IHostEnvironment.ContentRootPath"/> to the result of <see cref="Directory.GetCurrentDirectory()"/></description></item>
    -- <item><description>load host <see cref="IConfiguration"/> from "DOTNET_" prefixed environment variables</description></item>
    -- <item><description>load app <see cref="IConfiguration"/> from 'appsettings.json' and 'appsettings.[<see cref="IHostEnvironment.EnvironmentName"/>].json'</description></item>
    -- <item><description>load app <see cref="IConfiguration"/> from User Secrets when <see cref="IHostEnvironment.EnvironmentName"/> is 'Development' using the entry assembly</description></item>
    -- <item><description>load app <see cref="IConfiguration"/> from environment variables</description></item>
    -- <item><description>configure the <see cref="ILoggerFactory"/> to log to the console, debug, and event source output</description></item>
    -- <item><description>enables scope validation on the dependency injection container when <see cref="IHostEnvironment.EnvironmentName"/> is 'Development'</description></item>
    -- </list>
    -- </remarks>
    -- <returns>The initialized <see cref="IHostBuilder"/>.</returns>
    CreateDefaultBuilder = function ()
      return CreateDefaultBuilder1()
    end
    -- <summary>
    -- Initializes a new instance of the <see cref="HostBuilder"/> class with pre-configured defaults.
    -- </summary>
    -- The following defaults are applied to the returned <see cref="HostBuilder"/>:
    -- <remarks>
    -- The following defaults are applied to the returned <see cref="HostBuilder"/>:
    -- <list type="bullet">
    -- <item><description>set the <see cref="IHostEnvironment.ContentRootPath"/> to the result of <see cref="Directory.GetCurrentDirectory()"/></description></item>
    -- <item><description>load host <see cref="IConfiguration"/> from "DOTNET_" prefixed environment variables</description></item>
    -- <item><description>load host <see cref="IConfiguration"/> from supplied command line args</description></item>
    -- <item><description>load app <see cref="IConfiguration"/> from 'appsettings.json' and 'appsettings.[<see cref="IHostEnvironment.EnvironmentName"/>].json'</description></item>
    -- <item><description>load app <see cref="IConfiguration"/> from User Secrets when <see cref="IHostEnvironment.EnvironmentName"/> is 'Development' using the entry assembly</description></item>
    -- <item><description>load app <see cref="IConfiguration"/> from environment variables</description></item>
    -- <item><description>load app <see cref="IConfiguration"/> from supplied command line args</description></item>
    -- <item><description>configure the <see cref="ILoggerFactory"/> to log to the console, debug, and event source output</description></item>
    -- <item><description>enables scope validation on the dependency injection container when <see cref="IHostEnvironment.EnvironmentName"/> is 'Development'</description></item>
    -- </list>
    -- </remarks>
    -- <param name="args">The command line args.</param>
    -- <returns>The initialized <see cref="IHostBuilder"/>.</returns>
    CreateDefaultBuilder1 = function (args)
      local builder =wdmExtensionsHosting.HostBuilder()
      return wdmExtensionsHosting.HostingHostBuilderExtensions.ConfigureDefaults(builder, args)
    end
    return {
      RequiresDynamicCodeMessage = RequiresDynamicCodeMessage,
      CreateDefaultBuilder = CreateDefaultBuilder,
      CreateDefaultBuilder1 = CreateDefaultBuilder1,
      __metadata__ = function (out)
        return {
          methods = {
            { "CreateDefaultBuilder", 0x8E, CreateDefaultBuilder, out.wdm.Extensions.Hosting.IHostBuilder },
            { "CreateDefaultBuilder", 0x18E, CreateDefaultBuilder1, System.Array(System.String), out.wdm.Extensions.Hosting.IHostBuilder }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}