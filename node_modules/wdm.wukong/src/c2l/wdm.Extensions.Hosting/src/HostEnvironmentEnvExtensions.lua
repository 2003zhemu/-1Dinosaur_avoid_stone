-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmExtensionsHosting
local wdmToolkitDiagnostics
local __importGlobal =function (out)
  wdmExtensionsHosting = out.wdm.Extensions.Hosting
  wdmToolkitDiagnostics = out.wdm.Toolkit.Diagnostics
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Hosting", function (namespace)
  -- <summary>
  -- Extension methods for <see cref="IHostEnvironment"/>.
  -- </summary>
  namespace.class("HostEnvironmentEnvExtensions", function (namespace)
    local IsDevelopment, IsStaging, IsProduction, IsEnvironment
    -- <summary>
    -- Checks if the current host environment name is <see cref="Environments.Development"/>.
    -- </summary>
    -- <param name="hostEnvironment">An instance of <see cref="IHostEnvironment"/>.</param>
    -- <returns>True if the environment name is <see cref="Environments.Development"/>, otherwise false.</returns>
    IsDevelopment = function (hostEnvironment)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(hostEnvironment)
      
      return IsEnvironment(hostEnvironment, wdmExtensionsHosting.Environments.Development)
    end
    -- <summary>
    -- Checks if the current host environment name is <see cref="Environments.Staging"/>.
    -- </summary>
    -- <param name="hostEnvironment">An instance of <see cref="IHostEnvironment"/>.</param>
    -- <returns>True if the environment name is <see cref="Environments.Staging"/>, otherwise false.</returns>
    IsStaging = function (hostEnvironment)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(hostEnvironment)
      
      return IsEnvironment(hostEnvironment, wdmExtensionsHosting.Environments.Staging)
    end
    -- <summary>
    -- Checks if the current host environment name is <see cref="Environments.Production"/>.
    -- </summary>
    -- <param name="hostEnvironment">An instance of <see cref="IHostEnvironment"/>.</param>
    -- <returns>True if the environment name is <see cref="Environments.Production"/>, otherwise false.</returns>
    IsProduction = function (hostEnvironment)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(hostEnvironment)
      
      return IsEnvironment(hostEnvironment, wdmExtensionsHosting.Environments.Production)
    end
    -- <summary>
    -- Compares the current host environment name against the specified value.
    -- </summary>
    -- <param name="hostEnvironment">An instance of <see cref="IHostEnvironment"/>.</param>
    -- <param name="environmentName">Environment name to validate against.</param>
    -- <returns>True if the specified name is the same as the current environment, otherwise false.</returns>
    IsEnvironment = function (hostEnvironment, environmentName)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(hostEnvironment)
      
      return System.String.Equals(hostEnvironment:getEnvironmentName(), environmentName, System.StringComparison.OrdinalIgnoreCase)
    end
    return {
      IsDevelopment = IsDevelopment,
      IsStaging = IsStaging,
      IsProduction = IsProduction,
      IsEnvironment = IsEnvironment,
      __metadata__ = function (out)
        return {
          methods = {
            { "IsDevelopment", 0x18E, IsDevelopment, out.wdm.Extensions.Hosting.IHostEnvironment, System.Boolean },
            { "IsEnvironment", 0x28E, IsEnvironment, out.wdm.Extensions.Hosting.IHostEnvironment, System.String, System.Boolean },
            { "IsProduction", 0x18E, IsProduction, out.wdm.Extensions.Hosting.IHostEnvironment, System.Boolean },
            { "IsStaging", 0x18E, IsStaging, out.wdm.Extensions.Hosting.IHostEnvironment, System.Boolean }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}