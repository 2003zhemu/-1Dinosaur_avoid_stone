-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmToolkitDiagnostics
local __importGlobal =function (out)
  wdmToolkitDiagnostics = out.wdm.Toolkit.Diagnostics
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Configuration", function (namespace)
  -- <summary>
  -- Utility methods and constants for manipulating Configuration paths
  -- </summary>
  namespace.class("ConfigurationPath", function (namespace)
    local KeyDelimiter, Combine, Combine1, GetSectionKey, GetParentPath
    KeyDelimiter = ":"
    -- <summary>
    -- Combines path segments into one path.
    -- </summary>
    -- <param name="pathSegments">The path segments to combine.</param>
    -- <returns>The combined path.</returns>
    Combine = function (...)
      local var= {...} 
      local pathSegments=System.arrayFromTable(var,System.Object)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(pathSegments)
      
      return System.String.JoinParams(KeyDelimiter, pathSegments)
    end
    -- <summary>
    -- Combines path segments into one path.
    -- </summary>
    -- <param name="pathSegments">The path segments to combine.</param>
    -- <returns>The combined path.</returns>
    Combine1 = function (pathSegments)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(pathSegments)
      
      return System.String.JoinEnumerable(KeyDelimiter, pathSegments)
    end
    -- <summary>
    -- Extracts the last path segment from the path.
    -- </summary>
    -- <param name="path">The path.</param>
    -- <returns>The last path segment of the path.</returns>
    GetSectionKey = function (path)
      if System.String.IsNullOrEmpty(path) then
        return path
      end
      
      local lastDelimiterIndex =System.S(path):LastIndexOf(KeyDelimiter, nil, nil, System.StringComparison.OrdinalIgnoreCase)
      local default
      if lastDelimiterIndex == - 1 then
        default = path
      else
        default = System.S(path):Substring(lastDelimiterIndex + 1)
      end
      return default
    end
    -- <summary>
    -- Extracts the path corresponding to the parent node for a given path.
    -- </summary>
    -- <param name="path">The path.</param>
    -- <returns>The original path minus the last individual segment found in it. Null if the original path corresponds to a top level node.</returns>
    GetParentPath = function (path)
      if System.String.IsNullOrEmpty(path) then
        return nil
      end
      
      local lastDelimiterIndex =System.S(path):LastIndexOf(KeyDelimiter, nil, nil, System.StringComparison.OrdinalIgnoreCase)
      local default
      if lastDelimiterIndex == - 1 then
        default = nil
      else
        default = System.S(path):Substring(0, lastDelimiterIndex)
      end
      return default
    end
    return {
      KeyDelimiter = KeyDelimiter,
      Combine = Combine,
      Combine1 = Combine1,
      GetSectionKey = GetSectionKey,
      GetParentPath = GetParentPath,
      __metadata__ = function (out)
        return {
          fields = {
            { "KeyDelimiter", 0xE, System.String }
          },
          methods = {
            { "Combine", 0x18E, Combine, System.Array(System.String), System.String },
            { "Combine", 0x18E, Combine1, System.IEnumerable_1(System.String), System.String },
            { "GetParentPath", 0x18E, GetParentPath, System.String, System.String },
            { "GetSectionKey", 0x18E, GetSectionKey, System.String, System.String }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}