-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmExtensionsLogging
local ListLogInfo
local __importGlobal =function (out)
  wdmExtensionsLogging = out.wdm.Extensions.Logging
  ListLogInfo = System.List(_G.wdm.Extensions.Logging.LogInfo)
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Logging.Http", function (namespace)
  namespace.class("BundleLoggerStore_1", function (namespace)
    return function (T)
      --检查泛型约束
      local function checkConstraint(generic, check) 
        local tGeneric = System.typeof(generic)
        local tCheck = System.typeof(check)
        if not (tGeneric:IsAssignableFrom(tCheck) or tGeneric == tCheck) then
          local message = tGeneric:getFullName().." on "..tCheck:getFullName()
          System.throw(System.ArgumentException(message))
        end
      end
      checkConstraint(_G.wdm.Extensions.Logging.Http.IBundleLoggerStoreStaff, T)
      local _loop, Log, TryPopBundle, _canPopBundle, AddToBundle, _tryUpdate, _consume, Dispose, 
      __ctor__
      __ctor__ = function (this, staff)
        this._staff = System.default(T)
        this.Logs = ListLogInfo()
        this._staff = staff
        --
        -- /*
        --  [[
        --     task.spawn(function()
        --         _loop(this)
        --     end)
        -- ]]
        --  */
        
        
        System.Task.Run(function ()
          _loop(this)
        end)
      end
      _loop = function (this)
        return System.async(function (async, this)
          while not this._isStop do
            async:await(System.Task.Delay(100))
            _tryUpdate(this)
          end
        end, nil, this)
      end
      Log = function (this, loggerName, logLevel, eventId, state, exception, formatter, stackTraceLevelIncreased, TState)
        local message =formatter(state, exception, TState)
        
        local logInfo =wdmExtensionsLogging.LogInfoFactory.CreateLogInfo(loggerName, logLevel, eventId, exception, message, stackTraceLevelIncreased + 1)
        
        AddToBundle(this, logInfo)
      end
      TryPopBundle = function (this, logInfo)
        logInfo = nil
        local can =_canPopBundle(this)
        if can then
          logInfo = this.Logs:ToArray()
          this.Logs:Clear()
          return true, logInfo
        end
        return false, logInfo
      end
      _canPopBundle = function (this)
        local default =this._staff
        if default ~= nil then
          default = default:getPopValidator()
        end
        if default == nil then
          return false
        end
        return this._staff:getPopValidator():CanPop(this.Logs)
      end
      AddToBundle = function (this, logInfo)
        this.Logs:Add(logInfo)
        _tryUpdate(this)
      end
      _tryUpdate = function (this)
        local default, bundle = TryPopBundle(this)
        if default then
          _consume(this, bundle)
        end
      end
      _consume = function (this, logInfos)
        System.Task.Run(function ()
          System.try(function ()
            local default =this._staff
            if default ~= nil then
              default:getConsumer():Consume(logInfos)
            end
          end, function (default)
            local e =default
            System.Debug.WriteLine(System.toString("error on consume bundle logger: Http Logger. \n") .. System.toString(System.toString(e)))
          end)
        end)
      end
      Dispose = function (this)
        this._isStop = true
      end
      return {
        base = function (out)
          return {
            out.System.IDisposable,
            out.wdm.Extensions.Logging.Http.IBundleLoggerStore
          }
        end,
        _isStop = false,
        Log = Log,
        TryPopBundle = TryPopBundle,
        AddToBundle = AddToBundle,
        Dispose = Dispose,
        __ctor__ = __ctor__,
        __metadata__ = function (out)
          return {
            fields = {
              { "Logs", 0x6, System.List(out.wdm.Extensions.Logging.LogInfo) },
              { "_isStop", 0x1, System.Boolean },
              { "_staff", 0x1, T }
            },
            methods = {
              { ".ctor", 0x106, nil, T },
              { "AddToBundle", 0x106, AddToBundle, out.wdm.Extensions.Logging.LogInfo },
              { "Dispose", 0x6, Dispose },
              { "Log", 0x10706, Log, function (TState) return System.String, out.wdm.Extensions.Logging.LogLevel, out.wdm.Extensions.Logging.EventId, TState, System.Exception, System.Delegate(TState, System.Exception, System.String), System.Int32 end },
              { "TryPopBundle", 0x186, TryPopBundle, System.Array(out.wdm.Extensions.Logging.LogInfo), System.Boolean },
              { "_canPopBundle", 0x81, _canPopBundle, System.Boolean },
              { "_consume", 0x101, _consume, System.Array(out.wdm.Extensions.Logging.LogInfo) },
              { "_loop", 0x81, _loop, System.Task },
              { "_tryUpdate", 0x1, _tryUpdate }
            },
            class = { 0x106, T }
          }
        end
      }
    end
  end)
end)
return {}