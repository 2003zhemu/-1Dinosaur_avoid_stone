-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongConfigs
local wdmWuKongConfigsCore
local wdmWuKongConfigsVendor
local ListCfgElementInfo
local __importGlobal =function (out)
  wdmWuKongConfigs = out.wdm.WuKong.Configs
  wdmWuKongConfigsCore = out.wdm.WuKong.Configs.Core
  wdmWuKongConfigsVendor = out.wdm.WuKong.Configs.Vendor
  ListCfgElementInfo = System.List(_G.wdm.WuKong.Configs.Vendor.CfgElementInfo)
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Configs.Vendor", function (namespace)
  -- <summary>
  -- 执行行动,不进行验证,失败直接报错
  -- </summary>
  namespace.class("ExecuteActionComponent", function (namespace)
    local DeserializeExecuteActionComponent, getAlias, setAlias, GetTypeId, Resolve, AfterResolve, ToString, class, 
    __ctor1__, __ctor2__
    __ctor1__ = function (this)
      System.base(this).__ctor__[1](this)
    end
    __ctor2__ = function (this, data)
      System.base(this).__ctor__[2](this, data)
      this.Operator = ""
      this.OriginConfig = data
      local dy =data
      dy.get=function(this,id)
        return this[id]
      end
      
      
      if wdmWuKongConfigs.LuaTable.GetObject(dy, "Action") ~= nil then
        this.Action = wdmWuKongConfigsCore.CfgAction.DeserializeCfgAction(dy["Action"])
      end
      
      if wdmWuKongConfigs.LuaTable.GetObject(dy, "ElementInfos") ~= nil then
        do
          -- Array<Vendor.CfgElementInfo>
          local _item =dy["ElementInfos"]
          local list =ListCfgElementInfo()
          local dict =wdmWuKongConfigs.LuaTable.GetDict(_item)
          for _, pair in System.each(dict) do
            local __v
            __v = wdmWuKongConfigsVendor.CfgElementInfo.DeserializeCfgElementInfo(pair[2])
            list:Add(__v)
          end
          this.ElementInfos = list:ToArray()
        end
      end
      
      
      this:SelfCheck()
    end
    DeserializeExecuteActionComponent = function (data)
      local obj =data
      data.get=function(this,id)
        return this[id]
      end
      return System.new(class, 2, obj)
    end
    getAlias, setAlias = System.property("Alias")
    GetTypeId = function (this)
      return -1565379944 --[[ExecuteActionComponent.__ID__]]
    end
    Resolve = function (this, _tables)
      System.base(this).Resolve(this, _tables)
      local default =this.Action
      if default ~= nil then
        default:Resolve(_tables)
      end
      for _, _e in System.each(this.ElementInfos) do
        local default =_e
        if default ~= nil then
          default:Resolve(_tables)
        end
      end
      this:InitOperator()
    end
    AfterResolve = function (this, _tables)
      System.base(this).AfterResolve(this, _tables)
      local default =this.Action
      if default ~= nil then
        default:AfterResolve(_tables)
      end
      for _, _e in System.each(this.ElementInfos) do
        local default =_e
        if default ~= nil then
          default:AfterResolve(_tables)
        end
      end
    end
    ToString = function (this)
      return ""
    end
    class = {
      base = function (out)
        return {
          out.wdm.WuKong.Configs.Vendor.Component
        }
      end,
      DeserializeExecuteActionComponent = DeserializeExecuteActionComponent,
      Alias = "执行行动",
      getAlias = getAlias,
      setAlias = setAlias,
      GetTypeId = GetTypeId,
      Resolve = Resolve,
      AfterResolve = AfterResolve,
      ToString = ToString,
      __ctor__ = {
        __ctor1__,
        __ctor2__
      },
      __metadata__ = function (out)
        return {
          methods = {
            { ".ctor", 0x6, __ctor1__ },
            { ".ctor", 0x106, __ctor2__, System.Object },
            { "AfterResolve", 0x106, AfterResolve, out.wdm.WuKong.Configs.Tables },
            { "DeserializeExecuteActionComponent", 0x18E, DeserializeExecuteActionComponent, System.Object, class },
            { "GetTypeId", 0x86, GetTypeId, System.Int32 },
            { "Resolve", 0x106, Resolve, System.Dictionary(System.String, System.Object) },
            { "ToString", 0x86, ToString, System.String }
          },
          properties = {
            { "Alias", 0x106, System.String, getAlias, setAlias }
          },
          fields = {
            { "Action", 0x6, out.wdm.WuKong.Configs.Core.CfgAction },
            { "ElementInfos", 0x6, System.Array(out.wdm.WuKong.Configs.Vendor.CfgElementInfo) }
          },
          class = { 0x6 }
        }
      end
    }
    return class
  end)
end)
return {}