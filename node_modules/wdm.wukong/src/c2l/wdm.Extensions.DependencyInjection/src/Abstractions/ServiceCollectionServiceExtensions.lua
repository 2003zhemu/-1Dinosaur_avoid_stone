-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmDependencyInjection
local wdmToolkitDiagnostics
local __importGlobal =function (out)
  wdmDependencyInjection = out.wdm.Extensions.DependencyInjection
  wdmToolkitDiagnostics = out.wdm.Toolkit.Diagnostics
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.DependencyInjection", function (namespace)
  -- <summary>
  -- Extension methods for adding services to an <see cref="IServiceCollection" />.
  -- </summary>
  namespace.class("ServiceCollectionServiceExtensions", function (namespace)
    local AddTransient, AddTransient1, AddTransient2, AddTransient3, AddTransient4, AddTransient5, AddTransient6, AddScoped, 
    AddScoped1, AddScoped2, AddScoped3, AddScoped4, AddScoped5, AddScoped6, AddSingleton, AddSingleton1, 
    AddSingleton2, AddSingleton3, AddSingleton4, AddSingleton5, AddSingleton6, AddSingleton7, AddSingleton8, Add, 
    Add1
    -- <summary>
    -- Adds a transient service of the type specified in <paramref name="serviceType"/> with an
    -- implementation of the type specified in <paramref name="implementationType"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="serviceType">The type of the service to register.</param>
    -- <param name="implementationType">The implementation type of the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Transient"/>
    AddTransient = function (services, serviceType, implementationType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationType)
      
      return Add(services, serviceType, implementationType, wdmDependencyInjection.ServiceLifetime.Transient)
    end
    -- <summary>
    -- Adds a transient service of the type specified in <paramref name="serviceType"/> with a
    -- factory specified in <paramref name="implementationFactory"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="serviceType">The type of the service to register.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Transient"/>
    AddTransient1 = function (services, serviceType, implementationFactory)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      return Add1(services, serviceType, implementationFactory, wdmDependencyInjection.ServiceLifetime.Transient)
    end
    -- <summary>
    -- Adds a transient service of the type specified in <typeparamref name="TService"/> with an
    -- implementation type specified in <typeparamref name="TImplementation"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <typeparam name="TImplementation">The type of the implementation to use.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Transient"/>
    AddTransient2 = function (services, TService, TImplementation)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      
      return AddTransient(services, System.typeof(TService), System.typeof(TImplementation))
    end
    -- <summary>
    -- Adds a transient service of the type specified in <paramref name="serviceType"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="serviceType">The type of the service to register and the implementation to use.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Transient"/>
    AddTransient3 = function (services, serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      
      return AddTransient(services, serviceType, serviceType)
    end
    -- <summary>
    -- Adds a transient service of the type specified in <typeparamref name="TService"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Transient"/>
    AddTransient4 = function (services, TService)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      
      return AddTransient3(services, System.typeof(TService))
    end
    -- <summary>
    -- Adds a transient service of the type specified in <typeparamref name="TService"/> with a
    -- factory specified in <paramref name="implementationFactory"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Transient"/>
    AddTransient5 = function (services, implementationFactory, TService)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      return AddTransient1(services, System.typeof(TService), implementationFactory)
    end
    -- <summary>
    -- Adds a transient service of the type specified in <typeparamref name="TService"/> with an
    -- implementation type specified in <typeparamref name="TImplementation" /> using the
    -- factory specified in <paramref name="implementationFactory"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <typeparam name="TImplementation">The type of the implementation to use.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Transient"/>
    AddTransient6 = function (services, implementationFactory, TService, TImplementation)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      return AddTransient1(services, System.typeof(TService), implementationFactory)
    end
    -- <summary>
    -- Adds a scoped service of the type specified in <paramref name="serviceType"/> with an
    -- implementation of the type specified in <paramref name="implementationType"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="serviceType">The type of the service to register.</param>
    -- <param name="implementationType">The implementation type of the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Scoped"/>
    AddScoped = function (services, serviceType, implementationType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationType)
      
      return Add(services, serviceType, implementationType, wdmDependencyInjection.ServiceLifetime.Scoped)
    end
    -- <summary>
    -- Adds a scoped service of the type specified in <paramref name="serviceType"/> with a
    -- factory specified in <paramref name="implementationFactory"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="serviceType">The type of the service to register.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Scoped"/>
    AddScoped1 = function (services, serviceType, implementationFactory)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      return Add1(services, serviceType, implementationFactory, wdmDependencyInjection.ServiceLifetime.Scoped)
    end
    -- <summary>
    -- Adds a scoped service of the type specified in <typeparamref name="TService"/> with an
    -- implementation type specified in <typeparamref name="TImplementation"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <typeparam name="TImplementation">The type of the implementation to use.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Scoped"/>
    AddScoped2 = function (services, TService, TImplementation)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      
      return AddScoped(services, System.typeof(TService), System.typeof(TImplementation))
    end
    -- <summary>
    -- Adds a scoped service of the type specified in <paramref name="serviceType"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="serviceType">The type of the service to register and the implementation to use.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Scoped"/>
    AddScoped3 = function (services, serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      
      return AddScoped(services, serviceType, serviceType)
    end
    -- <summary>
    -- Adds a scoped service of the type specified in <typeparamref name="TService"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Scoped"/>
    AddScoped4 = function (services, TService)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      
      return AddScoped3(services, System.typeof(TService))
    end
    -- <summary>
    -- Adds a scoped service of the type specified in <typeparamref name="TService"/> with a
    -- factory specified in <paramref name="implementationFactory"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Scoped"/>
    AddScoped5 = function (services, implementationFactory, TService)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      return AddScoped1(services, System.typeof(TService), implementationFactory)
    end
    -- <summary>
    -- Adds a scoped service of the type specified in <typeparamref name="TService"/> with an
    -- implementation type specified in <typeparamref name="TImplementation" /> using the
    -- factory specified in <paramref name="implementationFactory"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <typeparam name="TImplementation">The type of the implementation to use.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Scoped"/>
    AddScoped6 = function (services, implementationFactory, TService, TImplementation)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      return AddScoped1(services, System.typeof(TService), implementationFactory)
    end
    -- <summary>
    -- Adds a singleton service of the type specified in <paramref name="serviceType"/> with an
    -- implementation of the type specified in <paramref name="implementationType"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="serviceType">The type of the service to register.</param>
    -- <param name="implementationType">The implementation type of the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Singleton"/>
    AddSingleton = function (services, serviceType, implementationType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationType)
      
      return Add(services, serviceType, implementationType, wdmDependencyInjection.ServiceLifetime.Singleton)
    end
    -- <summary>
    -- Adds a singleton service of the type specified in <paramref name="serviceType"/> with a
    -- factory specified in <paramref name="implementationFactory"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="serviceType">The type of the service to register.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Singleton"/>
    AddSingleton1 = function (services, serviceType, implementationFactory)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      return Add1(services, serviceType, implementationFactory, wdmDependencyInjection.ServiceLifetime.Singleton)
    end
    -- <summary>
    -- Adds a singleton service of the type specified in <typeparamref name="TService"/> with an
    -- implementation type specified in <typeparamref name="TImplementation"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <typeparam name="TImplementation">The type of the implementation to use.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Singleton"/>
    AddSingleton2 = function (services, TService, TImplementation)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      
      return AddSingleton(services, System.typeof(TService), System.typeof(TImplementation))
    end
    -- <summary>
    -- Adds a singleton service of the type specified in <paramref name="serviceType"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="serviceType">The type of the service to register and the implementation to use.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Singleton"/>
    AddSingleton3 = function (services, serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      
      return AddSingleton(services, serviceType, serviceType)
    end
    -- <summary>
    -- Adds a singleton service of the type specified in <typeparamref name="TService"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Singleton"/>
    AddSingleton4 = function (services, TService)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      
      return AddSingleton3(services, System.typeof(TService))
    end
    -- <summary>
    -- Adds a singleton service of the type specified in <typeparamref name="TService"/> with a
    -- factory specified in <paramref name="implementationFactory"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Singleton"/>
    AddSingleton5 = function (services, implementationFactory, TService)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      return AddSingleton1(services, System.typeof(TService), implementationFactory)
    end
    -- <summary>
    -- Adds a singleton service of the type specified in <typeparamref name="TService"/> with an
    -- implementation type specified in <typeparamref name="TImplementation" /> using the
    -- factory specified in <paramref name="implementationFactory"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <typeparam name="TImplementation">The type of the implementation to use.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Singleton"/>
    AddSingleton6 = function (services, implementationFactory, TService, TImplementation)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      return AddSingleton1(services, System.typeof(TService), implementationFactory)
    end
    -- <summary>
    -- Adds a singleton service of the type specified in <paramref name="serviceType"/> with an
    -- instance specified in <paramref name="implementationInstance"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="serviceType">The type of the service to register.</param>
    -- <param name="implementationInstance">The instance of the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Singleton"/>
    AddSingleton7 = function (services, serviceType, implementationInstance)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationInstance)
      
      local serviceDescriptor =System.new(wdmDependencyInjection.ServiceDescriptor, 2, serviceType, implementationInstance)
      services:Add(serviceDescriptor)
      return services
    end
    -- <summary>
    -- Adds a singleton service of the type specified in <typeparamref name="TService" /> with an
    -- instance specified in <paramref name="implementationInstance"/> to the
    -- specified <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/> to add the service to.</param>
    -- <param name="implementationInstance">The instance of the service.</param>
    -- <returns>A reference to this instance after the operation has completed.</returns>
    -- <seealso cref="ServiceLifetime.Singleton"/>
    AddSingleton8 = function (services, implementationInstance, TService)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationInstance)
      
      return AddSingleton7(services, System.typeof(TService), implementationInstance)
    end
    Add = function (collection, serviceType, implementationType, lifetime)
      local descriptor =wdmDependencyInjection.ServiceDescriptor(serviceType, implementationType, lifetime)
      collection:Add(descriptor)
      return collection
    end
    Add1 = function (collection, serviceType, implementationFactory, lifetime)
      local descriptor =System.new(wdmDependencyInjection.ServiceDescriptor, 3, serviceType, implementationFactory, lifetime)
      collection:Add(descriptor)
      return collection
    end
    return {
      AddTransient = AddTransient,
      AddTransient1 = AddTransient1,
      AddTransient2 = AddTransient2,
      AddTransient3 = AddTransient3,
      AddTransient4 = AddTransient4,
      AddTransient5 = AddTransient5,
      AddTransient6 = AddTransient6,
      AddScoped = AddScoped,
      AddScoped1 = AddScoped1,
      AddScoped2 = AddScoped2,
      AddScoped3 = AddScoped3,
      AddScoped4 = AddScoped4,
      AddScoped5 = AddScoped5,
      AddScoped6 = AddScoped6,
      AddSingleton = AddSingleton,
      AddSingleton1 = AddSingleton1,
      AddSingleton2 = AddSingleton2,
      AddSingleton3 = AddSingleton3,
      AddSingleton4 = AddSingleton4,
      AddSingleton5 = AddSingleton5,
      AddSingleton6 = AddSingleton6,
      AddSingleton7 = AddSingleton7,
      AddSingleton8 = AddSingleton8,
      __metadata__ = function (out)
        return {
          methods = {
            { "Add", 0x489, Add1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Delegate(System.IServiceProvider, System.Object), out.wdm.Extensions.DependencyInjection.ServiceLifetime, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "Add", 0x489, Add, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Type, out.wdm.Extensions.DependencyInjection.ServiceLifetime, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "AddScoped", 0x2028E, AddScoped6, function (TService, TImplementation) return out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Delegate(System.IServiceProvider, TImplementation), out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddScoped", 0x1018E, AddScoped4, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddScoped", 0x28E, AddScoped3, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "AddScoped", 0x2018E, AddScoped2, function (TService, TImplementation) return out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddScoped", 0x38E, AddScoped1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Delegate(System.IServiceProvider, System.Object), out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "AddScoped", 0x1028E, AddScoped5, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Delegate(System.IServiceProvider, TService), out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddScoped", 0x38E, AddScoped, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Type, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "AddSingleton", 0x2018E, AddSingleton2, function (TService, TImplementation) return out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddSingleton", 0x1028E, AddSingleton8, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection, TService, out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddSingleton", 0x38E, AddSingleton7, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Object, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "AddSingleton", 0x2028E, AddSingleton6, function (TService, TImplementation) return out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Delegate(System.IServiceProvider, TImplementation), out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddSingleton", 0x1028E, AddSingleton5, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Delegate(System.IServiceProvider, TService), out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddSingleton", 0x1018E, AddSingleton4, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddSingleton", 0x38E, AddSingleton, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Type, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "AddSingleton", 0x38E, AddSingleton1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Delegate(System.IServiceProvider, System.Object), out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "AddSingleton", 0x28E, AddSingleton3, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "AddTransient", 0x2028E, AddTransient6, function (TService, TImplementation) return out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Delegate(System.IServiceProvider, TImplementation), out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddTransient", 0x2018E, AddTransient2, function (TService, TImplementation) return out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddTransient", 0x28E, AddTransient3, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "AddTransient", 0x1018E, AddTransient4, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddTransient", 0x1028E, AddTransient5, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Delegate(System.IServiceProvider, TService), out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "AddTransient", 0x38E, AddTransient1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Delegate(System.IServiceProvider, System.Object), out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "AddTransient", 0x38E, AddTransient, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Type, out.wdm.Extensions.DependencyInjection.IServiceCollection }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}