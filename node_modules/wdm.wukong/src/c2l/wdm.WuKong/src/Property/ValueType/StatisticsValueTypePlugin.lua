-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
local wdmContainer
local wdmWuKongConfigs
local wdmWuKongConfigsProperty
local wdmWuKongContainer
local wdmWuKongCore
local HashSetIContainer
local __importGlobal =function (out)
  wdmContainer = out.wdm.WuKong.Abstractions.Container
  wdmWuKongConfigs = out.wdm.WuKong.Configs
  wdmWuKongConfigsProperty = out.wdm.WuKong.Configs.Property
  wdmWuKongContainer = out.wdm.WuKong.Container
  wdmWuKongCore = out.wdm.WuKong.Core
  HashSetIContainer = System.HashSet(_G.wdm.WuKong.Abstractions.Container.IContainer)
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Property", function (namespace)
  -- <summary>
  -- 统计类型
  -- </summary>
  namespace.class("StatisticsValueTypePlugin", function (namespace)
    local GetTargetContainers, Report
    GetTargetContainers = function (this, container)
      local targets =HashSetIContainer()
      
      for _, observer in System.each(this.Cfg.Observers) do
        local length =#observer.Location.Path
        local lastChar =System.String.get(observer.Location.Path, length - 1)
        
        if lastChar == 42 --[['*']] then
          local root =wdmWuKongContainer.ContainerExtension.GetRootContainer(container)
          local parent =wdmWuKongCore.PluginOwnerPathExtension.Find2(root, System.S(observer.Location.Path):Substring(0, length - 2))
          for _, child in System.each(parent:getChildren()) do
            targets:Add(child)
          end
        else
          targets:Add(wdmWuKongConfigs.CfgContainerLocationExtension.GetTargetContainer(observer.Location, container))
        end
      end
      
      return Linq.ToArray(targets)
    end
    Report = function (this, container, target, actionName, args)
      for _, observer in System.each(this.Cfg.Observers) do
        local continue_lua
        repeat
          if actionName ~= observer.ActionName then
            continue_lua = true
            break
          end
          
          local length =#observer.Location.Path
          if System.String.get(observer.Location.Path, length - 1) == 42 --[['*']] then
            local root =wdmWuKongContainer.ContainerExtension.GetRootContainer(container)
            local parent =wdmWuKongCore.PluginOwnerPathExtension.Find2(root, System.S(observer.Location.Path):Substring(0, #observer.Location.Path - 2))
            if parent ~= target:getParent() then
              continue_lua = true
              break
            end
          else
            if wdmWuKongConfigs.CfgContainerLocationExtension.GetTargetContainer(observer.Location, container) ~= target then
              continue_lua = true
              break
            end
          end
          
          local slot =wdmWuKongContainer.ContainerExtension.AssertAsSlot(container)
          local count =observer.StatisticsType
          if System.is(count, wdmWuKongConfigsProperty.StatisticsTypeCount) then
            slot:getElement():setCount(slot:getElement():getCount() + 1)
          else
            local a =observer.StatisticsType
            if System.is(a, wdmWuKongConfigsProperty.StatisticsTypeAdd) then
              local index =a.ArgIndex - 1
              if index >= #args then
                System.throw(System.IndexOutOfRangeException("统计配置的累加参数越界"))
              end
              -- 不知道为啥, 原来是需要+1的, 现在注释掉才不报错
              -- // lua特殊对待
              -- /*
              --  [[
              --     index = index + 1;
              -- ]]
              --  */
              local arg =args:get(index)
              slot:getElement():setCount(slot:getElement():getCount() + arg)
            else
              System.throw(System.NotImplementedException())
            end
          end
          continue_lua = true
        until 1
        if not continue_lua then
          break
        end
      end
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.Configs.OperatorBase_1(out.wdm.WuKong.Configs.Property.CfgStatisticsValueType),
          out.wdm.WuKong.Property.IValueTypePlugin,
          out.wdm.WuKong.Container.IActionObserverPlugin
        }
      end,
      GetTargetContainers = GetTargetContainers,
      Report = Report,
      __metadata__ = function (out)
        return {
          methods = {
            { "GetTargetContainers", 0x186, GetTargetContainers, out.wdm.WuKong.Abstractions.Container.IContainer, System.Array(out.wdm.WuKong.Abstractions.Container.IContainer) },
            { "Report", 0x406, Report, out.wdm.WuKong.Abstractions.Container.IContainer, out.wdm.WuKong.Abstractions.Container.IContainer, System.String, System.Array(System.Object) }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}