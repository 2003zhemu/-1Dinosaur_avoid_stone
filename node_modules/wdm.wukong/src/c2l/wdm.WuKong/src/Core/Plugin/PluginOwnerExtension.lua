-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmContainer
local wdmWuKongContainer
local __importGlobal =function (out)
  wdmContainer = out.wdm.WuKong.Abstractions.Container
  wdmWuKongContainer = out.wdm.WuKong.Container
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Core", function (namespace)
  namespace.class("PluginOwnerExtension", function (namespace)
    local GetUserPath, GetRootContainer
    -- <summary>
    -- 获取对象在玩家容器内的位置
    -- </summary>
    -- <param name="owner"></param>
    -- <returns></returns>
    -- <exception cref="NotImplementedException"></exception>
    GetUserPath = function (owner)
      local path =""
      local c =owner
      if System.is(c, wdmWuKongContainer.ElementContainer) then
        path = c:getPath()
      else
        local a =owner
        if System.is(a, wdmWuKongContainer.AggregateContainer) then
          path = a:getPath()
        else
          local s =owner
          if System.is(s, wdmWuKongContainer.Slot) then
            path = s:getPath()
          else
            System.throw(System.NotImplementedException())
          end
        end
      end
      
      if System.String.IsNullOrEmpty(path) then
        return ""
      end
      
      return System.toString("/") .. System.toString(path)
    end
    GetRootContainer = function (owner)
      local c =owner
      if System.is(c, wdmContainer.IContainer) then
        local default
        if c:getParent() ~= nil then
          default = GetRootContainer(c:getParent())
        else
          default = c
        end
        return default
      else
        local s =owner
        if System.is(s, wdmWuKongContainer.Slot) then
          return GetRootContainer(s:getEParent())
        end
      end
      
      System.throw(System.NotImplementedException())
    end
    return {
      GetUserPath = GetUserPath,
      GetRootContainer = GetRootContainer,
      __metadata__ = function (out)
        return {
          methods = {
            { "GetRootContainer", 0x18E, GetRootContainer, out.wdm.WuKong.Abstractions.Core.IHasPlugin, out.wdm.WuKong.Abstractions.Container.IContainer },
            { "GetUserPath", 0x18E, GetUserPath, out.wdm.WuKong.Abstractions.Core.IHasPlugin, System.String }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}