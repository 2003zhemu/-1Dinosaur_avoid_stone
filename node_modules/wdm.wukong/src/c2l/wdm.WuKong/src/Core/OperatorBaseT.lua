-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongConfigs
local wdmWuKongCore
local __importGlobal =function (out)
  wdmWuKongConfigs = out.wdm.WuKong.Configs
  wdmWuKongCore = out.wdm.WuKong.Core
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Configs", function (namespace)
  namespace.class("OperatorBase_1", function (namespace)
    return function (T)
      --检查泛型约束
      local function checkConstraint(generic, check) 
        local tGeneric = System.typeof(generic)
        local tCheck = System.typeof(check)
        if not (tGeneric:IsAssignableFrom(tCheck) or tGeneric == tCheck) then
          local message = tGeneric:getFullName().." on "..tCheck:getFullName()
          System.throw(System.ArgumentException(message))
        end
      end
      checkConstraint(_G.wdm.WuKong.Configs.BeanBase, T)
      local getTables, Init
      getTables = function (this)
        return wdmWuKongCore.CoreTablesProvider.GetTables()
      end
      Init = function (this, config)
        this.Cfg = wdmWuKongConfigs.BeanBaseExtension.CfgAssertAs(config, T)
      end
      return {
        base = function (out)
          return {
            out.wdm.WuKong.Core.OperatorBase,
            out.wdm.WuKong.Abstractions.Core.IPlugin_1(T)
          }
        end,
        getTables = getTables,
        Init = Init,
        __metadata__ = function (out)
          return {
            fields = {
              { "Cfg", 0x6, T }
            },
            properties = {
              { "Tables", 0x203, out.wdm.WuKong.Configs.Tables, getTables }
            },
            methods = {
              { "Init", 0x106, Init, out.wdm.WuKong.Configs.BeanBase }
            },
            class = { 0x106, T }
          }
        end
      }
    end
  end)
end)
return {}