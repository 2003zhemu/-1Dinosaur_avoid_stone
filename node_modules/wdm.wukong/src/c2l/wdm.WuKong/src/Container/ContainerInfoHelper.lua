-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmContainer
local wdmWuKongContainer
local wdmWuKongCore
local __importGlobal =function (out)
  wdmContainer = out.wdm.WuKong.Abstractions.Container
  wdmWuKongContainer = out.wdm.WuKong.Container
  wdmWuKongCore = out.wdm.WuKong.Core
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Container", function (namespace)
  namespace.class("ContainerInfoHelper", function (namespace)
    local GetContainerInfo, _getContainer
    -- <summary>
    -- 根据路径获取容器信息
    -- 路径可以带能力名称
    -- </summary>
    -- <param name="location"></param>
    -- <param name="owner"></param>
    -- <returns></returns>
    GetContainerInfo = function (location, owner)
      local container =_getContainer(location, owner)
      local info =wdmWuKongContainer.ContainerInfo()
      info.Path = container:getPath()
      info.Id = container:getId()
      
      local e =container
      if System.is(e, wdmWuKongContainer.ElementContainer) then
        info.Type = e.ElementType
      else
        local s =container
        if System.is(s, wdmWuKongContainer.Slot) then
          info.Type = s:getParent().ElementType
        end
      end
      
      return info
    end
    _getContainer = function (location, owner)
      local split =System.S(location):Split("?", 0)
      
      -- 分割
      local path =split:get(0)
      
      return wdmWuKongCore.PluginOwnerPathExtension.FindRequired2(owner, path, wdmContainer.IContainer)
    end
    return {
      GetContainerInfo = GetContainerInfo,
      __metadata__ = function (out)
        return {
          methods = {
            { "GetContainerInfo", 0x28E, GetContainerInfo, System.String, out.wdm.WuKong.Abstractions.Container.IContainer, out.wdm.WuKong.Container.ContainerInfo },
            { "_getContainer", 0x289, _getContainer, System.String, out.wdm.WuKong.Abstractions.Container.IContainer, out.wdm.WuKong.Abstractions.Container.IContainer }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}