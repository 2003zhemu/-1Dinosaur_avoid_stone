-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongAbstractions
local __importGlobal =function (out)
  wdmWuKongAbstractions = out.wdm.WuKong.Abstractions
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Container", function (namespace)
  namespace.class("ContainerExtraInfoPlugin", function (namespace)
    local _getExtraValueLua, GetExtraInfoString, GetExtraInfoLong, GetExtraInfoFloat
    _getExtraValueLua = function (this, e, key)
      local config
      
      local e1 =e
      if System.is(e1, wdmWuKongAbstractions.IHasOriginConfig) then
        config = e1:getOriginConfig()
      else
        local e2 =e
        if System.is(e2, wdmWuKongAbstractions.IHasOriginConfigOrNull) then
          config = e2:getOriginConfig()
        else
          System.throw(System.NotImplementedException())
        end
      end
      
      local result =config[key]
      if result == nil then
        System.throw(System.KeyNotFoundException(key))
      end
      return result
    end
    -- <summary>
    -- 获取配置的扩展数据, 必须执行key
    -- CSharp 返回空字符串(扩展数据无法在CSharp拿到)
    -- </summary>
    GetExtraInfoString = function (this, e, key)
      return _getExtraValueLua(this, e, key)
    end
    -- <summary>
    -- 获取配置的扩展数据, 必须执行key
    -- CSharp 0(扩展数据无法在CSharp拿到)
    -- </summary>
    GetExtraInfoLong = function (this, e, key)
      return _getExtraValueLua(this, e, key)
    end
    -- <summary>
    -- 获取配置的扩展数据, 必须执行key
    -- CSharp 0(扩展数据无法在CSharp拿到)
    -- </summary>
    GetExtraInfoFloat = function (this, e, key)
      return _getExtraValueLua(this, e, key)
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.Configs.OperatorBase_1(out.wdm.WuKong.Configs.Container.CfgContainerExtraInfoPlugin)
        }
      end,
      GetExtraInfoString = GetExtraInfoString,
      GetExtraInfoLong = GetExtraInfoLong,
      GetExtraInfoFloat = GetExtraInfoFloat,
      __metadata__ = function (out)
        return {
          methods = {
            { "GetExtraInfoFloat", 0x286, GetExtraInfoFloat, out.wdm.WuKong.Abstractions.Container.IContainer, System.String, System.Single, out.wdm.WuKong.Abstractions.Core.QueryAttribute("获取扩展数据(float)") },
            { "GetExtraInfoLong", 0x286, GetExtraInfoLong, out.wdm.WuKong.Abstractions.Container.IContainer, System.String, System.Int64, out.wdm.WuKong.Abstractions.Core.QueryAttribute("获取扩展数据(long)") },
            { "GetExtraInfoString", 0x286, GetExtraInfoString, out.wdm.WuKong.Abstractions.Container.IContainer, System.String, System.String, out.wdm.WuKong.Abstractions.Core.QueryAttribute("获取扩展数据(string)") },
            { "_getExtraValueLua", 0x281, _getExtraValueLua, out.wdm.WuKong.Abstractions.Container.IContainer, System.String, System.Object }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}