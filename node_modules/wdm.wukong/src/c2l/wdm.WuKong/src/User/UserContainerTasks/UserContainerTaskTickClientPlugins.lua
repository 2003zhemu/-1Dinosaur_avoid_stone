-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
local wdmContainer
local wdmWuKongContainer
local KeyValuePairIContainerIClientTickablePlugin
local ListKeyValuePairIContainerIClientTickablePlugin
local __importGlobal =function (out)
  wdmContainer = out.wdm.WuKong.Abstractions.Container
  wdmWuKongContainer = out.wdm.WuKong.Container
  KeyValuePairIContainerIClientTickablePlugin = System.KeyValuePair(_G.wdm.WuKong.Abstractions.Container.IContainer, _G.wdm.WuKong.Container.IClientTickablePlugin)
  ListKeyValuePairIContainerIClientTickablePlugin = System.List(KeyValuePairIContainerIClientTickablePlugin)
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.User.UserContainerTasks", function (namespace)
  namespace.class("UserContainerTaskTickClientPlugins", function (namespace)
    local _startTickLoop, _updateTickables, _startTick, Dispose, Start, __ctor__
    __ctor__ = function (this, userContainer)
      this._tickablePlugins = ListKeyValuePairIContainerIClientTickablePlugin()
      this._userContainer = userContainer
    end
    _startTickLoop = function (this)
      System.Thread(function ()
        while this.IsStart do
          for _, pair in System.each(this._tickablePlugins) do
            System.try(function ()
              pair[2]:TickClient(pair[1])
            end, function (default)
              local e =default
              warn(e)
            end)
          end
          System.Thread.Sleep(1000)
        end
      end):Start()
    end
    _updateTickables = function (this, container)
      if container:getPlugins() ~= nil then
        for _, plugin in System.each(container:getPlugins()) do
          local t =plugin
          if System.is(t, wdmWuKongContainer.IClientTickablePlugin) then
            local pair =KeyValuePairIContainerIClientTickablePlugin(container, t)
            
            this._tickablePlugins:Add(pair)
          end
        end
      end
      
      if container:getChildren() == nil then
        return
      end
      
      for _, child in System.each(container:getChildren()) do
        _updateTickables(this, child)
      end
    end
    _startTick = function (this)
      this._tickablePlugins:Clear()
      _updateTickables(this, this._userContainer)
      if Linq.Any(this._tickablePlugins) then
        _startTickLoop(this)
      end
    end
    Dispose = function (this)
      this.IsStart = false
      this._tickablePlugins:Clear()
    end
    Start = function (this)
      this.IsStart = true
      _startTick(this)
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.User.UserContainerTasks.IUserContainerTasks
        }
      end,
      IsStart = false,
      Dispose = Dispose,
      Start = Start,
      __ctor__ = __ctor__,
      __metadata__ = function (out)
        return {
          fields = {
            { "_tickablePlugins", 0x1, System.List(System.KeyValuePair(out.wdm.WuKong.Abstractions.Container.IContainer, out.wdm.WuKong.Container.IClientTickablePlugin)) },
            { "_userContainer", 0x1, out.wdm.WuKong.User.UserContainer }
          },
          methods = {
            { ".ctor", 0x106, nil, out.wdm.WuKong.User.UserContainer },
            { "Dispose", 0x6, Dispose },
            { "Start", 0x6, Start },
            { "_startTick", 0x1, _startTick },
            { "_startTickLoop", 0x1, _startTickLoop },
            { "_updateTickables", 0x101, _updateTickables, out.wdm.WuKong.Abstractions.Container.IContainer }
          },
          properties = {
            { "IsStart", 0x6, System.Boolean }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}