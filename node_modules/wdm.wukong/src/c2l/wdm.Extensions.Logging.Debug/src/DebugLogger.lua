-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmExtensionsLogging
local wdmToolkitDiagnostics
local __importGlobal =function (out)
  wdmExtensionsLogging = out.wdm.Extensions.Logging
  wdmToolkitDiagnostics = out.wdm.Toolkit.Diagnostics
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Logging", function (namespace)
  -- <summary>
  -- A logger that writes messages in the debug output window only when a debugger is attached.
  -- </summary>
  namespace.class("DebugLogger", function (namespace)
    local BeginScope, IsEnabled, Log, DebugWriteLine, __ctor__
    -- <summary>
    -- Initializes a new instance of the <see cref="DebugLogger"/> class.
    -- </summary>
    -- <param name="name">The name of the logger.</param>
    __ctor__ = function (this, name)
      this._name = name
    end
    -- <inheritdoc />
    BeginScope = function (this, state, TState)
      return wdmExtensionsLogging.NullScope.Instance
    end
    -- <inheritdoc />
    IsEnabled = function (this, logLevel)
      -- If the filter is null, everything is enabled
      -- unless the debugger is not attached
      return true
    end
    -- <inheritdoc />
    Log = function (this, logLevel, eventId, state, exception, formatter, TState)
      if not IsEnabled(this, logLevel) then
        return
      end
      
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(formatter)
      
      local message =formatter(state, exception, TState)
      
      if System.String.IsNullOrEmpty(message) then
        return
      end
      
      message = System.toString(System.toString(System.EnumToString(logLevel, wdmExtensionsLogging.LogLevel)) .. System.toString(": ")) .. System.toString(message)
      
      if exception ~= nil then
        message = System.toString(message) .. System.toString((System.toString(System.toString(System.Environment.getNewLine()) .. System.toString(System.Environment.getNewLine())) .. System.toString(System.toString(exception))))
      end
      
      DebugWriteLine(logLevel, message, this._name)
    end
    DebugWriteLine = function (logLevel, message, name)
      print(message)
    end
    return {
      base = function (out)
        return {
          out.wdm.Extensions.Logging.ILogger
        }
      end,
      BeginScope = BeginScope,
      IsEnabled = IsEnabled,
      Log = Log,
      __ctor__ = __ctor__,
      __metadata__ = function (out)
        return {
          fields = {
            { "_name", 0x1, System.String }
          },
          methods = {
            { ".ctor", 0x106, nil, System.String },
            { "BeginScope", 0x10186, BeginScope, function (TState) return TState, System.IDisposable end },
            { "DebugWriteLine", 0x309, DebugWriteLine, out.wdm.Extensions.Logging.LogLevel, System.String, System.String },
            { "IsEnabled", 0x186, IsEnabled, out.wdm.Extensions.Logging.LogLevel, System.Boolean },
            { "Log", 0x10506, Log, function (TState) return out.wdm.Extensions.Logging.LogLevel, out.wdm.Extensions.Logging.EventId, TState, System.Exception, System.Delegate(TState, System.Exception, System.String) end }
          },
          class = { 0x4 }
        }
      end
    }
  end)
end)
return {}