-- Generated by CSharp.lua Compiler
-- ==++==
-- 
--   Copyright (c) Microsoft Corporation.  All rights reserved.
-- 
-- ==--==
--[[============================================================
**
** Class:  Path
** 
** <OWNER>Microsoft</OWNER>
**
**
** Purpose: A collection of path manipulation methods.
**
**
===========================================================]]
local System =_G.System
local Linq =System.Linq.Enumerable
local SystemIO =_G.System.IO
local ArrayChar =System.Array(System.Char)
local SystemIO
local wdmRuntime
local __importGlobal =function (out)
  SystemIO = out.System.IO
  wdmRuntime = out.wdm.Runtime
end
System.import(__importGlobal)
System.namespace("System.IO", function (namespace)
  -- Provides methods for processing directory strings in an ideally
  -- cross-platform manner.  Most of the methods don't do a complete
  -- full parsing (such as examining a UNC hostname), but they will
  -- handle most string operations.  
  -- 
  -- File names cannot contain backslash (\), slash (/), colon (:),
  -- asterick (*), question mark (?), quote ("), less than (<;), 
  -- greater than (>;), or pipe (|).  The first three are used as directory
  -- separators on various platforms.  Asterick and question mark are treated
  -- as wild cards.  Less than, Greater than, and pipe all redirect input
  -- or output from a program to a file or some combination thereof.  Quotes
  -- are special.
  -- 
  -- We are guaranteeing that Path.SeparatorChar is the correct 
  -- directory separator on all platforms, and we will support 
  -- Path.AltSeparatorChar as well.  To write cross platform
  -- code with minimal pain, you can use slash (/) as a directory separator in
  -- your strings.
  -- Class contains only static data, no need to serialize
  namespace.class("Path", function (namespace)
    local DirectorySeparatorChar, AltDirectorySeparatorChar, VolumeSeparatorChar, InvalidPathCharsWithAdditionalChecks, InvalidFileNameChars, PathSeparator, ChangeExtension, GetDirectoryName, 
    NormalizePath, NormalizePath1, NormalizePath2, NormalizePath3, NewNormalizePathLimitedChecks, InternalGetDirectoryName, GetRootLength, LegacyGetRootLength, 
    IsDirectorySeparator, GetInvalidPathChars, GetInvalidFileNameChars, GetExtension, GetFullPath, GetFileName, GetFileNameWithoutExtension, GetPathRoot, 
    GetTempPath, InternalGetFullPath, SplitFullPath, GetRandomFileName, GetTempFileName, InternalGetTempFileName, HasExtension, IsPathRooted, 
    Combine, Combine1, Combine2, Combine3, CombineNoChecks, s_Base32Char, ToBase32StringSuitableForDirName, CheckSearchPattern, 
    CheckInvalidPathChars, InternalCombine, static
    static = function (this)
      InvalidPathCharsWithAdditionalChecks = ArrayChar { 34 --[['"']], 60 --[['<']], 62 --[['>']], 124 --[['|']], 0 --[['\0']], 1 --[['\u0001']] --[[(Char)1]], 2 --[['\u0002']] --[[(Char)2]], 3 --[['\u0003']] --[[(Char)3]], 4 --[['\u0004']] --[[(Char)4]], 5 --[['\u0005']] --[[(Char)5]], 6 --[['\u0006']] --[[(Char)6]], 7 --[['\a']] --[[(Char)7]], 8 --[['\b']] --[[(Char)8]], 9 --[['\t']] --[[(Char)9]], 10 --[['\n']] --[[(Char)10]], 11 --[['\v']] --[[(Char)11]], 12 --[['\f']] --[[(Char)12]], 13 --[['\r']] --[[(Char)13]], 14 --[['\u000e']] --[[(Char)14]], 15 --[['\u000f']] --[[(Char)15]], 16 --[['\u0010']] --[[(Char)16]], 17 --[['\u0011']] --[[(Char)17]], 18 --[['\u0012']] --[[(Char)18]], 19 --[['\u0013']] --[[(Char)19]], 20 --[['\u0014']] --[[(Char)20]], 21 --[['\u0015']] --[[(Char)21]], 22 --[['\u0016']] --[[(Char)22]], 23 --[['\u0017']] --[[(Char)23]], 24 --[['\u0018']] --[[(Char)24]], 25 --[['\u0019']] --[[(Char)25]], 26 --[['\u001a']] --[[(Char)26]], 27 --[['\u001b']] --[[(Char)27]], 28 --[['\u001c']] --[[(Char)28]], 29 --[['\u001d']] --[[(Char)29]], 30 --[['\u001e']] --[[(Char)30]], 31 --[['\u001f']] --[[(Char)31]], 42 --[['*']], 63 --[['?']] }
      InvalidFileNameChars = ArrayChar { 34 --[['"']], 60 --[['<']], 62 --[['>']], 124 --[['|']], 0 --[['\0']], 1 --[['\u0001']] --[[(Char)1]], 2 --[['\u0002']] --[[(Char)2]], 3 --[['\u0003']] --[[(Char)3]], 4 --[['\u0004']] --[[(Char)4]], 5 --[['\u0005']] --[[(Char)5]], 6 --[['\u0006']] --[[(Char)6]], 7 --[['\a']] --[[(Char)7]], 8 --[['\b']] --[[(Char)8]], 9 --[['\t']] --[[(Char)9]], 10 --[['\n']] --[[(Char)10]], 11 --[['\v']] --[[(Char)11]], 12 --[['\f']] --[[(Char)12]], 13 --[['\r']] --[[(Char)13]], 14 --[['\u000e']] --[[(Char)14]], 15 --[['\u000f']] --[[(Char)15]], 16 --[['\u0010']] --[[(Char)16]], 17 --[['\u0011']] --[[(Char)17]], 18 --[['\u0012']] --[[(Char)18]], 19 --[['\u0013']] --[[(Char)19]], 20 --[['\u0014']] --[[(Char)20]], 21 --[['\u0015']] --[[(Char)21]], 22 --[['\u0016']] --[[(Char)22]], 23 --[['\u0017']] --[[(Char)23]], 24 --[['\u0018']] --[[(Char)24]], 25 --[['\u0019']] --[[(Char)25]], 26 --[['\u001a']] --[[(Char)26]], 27 --[['\u001b']] --[[(Char)27]], 28 --[['\u001c']] --[[(Char)28]], 29 --[['\u001d']] --[[(Char)29]], 30 --[['\u001e']] --[[(Char)30]], 31 --[['\u001f']] --[[(Char)31]], 58 --[[':']], 42 --[['*']], 63 --[['?']], 92 --[['\\']], 47 --[['/']] }
      s_Base32Char = ArrayChar { 97 --[['a']], 98 --[['b']], 99 --[['c']], 100 --[['d']], 101 --[['e']], 102 --[['f']], 103 --[['g']], 104 --[['h']], 105 --[['i']], 106 --[['j']], 107 --[['k']], 108 --[['l']], 109 --[['m']], 110 --[['n']], 111 --[['o']], 112 --[['p']], 113 --[['q']], 114 --[['r']], 115 --[['s']], 116 --[['t']], 117 --[['u']], 118 --[['v']], 119 --[['w']], 120 --[['x']], 121 --[['y']], 122 --[['z']], 48 --[['0']], 49 --[['1']], 50 --[['2']], 51 --[['3']], 52 --[['4']], 53 --[['5']] }
    end
    DirectorySeparatorChar = 47 --[['/']]
    AltDirectorySeparatorChar = 92 --[['\\']]
    VolumeSeparatorChar = 58 --[[':']]
    PathSeparator = 59 --[[';']]
    ChangeExtension = function (path, extension)
      if path ~= nil then
        CheckInvalidPathChars(path, false)
        
        local s =path
        do
          local i =#path
          while true do
            i = i - 1
            if not (i >= 0) then
              break
            end
            local ch =System.String.get(path, i)
            if ch == 46 --[['.']] then
              s = System.S(path):Substring(0, i)
              break
            end
            if ch == DirectorySeparatorChar or ch == AltDirectorySeparatorChar or ch == VolumeSeparatorChar then
              break
            end
          end
        end
        if extension ~= nil and #path ~= 0 then
          if #extension == 0 or System.String.get(extension, 0) ~= 46 --[['.']] then
            s = System.toString(s) .. System.toString(".")
          end
          s = System.toString(s) .. System.toString(extension)
        end
        return s
      end
      return nil
    end
    GetDirectoryName = function (path)
      return InternalGetDirectoryName(path)
    end
    NormalizePath = function (path, fullCheck)
      return NormalizePath2(path, fullCheck, 256)
    end
    NormalizePath1 = function (path, fullCheck, expandShortPaths)
      return NormalizePath3(path, fullCheck, 256, expandShortPaths)
    end
    NormalizePath2 = function (path, fullCheck, maxPathLength)
      return NormalizePath3(path, fullCheck, maxPathLength, true)
    end
    NormalizePath3 = function (path, fullCheck, maxPathLength, expandShortPaths)
      if SystemIO.PathInternal.IsExtended(path) then
        -- We can't really know what is valid for all cases of extended paths.
        --
        --  - object names can include other characters as well (':', '/', etc.)
        --  - even file objects have different rules (pipe names can contain most characters)
        --
        -- As such we will do no further analysis of extended paths to avoid blocking known and unknown
        -- scenarios as well as minimizing compat breaks should we block now and need to unblock later.
        return path
      end
      
      local normalizedPath =nil
      
      normalizedPath = NewNormalizePathLimitedChecks(path, maxPathLength, expandShortPaths)
      
      if System.String.IsNullOrWhiteSpace(normalizedPath) then
        System.throw(System.ArgumentException("Arg_PathIllegal"))
      end
      return normalizedPath
    end
    NewNormalizePathLimitedChecks = function (path, maxPathLength, expandShortPaths)
      local normalized =SystemIO.PathInternal.NormalizeDirectorySeparators(path)
      
      if SystemIO.PathInternal.IsPathTooLong(normalized) or SystemIO.PathInternal.AreSegmentsTooLong(normalized) then
        System.throw(SystemIO.PathTooLongException())
      end
      
      -- Under old logic certain subsets of paths containing colons were rejected. Some portion of that comes
      -- indirectly from FileIOPermissions, the rest comes from the section in LegacyNormalizePath below:
      --
      --   // To reject strings like "C:...\foo" and "C  :\foo"
      --   if (firstSegment && currentChar == VolumeSeparatorChar)
      --
      -- The superset of this now is PathInternal.HasInvalidVolumeSeparator().
      --
      -- Unfortunately a side effect of the old split logic is that some "bad" colon paths slip through when
      -- fullChecks=false. Notably this means that GetDirectoryName and GetPathRoot would allow URIs (although
      -- it would mangle them). A user could pass a "file://..." uri to GetDirectoryName(), get "file:\..." back,
      -- then pass it to Uri which fixes up the bad URI. One particular user code path for this is calling
      -- Assembly.CodePath and trying to get the directory before passing to the Uri class.
      --
      -- To ease transitioning code forward we'll allow all "bad" colon paths through when we are doing
      -- limited checks. If we want to add this back (under a quirk perhaps), we would need to conditionalize
      -- for Device paths as follows:
      --
      --   if (!PathInternal.IsDevice(normalized) && PathInternal.HasInvalidVolumeSeparator(path))
      --      throw new ArgumentException(Environment.GetResourceString("Arg_PathIllegal"));
      
      return normalized
    end
    InternalGetDirectoryName = function (path)
      if path ~= nil then
        CheckInvalidPathChars(path, false)
        
        -- Expanding short paths is dangerous in this case as the results will change with the current directory.
        --
        -- Suppose you have a path called "PICTUR~1\Foo". Now suppose you have two folders on disk "C:\Mine\Pictures Of Me"
        -- and "C:\Yours\Pictures of You". If the current directory is neither you'll get back "PICTUR~1". If it is "C:\Mine"
        -- get back "Pictures Of Me". "C:\Yours" would give back "Pictures of You".
        --
        -- Because of this and as it isn't documented that short paths are expanded we will not expand short names unless
        -- we're in legacy mode.
        local normalizedPath =NormalizePath1(path, false, false)
        
        path = normalizedPath
        
        
        local root =GetRootLength(path)
        local i =#path
        if i > root then
          i = #path
          if i == root then
            return nil
          end
          while true do
            local default
            if i > root then
              i = i - 1
              if System.String.get(path, i) ~= DirectorySeparatorChar then
                default = true
              end
            end
            if not (default and System.String.get(path, i) ~= AltDirectorySeparatorChar) then
              break
            end
          end
          local dir =System.S(path):Substring(0, i)
          
          return dir
        end
      end
      return nil
    end
    GetRootLength = function (path)
      CheckInvalidPathChars(path, false)
      
      return LegacyGetRootLength(path)
    end
    LegacyGetRootLength = function (path)
      local i =0
      local length =#path
      
      if length >= 1 and (IsDirectorySeparator(System.String.get(path, 0))) then
        -- handles UNC names and directories off current drive's root.
        i = 1
        if length >= 2 and (IsDirectorySeparator(System.String.get(path, 1))) then
          i = 2
          local n =2
          while true do
            local default
            if i < length then
              local default
              if (System.String.get(path, i) ~= DirectorySeparatorChar and System.String.get(path, i) ~= AltDirectorySeparatorChar) then
                default = true
              else
                n = n - 1
                if n > 0 then
                  default = true
                end
              end
              if default then
                default = true
              end
            end
            if not default then
              break
            end
            i = i + 1
          end
        end
      elseif length >= 2 and System.String.get(path, 1) == VolumeSeparatorChar then
        -- handles A:\foo.
        i = 2
        if length >= 3 and (IsDirectorySeparator(System.String.get(path, 2))) then
          i = i + 1
        end
      end
      return i
    end
    IsDirectorySeparator = function (c)
      return (c == DirectorySeparatorChar or c == AltDirectorySeparatorChar)
    end
    GetInvalidPathChars = function ()
      return InvalidPathCharsWithAdditionalChecks:Clone()
    end
    GetInvalidFileNameChars = function ()
      return InvalidFileNameChars:Clone()
    end
    GetExtension = function (path)
      if path == nil then
        return nil
      end
      
      CheckInvalidPathChars(path, false)
      local length =#path
      do
        local i =length
        while true do
          i = i - 1
          if not (i >= 0) then
            break
          end
          local ch =System.String.get(path, i)
          if ch == 46 --[['.']] then
            if i ~= length - 1 then
              return System.S(path):Substring(i, length - i)
            else
              return ("")
            end
          end
          if ch == DirectorySeparatorChar or ch == AltDirectorySeparatorChar or ch == VolumeSeparatorChar then
            break
          end
        end
      end
      return ("")
    end
    GetFullPath = function (path)
      local fullPath =InternalGetFullPath(path)
      return fullPath
    end
    GetFileName = function (path)
      if path ~= nil then
        CheckInvalidPathChars(path, false)
        
        local length =#path
        do
          local i =length
          while true do
            i = i - 1
            if not (i >= 0) then
              break
            end
            local ch =System.String.get(path, i)
            if ch == DirectorySeparatorChar or ch == AltDirectorySeparatorChar or ch == VolumeSeparatorChar then
              return System.S(path):Substring(i + 1, length - i - 1)
            end
          end
        end
      end
      return path
    end
    GetFileNameWithoutExtension = function (path)
      path = GetFileName(path)
      if path ~= nil then
        local i
        i = System.S(path):LastIndexOf(46 --[['.']])
        if i == - 1 then
          return path
        else
          return System.S(path):Substring(0, i)
        end
      end
      return nil
    end
    GetPathRoot = function (path)
      if path == nil then
        return nil
      end
      return System.S(path):Substring(0, GetRootLength(path))
    end
    GetTempPath = function ()
      local folder =GetRandomFileName()
      return InternalGetFullPath(folder)
    end
    InternalGetFullPath = function (path)
      if path == nil then
        return nil
      end
      if IsPathRooted(path) then
        return path
      end
      
      return Combine2("/", wdmRuntime.RbxEnvironment.getAppRootName(), wdmRuntime.RbxEnvironment.AppFolderName, path)
    end
    -- <summary>
    -- 将FullPath分解为节点名称数组
    -- </summary>
    -- <param name="path"></param>
    SplitFullPath = function (path)
      if path == nil then
        System.throw(System.Exception("Path is null"))
      end
      if not IsPathRooted(path) then
        System.throw(System.Exception("Path is not rooted"))
      end
      local nodes =Linq.ToArray(System.S(path):Split(47 --[['/']], nil, 0))
      if #nodes == 0 then
        System.throw(System.Exception("Root is null"))
      end
      return nodes
    end
    GetRandomFileName = function ()
      local guid =wdmRuntime.RbxServices.getHttpService():GenerateGUID(false)
      return guid
    end
    GetTempFileName = function ()
      return InternalGetTempFileName(true)
    end
    InternalGetTempFileName = function (checkHost)
      -- String path = GetTempPath();
      --
      -- StringBuilder sb = new StringBuilder();
      --
      -- uint r = Win32Native.GetTempFileName(path, "tmp", 0, sb);
      -- if (r == 0) __Error.WinIOError();
      -- return sb.ToString();
      System.throw(System.NotImplementedException())
    end
    HasExtension = function (path)
      if path ~= nil then
        CheckInvalidPathChars(path, false)
        
        do
          local i =#path
          while true do
            i = i - 1
            if not (i >= 0) then
              break
            end
            local ch =System.String.get(path, i)
            if ch == 46 --[['.']] then
              if i ~= #path - 1 then
                return true
              else
                return false
              end
            end
            if ch == DirectorySeparatorChar or ch == AltDirectorySeparatorChar or ch == VolumeSeparatorChar then
              break
            end
          end
        end
      end
      return false
    end
    IsPathRooted = function (path)
      if path ~= nil then
        CheckInvalidPathChars(path, false)
        
        local length =#path
        if (length >= 1 and (System.String.get(path, 0) == DirectorySeparatorChar or System.String.get(path, 0) == AltDirectorySeparatorChar)) then
          return true
        end
      end
      return false
    end
    Combine = function (path1, path2)
      if path1 == nil or path2 == nil then
        System.throw(System.ArgumentNullException((path1 == nil) and "path1" or "path2"))
      end
      CheckInvalidPathChars(path1, false)
      CheckInvalidPathChars(path2, false)
      
      return CombineNoChecks(path1, path2)
    end
    Combine1 = function (path1, path2, path3)
      if path1 == nil or path2 == nil or path3 == nil then
        System.throw(System.ArgumentNullException((path1 == nil) and "path1" or ((path2 == nil) and "path2" or "path3")))
      end
      CheckInvalidPathChars(path1, false)
      CheckInvalidPathChars(path2, false)
      CheckInvalidPathChars(path3, false)
      return CombineNoChecks(CombineNoChecks(path1, path2), path3)
    end
    Combine2 = function (path1, path2, path3, path4)
      if path1 == nil or path2 == nil or path3 == nil or path4 == nil then
        System.throw(System.ArgumentNullException((path1 == nil) and "path1" or ((path2 == nil) and "path2" or ((path3 == nil) and "path3" or "path4"))))
      end
      CheckInvalidPathChars(path1, false)
      CheckInvalidPathChars(path2, false)
      CheckInvalidPathChars(path3, false)
      CheckInvalidPathChars(path4, false)
      
      return CombineNoChecks(CombineNoChecks(CombineNoChecks(path1, path2), path3), path4)
    end
    Combine3 = function (...)
      local var= {...} 
      local paths=System.arrayFromTable(var,System.Object)
      if paths == nil then
        System.throw(System.ArgumentNullException("paths"))
      end
      
      local finalSize =0
      local firstComponent =0
      
      -- We have two passes, the first calcuates how large a buffer to allocate and does some precondition
      -- checks on the paths passed in.  The second actually does the combination.
      
      for i = 0, #paths - 1 do
        local continue
        repeat
          if paths:get(i) == nil then
            System.throw(System.ArgumentNullException("paths"))
          end
          
          if #paths:get(i) == 0 then
            continue = true
            break
          end
          
          CheckInvalidPathChars(paths:get(i), false)
          
          if IsPathRooted(paths:get(i)) then
            firstComponent = i
            finalSize = #paths:get(i)
          else
            finalSize = finalSize + #paths:get(i)
          end
          
          local ch =System.String.get(paths:get(i), #paths:get(i) - 1)
          if ch ~= DirectorySeparatorChar and ch ~= AltDirectorySeparatorChar and ch ~= VolumeSeparatorChar then
            finalSize = finalSize + 1
          end
          continue = true
        until 1
        if not continue then
          break
        end
      end
      
      local finalPath =System.StringBuilder()
      
      for i = firstComponent, #paths - 1 do
        local continue
        repeat
          if #paths:get(i) == 0 then
            continue = true
            break
          end
          
          if finalPath:getLength() == 0 then
            finalPath:Append(paths:get(i))
          else
            local ch =finalPath:get(finalPath:getLength() - 1)
            if ch ~= DirectorySeparatorChar and ch ~= AltDirectorySeparatorChar and ch ~= VolumeSeparatorChar then
              finalPath:AppendChar(DirectorySeparatorChar)
            end
            
            finalPath:Append(paths:get(i))
          end
          continue = true
        until 1
        if not continue then
          break
        end
      end
      
      return finalPath:ToString()
    end
    CombineNoChecks = function (path1, path2)
      if #path2 == 0 then
        return path1
      end
      
      if #path1 == 0 then
        return path2
      end
      
      if IsPathRooted(path2) then
        return path2
      end
      
      local ch =System.String.get(path1, #path1 - 1)
      if ch ~= DirectorySeparatorChar and ch ~= AltDirectorySeparatorChar and ch ~= VolumeSeparatorChar then
        return System.toString(System.toString(path1) .. System.toString("/" --[[Path.DirectorySeparatorCharAsString]])) .. System.toString(path2)
      end
      return System.toString(path1) .. System.toString(path2)
    end
    ToBase32StringSuitableForDirName = function (buff)
      -- This routine is optimised to be used with buffs of length 20
      
      local sb =System.StringBuilder()
      local b0, b1, b2, b3, b4
      local l, i
      
      l = #buff
      i = 0
      
      -- Create l chars using the last 5 bits of each byte.  
      -- Consume 3 MSB bits 5 bytes at a time.
      
      repeat
        local default =i
        i = default + 1
        b0 = (i < l) and buff:get(default) or 0 --[[(byte)0]]
        local extern =i
        i = extern + 1
        b1 = (i < l) and buff:get(extern) or 0 --[[(byte)0]]
        local ref =i
        i = ref + 1
        b2 = (i < l) and buff:get(ref) or 0 --[[(byte)0]]
        local out =i
        i = out + 1
        b3 = (i < l) and buff:get(out) or 0 --[[(byte)0]]
        local try =i
        i = try + 1
        b4 = (i < l) and buff:get(try) or 0 --[[(byte)0]]
        
        -- Consume the 5 Least significant bits of each byte
        sb:AppendChar(s_Base32Char:get(System.band(b0, 0x1F)))
        sb:AppendChar(s_Base32Char:get(System.band(b1, 0x1F)))
        sb:AppendChar(s_Base32Char:get(System.band(b2, 0x1F)))
        sb:AppendChar(s_Base32Char:get(System.band(b3, 0x1F)))
        sb:AppendChar(s_Base32Char:get(System.band(b4, 0x1F)))
        
        -- Consume 3 MSB of b0, b1, MSB bits 6, 7 of b3, b4
        sb:AppendChar(s_Base32Char:get((System.bor((System.sr((System.band(b0, 0xE0)), 5)), (System.sr((System.band(b3, 0x60)), 2))))))
        
        sb:AppendChar(s_Base32Char:get((System.bor((System.sr((System.band(b1, 0xE0)), 5)), (System.sr((System.band(b4, 0x60)), 2))))))
        
        -- Consume 3 MSB bits of b2, 1 MSB bit of b3, b4
        
        b2 = System.sr(b2, 5)
        
        
        if (System.band(b3, 0x80)) ~= 0 then
          b2 = System.bor(b2, 0x08)
        end
        if (System.band(b4, 0x80)) ~= 0 then
          b2 = System.bor(b2, 0x10)
        end
        
        sb:AppendChar(s_Base32Char:get(b2))
      until not (i < l)
      
      return sb:ToString()
    end
    CheckSearchPattern = function (searchPattern)
      local index
      while true do
        index = System.S(searchPattern):IndexOf("..", System.StringComparison.Ordinal)
        if not (index ~= - 1) then
          break
        end
        if index + 2 == #searchPattern then
          System.throw(System.ArgumentException("Arg_InvalidSearchPattern"))
        end
        
        if (System.String.get(searchPattern, index + 2) == DirectorySeparatorChar) or (System.String.get(searchPattern, index + 2) == AltDirectorySeparatorChar) then
          System.throw(System.ArgumentException("Arg_InvalidSearchPattern"))
        end
        
        searchPattern = System.S(searchPattern):Substring(index + 2)
      end
    end
    CheckInvalidPathChars = function (path, checkAdditional)
      if path == nil then
        System.throw(System.ArgumentNullException("path"))
      end
      
      if SystemIO.PathInternal.HasIllegalCharacters(path, checkAdditional) then
        System.throw(System.ArgumentException("Argument_InvalidPathChars"))
      end
    end
    InternalCombine = function (path1, path2)
      if path1 == nil or path2 == nil then
        System.throw(System.ArgumentNullException((path1 == nil) and "path1" or "path2"))
      end
      CheckInvalidPathChars(path1, false)
      CheckInvalidPathChars(path2, false)
      
      if #path2 == 0 then
        System.throw(System.ArgumentException(System.toString("Argument_PathEmpty:") .. System.toString(path2)))
      end
      if IsPathRooted(path2) then
        System.throw(System.ArgumentException("Arg_Path2IsRooted:path2" --[["Arg_Path2IsRooted:"+"path2"]]))
      end
      local i =#path1
      if i == 0 then
        return path2
      end
      local ch =System.String.get(path1, i - 1)
      if ch ~= DirectorySeparatorChar and ch ~= AltDirectorySeparatorChar and ch ~= VolumeSeparatorChar then
        return System.toString(System.toString(path1) .. System.toString("/" --[[Path.DirectorySeparatorCharAsString]])) .. System.toString(path2)
      end
      return System.toString(path1) .. System.toString(path2)
    end
    return {
      DirectorySeparatorChar = DirectorySeparatorChar,
      AltDirectorySeparatorChar = AltDirectorySeparatorChar,
      VolumeSeparatorChar = VolumeSeparatorChar,
      PathSeparator = PathSeparator,
      ChangeExtension = ChangeExtension,
      GetDirectoryName = GetDirectoryName,
      NormalizePath = NormalizePath,
      NormalizePath1 = NormalizePath1,
      NormalizePath2 = NormalizePath2,
      NormalizePath3 = NormalizePath3,
      InternalGetDirectoryName = InternalGetDirectoryName,
      GetRootLength = GetRootLength,
      IsDirectorySeparator = IsDirectorySeparator,
      GetInvalidPathChars = GetInvalidPathChars,
      GetInvalidFileNameChars = GetInvalidFileNameChars,
      GetExtension = GetExtension,
      GetFullPath = GetFullPath,
      GetFileName = GetFileName,
      GetFileNameWithoutExtension = GetFileNameWithoutExtension,
      GetPathRoot = GetPathRoot,
      GetTempPath = GetTempPath,
      InternalGetFullPath = InternalGetFullPath,
      SplitFullPath = SplitFullPath,
      GetRandomFileName = GetRandomFileName,
      GetTempFileName = GetTempFileName,
      HasExtension = HasExtension,
      IsPathRooted = IsPathRooted,
      Combine = Combine,
      Combine1 = Combine1,
      Combine2 = Combine2,
      Combine3 = Combine3,
      CombineNoChecks = CombineNoChecks,
      ToBase32StringSuitableForDirName = ToBase32StringSuitableForDirName,
      CheckSearchPattern = CheckSearchPattern,
      CheckInvalidPathChars = CheckInvalidPathChars,
      InternalCombine = InternalCombine,
      static = static,
      __metadata__ = function (out)
        return {
          fields = {
            { "AltDirectorySeparatorChar", 0xE, System.Char },
            { "DirectorySeparatorChar", 0xE, System.Char },
            { "InvalidFileNameChars", 0x9, System.Array(System.Char) },
            { "InvalidPathCharsWithAdditionalChecks", 0x9, System.Array(System.Char) },
            { "PathSeparator", 0xE, System.Char },
            { "VolumeSeparatorChar", 0xE, System.Char },
            { "s_Base32Char", 0x9, System.Array(System.Char) }
          },
          methods = {
            { "ChangeExtension", 0x28E, ChangeExtension, System.String, System.String, System.String },
            { "CheckInvalidPathChars", 0x20C, CheckInvalidPathChars, System.String, System.Boolean },
            { "CheckSearchPattern", 0x10C, CheckSearchPattern, System.String },
            { "Combine", 0x18E, Combine3, System.Array(System.String), System.String },
            { "Combine", 0x48E, Combine2, System.String, System.String, System.String, System.String, System.String },
            { "Combine", 0x38E, Combine1, System.String, System.String, System.String, System.String },
            { "Combine", 0x28E, Combine, System.String, System.String, System.String },
            { "CombineNoChecks", 0x28C, CombineNoChecks, System.String, System.String, System.String },
            { "GetDirectoryName", 0x18E, GetDirectoryName, System.String, System.String },
            { "GetExtension", 0x18E, GetExtension, System.String, System.String },
            { "GetFileName", 0x18E, GetFileName, System.String, System.String },
            { "GetFileNameWithoutExtension", 0x18E, GetFileNameWithoutExtension, System.String, System.String },
            { "GetFullPath", 0x18E, GetFullPath, System.String, System.String },
            { "GetInvalidFileNameChars", 0x8E, GetInvalidFileNameChars, System.Array(System.Char) },
            { "GetInvalidPathChars", 0x8E, GetInvalidPathChars, System.Array(System.Char) },
            { "GetPathRoot", 0x18E, GetPathRoot, System.String, System.String },
            { "GetRandomFileName", 0x8E, GetRandomFileName, System.String },
            { "GetRootLength", 0x18C, GetRootLength, System.String, System.Int32 },
            { "GetTempFileName", 0x8E, GetTempFileName, System.String },
            { "GetTempPath", 0x8E, GetTempPath, System.String },
            { "HasExtension", 0x18E, HasExtension, System.String, System.Boolean },
            { "InternalCombine", 0x28C, InternalCombine, System.String, System.String, System.String },
            { "InternalGetDirectoryName", 0x18C, InternalGetDirectoryName, System.String, System.String },
            { "InternalGetFullPath", 0x18C, InternalGetFullPath, System.String, System.String },
            { "InternalGetTempFileName", 0x189, InternalGetTempFileName, System.Boolean, System.String },
            { "IsDirectorySeparator", 0x18C, IsDirectorySeparator, System.Char, System.Boolean },
            { "IsPathRooted", 0x18E, IsPathRooted, System.String, System.Boolean },
            { "LegacyGetRootLength", 0x189, LegacyGetRootLength, System.String, System.Int32 },
            { "NewNormalizePathLimitedChecks", 0x389, NewNormalizePathLimitedChecks, System.String, System.Int32, System.Boolean, System.String },
            { "NormalizePath", 0x48C, NormalizePath3, System.String, System.Boolean, System.Int32, System.Boolean, System.String },
            { "NormalizePath", 0x38C, NormalizePath2, System.String, System.Boolean, System.Int32, System.String },
            { "NormalizePath", 0x38C, NormalizePath1, System.String, System.Boolean, System.Boolean, System.String },
            { "NormalizePath", 0x28C, NormalizePath, System.String, System.Boolean, System.String },
            { "SplitFullPath", 0x18C, SplitFullPath, System.String, System.Array(System.String) },
            { "ToBase32StringSuitableForDirName", 0x18C, ToBase32StringSuitableForDirName, System.Array(System.Byte), System.String }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}