-- Generated by CSharp.lua Compiler
local System =_G.System
local Roblox =_G.Roblox
System.namespace("System", function (namespace)
  namespace.class("InstanceOperator", function (namespace)
    local CreateDirectoryFromRoot
    -- <summary>
    -- 使用目标路径创建一个folder
    -- 如果某个路径节点不存在,将同样创建为folder
    -- 如果最终子节点已存在,则忽略并返回该节点
    -- </summary>
    -- <param name="names">
    -- 路径节点名称
    -- </param>
    -- <returns></returns>
    CreateDirectoryFromRoot = function (...)
      local var= {...} 
      local names=System.arrayFromTable(var,System.Object)
      local child =game
      for _, name in System.each(names) do
        local sub =child:FindFirstChild(name, false)
        if sub == nil then
          sub = Instance.new("Folder")
          sub.Parent = child
          sub.Name = name
        end
        child = sub
      end
      return child
    end
    return {
      CreateDirectoryFromRoot = CreateDirectoryFromRoot,
      __metadata__ = function (out)
        return {
          methods = {
            { "CreateDirectoryFromRoot", 0x18E, CreateDirectoryFromRoot, System.Array(System.String), out.Roblox.Instance }
          },
          class = { 0x4 }
        }
      end
    }
  end)
end)
return {}