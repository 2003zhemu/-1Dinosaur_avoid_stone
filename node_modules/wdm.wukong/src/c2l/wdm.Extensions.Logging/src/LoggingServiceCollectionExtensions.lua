-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmDependencyInjection
local wdmExtensions
local wdmExtensionsLogging
local wdmToolkitDiagnostics
local __importGlobal =function (out)
  wdmDependencyInjection = out.wdm.Extensions.DependencyInjection
  wdmExtensions = out.wdm.Extensions.DependencyInjection.Extensions
  wdmExtensionsLogging = out.wdm.Extensions.Logging
  wdmToolkitDiagnostics = out.wdm.Toolkit.Diagnostics
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.DependencyInjection", function (namespace)
  -- <summary>
  -- Extension methods for setting up logging services in an <see cref="IServiceCollection" />.
  -- </summary>
  namespace.class("LoggingServiceCollectionExtensions", function (namespace)
    local AddLogging, AddLogging1
    -- <summary>
    -- Adds logging services to the specified <see cref="IServiceCollection" />.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection" /> to add services to.</param>
    -- <returns>The <see cref="IServiceCollection"/> so that additional calls can be chained.</returns>
    AddLogging = function (services)
      return AddLogging1(services, function (_)
      end)
    end
    -- <summary>
    -- Adds logging services to the specified <see cref="IServiceCollection" />.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection" /> to add services to.</param>
    -- <param name="configure">The <see cref="ILoggingBuilder"/> configuration delegate.</param>
    -- <returns>The <see cref="IServiceCollection"/> so that additional calls can be chained.</returns>
    AddLogging1 = function (services, configure)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      
      --services.AddOptions();
      
      wdmExtensions.ServiceCollectionDescriptorExtensions.TryAdd(services, wdmDependencyInjection.ServiceDescriptor.Singleton(wdmExtensionsLogging.ILoggerFactory, wdmExtensionsLogging.LoggerFactory))
      wdmExtensions.ServiceCollectionDescriptorExtensions.TryAdd(services, wdmDependencyInjection.ServiceDescriptor.Singleton1(System.typeof(wdmExtensionsLogging.ILogger_1), System.typeof(wdmExtensionsLogging.Logger_1)))
      
      -- services.TryAddEnumerable(ServiceDescriptor.Singleton<IConfigureOptions<LoggerFilterOptions>>(
      --     new DefaultLoggerLevelConfigureOptions(LogLevel.Information)));
      
      configure(wdmExtensionsLogging.LoggingBuilder(services))
      return services
    end
    return {
      AddLogging = AddLogging,
      AddLogging1 = AddLogging1,
      __metadata__ = function (out)
        return {
          methods = {
            { "AddLogging", 0x18E, AddLogging, out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "AddLogging", 0x28E, AddLogging1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Delegate(out.wdm.Extensions.Logging.ILoggingBuilder, System.Void), out.wdm.Extensions.DependencyInjection.IServiceCollection }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}