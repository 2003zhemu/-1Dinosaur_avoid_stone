-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local DictStringObject =System.Dictionary(System.String, System.Object)
local wdmConfiguration
local wdmToolkitDiagnostics
local ListIConfigurationSource
local ListIConfigurationProvider
local __importGlobal =function (out)
  wdmConfiguration = out.wdm.Extensions.Configuration
  wdmToolkitDiagnostics = out.wdm.Toolkit.Diagnostics
  ListIConfigurationSource = System.List(_G.wdm.Extensions.Configuration.IConfigurationSource)
  ListIConfigurationProvider = System.List(_G.wdm.Extensions.Configuration.IConfigurationProvider)
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Configuration", function (namespace)
  -- <summary>
  -- Used to build key/value based configuration settings for use in an application.
  -- </summary>
  namespace.class("ConfigurationBuilder", function (namespace)
    local getSources, getProperties, Add, Build, __ctor__
    __ctor__ = function (this)
      this.Sources = ListIConfigurationSource()
      this.Properties = DictStringObject()
    end
    getSources = System.property("Sources", true)
    getProperties = System.property("Properties", true)
    -- <summary>
    -- Adds a new configuration source.
    -- </summary>
    -- <param name="source">The configuration source to add.</param>
    -- <returns>The same <see cref="IConfigurationBuilder"/>.</returns>
    Add = function (this, source)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(source)
      
      this.Sources:Add(source)
      return this
    end
    -- <summary>
    -- Builds an <see cref="IConfiguration"/> with keys and values from the set of providers registered in
    -- <see cref="Sources"/>.
    -- </summary>
    -- <returns>An <see cref="IConfigurationRoot"/> with keys and values from the registered providers.</returns>
    Build = function (this)
      local providers =ListIConfigurationProvider()
      for _, source in System.each(this.Sources) do
        local provider =source:Build(this)
        providers:Add(provider)
      end
      return wdmConfiguration.ConfigurationRoot(providers)
    end
    return {
      base = function (out)
        return {
          out.wdm.Extensions.Configuration.IConfigurationBuilder
        }
      end,
      getSources = getSources,
      getProperties = getProperties,
      Add = Add,
      Build = Build,
      __ctor__ = __ctor__,
      __metadata__ = function (out)
        return {
          properties = {
            { "Properties", 0x206, System.IDictionary_2(System.String, System.Object), getProperties },
            { "Sources", 0x206, System.IList_1(out.wdm.Extensions.Configuration.IConfigurationSource), getSources }
          },
          methods = {
            { "Add", 0x186, Add, out.wdm.Extensions.Configuration.IConfigurationSource, out.wdm.Extensions.Configuration.IConfigurationBuilder },
            { "Build", 0x86, Build, out.wdm.Extensions.Configuration.IConfigurationRoot }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}