-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmConfiguration
local wdmToolkitDiagnostics
local __importGlobal =function (out)
  wdmConfiguration = out.wdm.Extensions.Configuration
  wdmToolkitDiagnostics = out.wdm.Toolkit.Diagnostics
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Configuration", function (namespace)
  -- <summary>
  -- Extension methods for adding <see cref="IConfiguration"/> to an <see cref="IConfigurationBuilder"/>.
  -- </summary>
  namespace.class("ChainedBuilderExtensions", function (namespace)
    local AddConfiguration, AddConfiguration1
    -- <summary>
    -- Adds an existing configuration to <paramref name="configurationBuilder"/>.
    -- </summary>
    -- <param name="configurationBuilder">The <see cref="IConfigurationBuilder"/> to add to.</param>
    -- <param name="config">The <see cref="IConfiguration"/> to add.</param>
    -- <returns>The <see cref="IConfigurationBuilder"/>.</returns>
    AddConfiguration = function (configurationBuilder, config)
      return AddConfiguration1(configurationBuilder, config, false)
    end
    -- <summary>
    -- Adds an existing configuration to <paramref name="configurationBuilder"/>.
    -- </summary>
    -- <param name="configurationBuilder">The <see cref="IConfigurationBuilder"/> to add to.</param>
    -- <param name="config">The <see cref="IConfiguration"/> to add.</param>
    -- <param name="shouldDisposeConfiguration">Whether the configuration should get disposed when the configuration provider is disposed.</param>
    -- <returns>The <see cref="IConfigurationBuilder"/>.</returns>
    AddConfiguration1 = function (configurationBuilder, config, shouldDisposeConfiguration)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(configurationBuilder)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(config)
      
      local default =wdmConfiguration.ChainedConfigurationSource()
      default.Configuration = config
      default.ShouldDisposeConfiguration = shouldDisposeConfiguration
      configurationBuilder:Add(default)
      return configurationBuilder
    end
    return {
      AddConfiguration = AddConfiguration,
      AddConfiguration1 = AddConfiguration1,
      __metadata__ = function (out)
        return {
          methods = {
            { "AddConfiguration", 0x28E, AddConfiguration, out.wdm.Extensions.Configuration.IConfigurationBuilder, out.wdm.Extensions.Configuration.IConfiguration, out.wdm.Extensions.Configuration.IConfigurationBuilder },
            { "AddConfiguration", 0x38E, AddConfiguration1, out.wdm.Extensions.Configuration.IConfigurationBuilder, out.wdm.Extensions.Configuration.IConfiguration, System.Boolean, out.wdm.Extensions.Configuration.IConfigurationBuilder }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}