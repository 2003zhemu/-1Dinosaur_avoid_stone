--[=[
    @class IHasPlugin
    插件载体, 可以容纳多个插件
]=]
local IHasPlugin = {}

local IPlugin = require(script.Parent.IPlugin)

--- 获取当前格子中的元素
function IHasPlugin:getPlugins(): { IPlugin.Type }? end

export type Type = typeof(IHasPlugin)

return IHasPlugin
