-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local KeyValuePairStringString =System.KeyValuePair(System.String, System.String)
local ArrayKeyValuePairStringString =System.Array(KeyValuePairStringString)
local wdmConfiguration
local wdmDependencyInjection
local wdmExtensionsHosting
local wdmToolkitDiagnostics
local __importGlobal =function (out)
  wdmConfiguration = out.wdm.Extensions.Configuration
  wdmDependencyInjection = out.wdm.Extensions.DependencyInjection
  wdmExtensionsHosting = out.wdm.Extensions.Hosting
  wdmToolkitDiagnostics = out.wdm.Toolkit.Diagnostics
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Hosting", function (namespace)
  -- <summary>
  -- Provides extension methods for the <see cref="IHostBuilder"/> from the hosting package.
  -- </summary>
  namespace.class("HostingHostBuilderExtensions", function (namespace)
    local UseEnvironment, UseContentRoot, UseDefaultServiceProvider, UseDefaultServiceProvider1, ConfigureLogging, ConfigureLogging1, ConfigureAppConfiguration, ConfigureServices, 
    ConfigureContainer, ConfigureDefaults, ApplyDefaultHostConfiguration, ApplyDefaultAppConfiguration, AddDefaultServices, CreateDefaultServiceProviderOptions
    -- <summary>
    -- Specify the environment to be used by the host.
    -- To avoid the environment being overwritten by a default
    -- value, ensure this is called after defaults are configured.
    -- </summary>
    -- <param name="hostBuilder">The <see cref="IHostBuilder"/> to configure.</param>
    -- <param name="environment">The environment to host the application in.</param>
    -- <returns>The <see cref="IHostBuilder"/>.</returns>
    UseEnvironment = function (hostBuilder, environment)
      return hostBuilder:ConfigureHostConfiguration(function (configBuilder)
        wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(environment)
        
        wdmConfiguration.MemoryConfigurationBuilderExtensions.AddInMemoryCollection1(configBuilder, ArrayKeyValuePairStringString { KeyValuePairStringString(wdmExtensionsHosting.HostDefaults.EnvironmentKey, environment) })
      end)
    end
    -- <summary>
    -- Specify the content root directory to be used by the host. To avoid the content root directory being
    -- overwritten by a default value, ensure this is called after defaults are configured.
    -- </summary>
    -- <param name="hostBuilder">The <see cref="IHostBuilder"/> to configure.</param>
    -- <param name="contentRoot">Path to root directory of the application.</param>
    -- <returns>The <see cref="IHostBuilder"/>.</returns>
    UseContentRoot = function (hostBuilder, contentRoot)
      return hostBuilder:ConfigureHostConfiguration(function (configBuilder)
        wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(contentRoot)
        
        wdmConfiguration.MemoryConfigurationBuilderExtensions.AddInMemoryCollection1(configBuilder, ArrayKeyValuePairStringString { KeyValuePairStringString(wdmExtensionsHosting.HostDefaults.ContentRootKey, contentRoot) })
      end)
    end
    -- <summary>
    -- Specify the <see cref="IServiceProvider"/> to be the default one.
    -- </summary>
    -- <param name="hostBuilder">The <see cref="IHostBuilder"/> to configure.</param>
    -- <param name="configure">The delegate that configures the <see cref="IServiceProvider"/>.</param>
    -- <returns>The <see cref="IHostBuilder"/>.</returns>
    UseDefaultServiceProvider = function (hostBuilder, configure)
      return UseDefaultServiceProvider1(hostBuilder, function (context, options)
        configure(options)
      end)
    end
    -- <summary>
    -- Specify the <see cref="IServiceProvider"/> to be the default one.
    -- </summary>
    -- <param name="hostBuilder">The <see cref="IHostBuilder"/> to configure.</param>
    -- <param name="configure">The delegate that configures the <see cref="IServiceProvider"/>.</param>
    -- <returns>The <see cref="IHostBuilder"/>.</returns>
    UseDefaultServiceProvider1 = function (hostBuilder, configure)
      return hostBuilder:UseServiceProviderFactory1(function (context)
        local options =wdmDependencyInjection.ServiceProviderOptions()
        configure(context, options)
        return System.new(wdmDependencyInjection.DefaultServiceProviderFactory, 2, options)
      end, wdmDependencyInjection.IServiceCollection)
    end
    -- <summary>
    -- Adds a delegate for configuring the provided <see cref="ILoggingBuilder"/>. This may be called multiple times.
    -- </summary>
    -- <param name="hostBuilder">The <see cref="IHostBuilder" /> to configure.</param>
    -- <param name="configureLogging">The delegate that configures the <see cref="ILoggingBuilder"/>.</param>
    -- <returns>The same instance of the <see cref="IHostBuilder"/> for chaining.</returns>
    ConfigureLogging = function (hostBuilder, configureLogging)
      return hostBuilder:ConfigureServices(function (context, collection)
        wdmDependencyInjection.LoggingServiceCollectionExtensions.AddLogging1(collection, function (builder)
          configureLogging(context, builder)
        end)
      end)
    end
    -- <summary>
    -- Adds a delegate for configuring the provided <see cref="ILoggingBuilder"/>. This may be called multiple times.
    -- </summary>
    -- <param name="hostBuilder">The <see cref="IHostBuilder" /> to configure.</param>
    -- <param name="configureLogging">The delegate that configures the <see cref="ILoggingBuilder"/>.</param>
    -- <returns>The same instance of the <see cref="IHostBuilder"/> for chaining.</returns>
    ConfigureLogging1 = function (hostBuilder, configureLogging)
      return hostBuilder:ConfigureServices(function (context, collection)
        wdmDependencyInjection.LoggingServiceCollectionExtensions.AddLogging1(collection, function (builder)
          configureLogging(builder)
        end)
      end)
    end
    -- <summary>
    -- Sets up the configuration for the remainder of the build process and application. This can be called multiple times and
    -- the results will be additive. The results will be available at <see cref="HostBuilderContext.Configuration"/> for
    -- subsequent operations, as well as in <see cref="IHost.Services"/>.
    -- </summary>
    -- <param name="hostBuilder">The <see cref="IHostBuilder" /> to configure.</param>
    -- <param name="configureDelegate">The delegate for configuring the <see cref="IConfigurationBuilder"/> that will be used
    -- to construct the <see cref="IConfiguration"/> for the host.</param>
    -- <returns>The same instance of the <see cref="IHostBuilder"/> for chaining.</returns>
    ConfigureAppConfiguration = function (hostBuilder, configureDelegate)
      return hostBuilder:ConfigureAppConfiguration(function (context, builder)
        configureDelegate(builder)
      end)
    end
    -- <summary>
    -- Adds services to the container. This can be called multiple times and the results will be additive.
    -- </summary>
    -- <param name="hostBuilder">The <see cref="IHostBuilder" /> to configure.</param>
    -- <param name="configureDelegate">The delegate for configuring the <see cref="IServiceCollection"/>.</param>
    -- <returns>The same instance of the <see cref="IHostBuilder"/> for chaining.</returns>
    ConfigureServices = function (hostBuilder, configureDelegate)
      return hostBuilder:ConfigureServices(function (context, collection)
        configureDelegate(collection)
      end)
    end
    -- <summary>
    -- Enables configuring the instantiated dependency container. This can be called multiple times and
    -- the results will be additive.
    -- </summary>
    -- <typeparam name="TContainerBuilder">The type of builder.</typeparam>
    -- <param name="hostBuilder">The <see cref="IHostBuilder" /> to configure.</param>
    -- <param name="configureDelegate">The delegate for configuring the <typeparamref name="TContainerBuilder"/>.</param>
    -- <returns>The same instance of the <see cref="IHostBuilder"/> for chaining.</returns>
    ConfigureContainer = function (hostBuilder, configureDelegate, TContainerBuilder)
      return hostBuilder:ConfigureContainer(function (context, builder)
        configureDelegate(builder, TContainerBuilder)
      end, TContainerBuilder)
    end
    -- <summary>
    -- Configures an existing <see cref="IHostBuilder"/> instance with pre-configured defaults. This will overwrite
    -- previously configured values and is intended to be called before additional configuration calls.
    -- </summary>
    -- <remarks>
    -- The following defaults are applied to the <see cref="IHostBuilder"/>:
    -- * set the <see cref="IHostEnvironment.ContentRootPath"/> to the result of <see cref="Directory.GetCurrentDirectory()"/>
    -- * load host <see cref="IConfiguration"/> from "DOTNET_" prefixed environment variables
    -- * load host <see cref="IConfiguration"/> from supplied command line args
    -- * load app <see cref="IConfiguration"/> from 'appsettings.json' and 'appsettings.[<see cref="IHostEnvironment.EnvironmentName"/>].json'
    -- * load app <see cref="IConfiguration"/> from User Secrets when <see cref="IHostEnvironment.EnvironmentName"/> is 'Development' using the entry assembly
    -- * load app <see cref="IConfiguration"/> from environment variables
    -- * load app <see cref="IConfiguration"/> from supplied command line args
    -- * configure the <see cref="ILoggerFactory"/> to log to the console, debug, and event source output
    -- * enables scope validation on the dependency injection container when <see cref="IHostEnvironment.EnvironmentName"/> is 'Development'
    -- </remarks>
    -- <param name="builder">The existing builder to configure.</param>
    -- <param name="args">The command line args.</param>
    -- <returns>The same instance of the <see cref="IHostBuilder"/> for chaining.</returns>
    ConfigureDefaults = function (builder, args)
      return builder:ConfigureHostConfiguration(function (config)
        ApplyDefaultHostConfiguration(config, args)
      end):ConfigureAppConfiguration(function (hostingContext, config)
        ApplyDefaultAppConfiguration(hostingContext, config, args)
      end):ConfigureServices(AddDefaultServices):UseServiceProviderFactory1(function (context)
        return System.new(wdmDependencyInjection.DefaultServiceProviderFactory, 2, CreateDefaultServiceProviderOptions(context))
      end, wdmDependencyInjection.IServiceCollection)
    end
    ApplyDefaultHostConfiguration = function (hostConfigBuilder, args)
    end
    ApplyDefaultAppConfiguration = function (hostingContext, appConfigBuilder, args)
    end
    AddDefaultServices = function (hostingContext, services)
      -- services.AddLogging(logging =>
      -- {
      --     logging.AddFilter<EventLogLoggerProvider>(level => level >= LogLevel.Warning);
      --
      --     logging.AddConfiguration(hostingContext.Configuration.GetSection("Logging"));
      --
      --     logging.AddDebug();
      --     logging.AddEventSourceLogger();
      --     logging.AddEventLog();
      --     logging.Configure(options =>
      --     {
      --         options.ActivityTrackingOptions =
      --             ActivityTrackingOptions.SpanId |
      --             ActivityTrackingOptions.TraceId |
      --             ActivityTrackingOptions.ParentId;
      --     });
      -- });
    end
    CreateDefaultServiceProviderOptions = function (context)
      local isDevelopment =wdmExtensionsHosting.HostEnvironmentEnvExtensions.IsDevelopment(context.HostingEnvironment)
      local default =wdmDependencyInjection.ServiceProviderOptions()
      default.ValidateScopes = isDevelopment
      default.ValidateOnBuild = isDevelopment
      return default
    end
    return {
      UseEnvironment = UseEnvironment,
      UseContentRoot = UseContentRoot,
      UseDefaultServiceProvider = UseDefaultServiceProvider,
      UseDefaultServiceProvider1 = UseDefaultServiceProvider1,
      ConfigureLogging = ConfigureLogging,
      ConfigureLogging1 = ConfigureLogging1,
      ConfigureAppConfiguration = ConfigureAppConfiguration,
      ConfigureServices = ConfigureServices,
      ConfigureContainer = ConfigureContainer,
      ConfigureDefaults = ConfigureDefaults,
      ApplyDefaultAppConfiguration = ApplyDefaultAppConfiguration,
      AddDefaultServices = AddDefaultServices,
      CreateDefaultServiceProviderOptions = CreateDefaultServiceProviderOptions,
      __metadata__ = function (out)
        return {
          methods = {
            { "AddDefaultServices", 0x20C, AddDefaultServices, out.wdm.Extensions.Hosting.HostBuilderContext, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "ApplyDefaultAppConfiguration", 0x30C, ApplyDefaultAppConfiguration, out.wdm.Extensions.Hosting.HostBuilderContext, out.wdm.Extensions.Configuration.IConfigurationBuilder, System.Array(System.String) },
            { "ApplyDefaultHostConfiguration", 0x209, ApplyDefaultHostConfiguration, out.wdm.Extensions.Configuration.IConfigurationBuilder, System.Array(System.String) },
            { "ConfigureAppConfiguration", 0x28E, ConfigureAppConfiguration, out.wdm.Extensions.Hosting.IHostBuilder, System.Delegate(out.wdm.Extensions.Configuration.IConfigurationBuilder, System.Void), out.wdm.Extensions.Hosting.IHostBuilder },
            { "ConfigureContainer", 0x1028E, ConfigureContainer, function (TContainerBuilder) return out.wdm.Extensions.Hosting.IHostBuilder, System.Delegate(TContainerBuilder, System.Void), out.wdm.Extensions.Hosting.IHostBuilder end },
            { "ConfigureDefaults", 0x28E, ConfigureDefaults, out.wdm.Extensions.Hosting.IHostBuilder, System.Array(System.String), out.wdm.Extensions.Hosting.IHostBuilder },
            { "ConfigureLogging", 0x28E, ConfigureLogging, out.wdm.Extensions.Hosting.IHostBuilder, System.Delegate(out.wdm.Extensions.Hosting.HostBuilderContext, out.wdm.Extensions.Logging.ILoggingBuilder, System.Void), out.wdm.Extensions.Hosting.IHostBuilder },
            { "ConfigureLogging", 0x28E, ConfigureLogging1, out.wdm.Extensions.Hosting.IHostBuilder, System.Delegate(out.wdm.Extensions.Logging.ILoggingBuilder, System.Void), out.wdm.Extensions.Hosting.IHostBuilder },
            { "ConfigureServices", 0x28E, ConfigureServices, out.wdm.Extensions.Hosting.IHostBuilder, System.Delegate(out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Void), out.wdm.Extensions.Hosting.IHostBuilder },
            { "CreateDefaultServiceProviderOptions", 0x18C, CreateDefaultServiceProviderOptions, out.wdm.Extensions.Hosting.HostBuilderContext, out.wdm.Extensions.DependencyInjection.ServiceProviderOptions },
            { "UseContentRoot", 0x28E, UseContentRoot, out.wdm.Extensions.Hosting.IHostBuilder, System.String, out.wdm.Extensions.Hosting.IHostBuilder },
            { "UseDefaultServiceProvider", 0x28E, UseDefaultServiceProvider, out.wdm.Extensions.Hosting.IHostBuilder, System.Delegate(out.wdm.Extensions.DependencyInjection.ServiceProviderOptions, System.Void), out.wdm.Extensions.Hosting.IHostBuilder },
            { "UseDefaultServiceProvider", 0x28E, UseDefaultServiceProvider1, out.wdm.Extensions.Hosting.IHostBuilder, System.Delegate(out.wdm.Extensions.Hosting.HostBuilderContext, out.wdm.Extensions.DependencyInjection.ServiceProviderOptions, System.Void), out.wdm.Extensions.Hosting.IHostBuilder },
            { "UseEnvironment", 0x28E, UseEnvironment, out.wdm.Extensions.Hosting.IHostBuilder, System.String, out.wdm.Extensions.Hosting.IHostBuilder }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}