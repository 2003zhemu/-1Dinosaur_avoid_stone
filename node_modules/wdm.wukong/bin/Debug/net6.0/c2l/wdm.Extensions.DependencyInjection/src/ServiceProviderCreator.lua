-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
local wdmDependencyInjection
local ListServiceMaker
local DictTypeServiceMaker
local DictTypeListServiceMaker
local __importGlobal =function (out)
  wdmDependencyInjection = out.wdm.Extensions.DependencyInjection
  ListServiceMaker = System.List(_G.wdm.Extensions.DependencyInjection.ServiceMaker)
  DictTypeServiceMaker = System.Dictionary(System.Type, _G.wdm.Extensions.DependencyInjection.ServiceMaker)
  DictTypeListServiceMaker = System.Dictionary(System.Type, ListServiceMaker)
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.DependencyInjection", function (namespace)
  -- <summary>
  -- ����ע������
  -- </summary>
  namespace.class("ServiceProviderCreator", function (namespace)
    local Register, Build, _validateDescrptor, _updateMakers, _getServiceMaker, internal, __ctor1__, __ctor2__
    internal = function (this)
      this._serviceMakers = DictTypeServiceMaker()
      this._serviceMakerLists = DictTypeListServiceMaker()
    end
    -- <summary>
    -- ���캯��
    -- </summary>
    __ctor1__ = function (this)
      internal(this)
      this.Services = wdmDependencyInjection.ServiceCollection()
      Register(this, System.new(wdmDependencyInjection.ServiceDescriptor, 3, System.typeof(System.IServiceProvider), function (_)
        return _
      end, wdmDependencyInjection.ServiceLifetime.Scoped))
      Register(this, System.new(wdmDependencyInjection.ServiceDescriptor, 3, System.typeof(wdmDependencyInjection.IServiceScopeFactory), function (x)
        return wdmDependencyInjection.ServiceScopeFactory(x)
      end, wdmDependencyInjection.ServiceLifetime.Singleton))
      local checker =wdmDependencyInjection.IsServiceChecker(this.Services)
      Register(this, System.new(wdmDependencyInjection.ServiceDescriptor, 2, System.typeof(wdmDependencyInjection.IServiceProviderIsService), checker))
    end
    __ctor2__ = function (this, services, options)
      __ctor1__(this)
      for _, serviceDescriptor in System.each(services) do
        Register(this, serviceDescriptor)
      end
    end
    -- <summary>
    -- ע�����,ʹ�� ServiceDescriptor ��������.
    -- </summary>
    Register = function (this, descriptor)
      this.Services:Add(descriptor)
    end
    Build = function (this)
      for _, descriptor in System.each(this.Services) do
        _validateDescrptor(this, descriptor)
        local sm =wdmDependencyInjection.ServiceMaker(descriptor)
        _updateMakers(this, descriptor.ServiceType, sm)
      end
      
      local lifetime =wdmDependencyInjection.ServiceProvider(nil, this.Services)
      return lifetime
    end
    _validateDescrptor = function (this, descriptor)
      local serviceType =descriptor.ServiceType
      local implementationType =descriptor.ImplementationType
      if implementationType == nil then
        return
      end
      local isPass =true
      local errMessage =System.toString(System.toString(System.toString(System.toString("Cannot instantiate implementation type '") .. System.toString(System.toString(implementationType))) .. System.toString("' for service type '")) .. System.toString(System.toString(serviceType))) .. System.toString("'.")
      if implementationType:getIsAbstract() then
        isPass = false
      end
      if implementationType:getIsGenericTypeDefinition() then
        if serviceType:getIsGenericTypeDefinition() then
          if #implementationType:GetGenericTypeDefinition():GetGenericArguments() ~= #serviceType:GetGenericArguments() then
            errMessage = System.toString(System.toString(System.toString(System.toString("Arity of open generic service type '") .. System.toString(System.toString(serviceType))) .. System.toString("' does not equal arity of open generic implementation type '")) .. System.toString(System.toString(implementationType))) .. System.toString("'.")
            isPass = false
          end
        else
          isPass = false
        end
      else
        if serviceType:getIsGenericTypeDefinition() then
          isPass = false
          errMessage = System.toString(System.toString("Open generic service type '") .. System.toString(System.toString(serviceType))) .. System.toString("' requires registering an open generic implementation type.")
        end
      end
      
      
      if not isPass then
        System.throw(System.ArgumentException(errMessage))
      end
    end
    _updateMakers = function (this, interface, sm)
      this._serviceMakers:set(interface, sm)
      
      -- ����Ѿ��Ǽ�����,�򲻲���.
      if System.typeof(System.IEnumerable):IsAssignableFrom(interface) then
        return
      end
      
      local default, list = this._serviceMakerLists:TryGetValue(interface, nil)
      if not default then
        list = ListServiceMaker()
        this._serviceMakerLists:AddKeyValue(interface, list)
      end
      list:Insert(0, sm)
    end
    _getServiceMaker = function (this, interface)
      -- �ѻ���
      local default, o = this._serviceMakers:TryGetValue(interface, nil)
      if default then
        return o
      end
      
      -- ����
      if System.typeof(System.IEnumerable):IsAssignableFrom(interface) then
        local list =ListServiceMaker()
        local emuTypes =interface:GetGenericArguments()
        local curType =emuTypes:get(0)
        local extern, sms = this._serviceMakerLists:TryGetValue(curType, nil)
        if extern then
          list:AddRange(sms)
        end
        
        if curType:getIsGenericType() then
          local res =curType:GetGenericTypeDefinition()
          local ref
          ref, sms = this._serviceMakerLists:TryGetValue(res, nil)
          if ref then
            list:AddRange(sms)
          end
        end
        
        list = Linq.ToList(Linq.OrderByDescending(list, function (x)
          return x.Order
        end, nil, System.Int32))
        
        return list
      end
      
      
      if interface:getIsGenericType() then
        local res =interface:GetGenericTypeDefinition()
        local extern
        extern, o = this._serviceMakers:TryGetValue(res, nil)
        if extern then
          return o
        end
      end
      
      return nil
    end
    return {
      Register = Register,
      Build = Build,
      __ctor__ = {
        __ctor1__,
        __ctor2__
      },
      __metadata__ = function (out)
        return {
          fields = {
            { "Services", 0x6, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "_serviceMakerLists", 0x1, System.Dictionary(System.Type, System.List(out.wdm.Extensions.DependencyInjection.ServiceMaker)) },
            { "_serviceMakers", 0x1, System.Dictionary(System.Type, out.wdm.Extensions.DependencyInjection.ServiceMaker) }
          },
          methods = {
            { ".ctor", 0x6, __ctor1__ },
            { ".ctor", 0x206, __ctor2__, out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.ServiceProviderOptions },
            { "Build", 0x86, Build, out.wdm.Extensions.DependencyInjection.ServiceProvider },
            { "Register", 0x106, Register, out.wdm.Extensions.DependencyInjection.ServiceDescriptor },
            { "_getServiceMaker", 0x181, _getServiceMaker, System.Type, System.Object },
            { "_updateMakers", 0x201, _updateMakers, System.Type, out.wdm.Extensions.DependencyInjection.ServiceMaker },
            { "_validateDescrptor", 0x101, _validateDescrptor, out.wdm.Extensions.DependencyInjection.ServiceDescriptor }
          },
          class = { 0x4 }
        }
      end
    }
  end)
end)
return {}