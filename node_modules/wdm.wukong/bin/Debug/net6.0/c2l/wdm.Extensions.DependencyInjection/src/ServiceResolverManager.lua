-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
local wdmDependencyInjection
local DictTypeServiceResolver
local __importGlobal =function (out)
  wdmDependencyInjection = out.wdm.Extensions.DependencyInjection
  DictTypeServiceResolver = System.Dictionary(System.Type, _G.wdm.Extensions.DependencyInjection.ServiceResolver)
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.DependencyInjection", function (namespace)
  namespace.class("ServiceResolverManager", function (namespace)
    local Resolve, Resolve1, _getOrAddServiceResolver, _newResolver, Dispose, __ctor__
    __ctor__ = function (this, services)
      this._services = services
      this._resolvers = DictTypeServiceResolver()
    end
    Resolve = function (this, serviceProvider, service, descriptor, unsolved)
      return wdmDependencyInjection.ServiceDescriptorResolver.Resolve(serviceProvider, service, descriptor, unsolved)
    end
    Resolve1 = function (this, serviceProvider, service, unsolved)
      local resolver =_getOrAddServiceResolver(this, service)
      if resolver == nil then
        return nil
      end
      local result =resolver:Relove(serviceProvider, service, unsolved)
      return result
    end
    _getOrAddServiceResolver = function (this, service)
      local default, resolver = this._resolvers:TryGetValue(service, nil)
      if not default then
        resolver = _newResolver(this, service)
        if resolver == nil then
          return nil
        end
        this._resolvers:AddKeyValue(service, resolver)
      end
      
      return resolver
    end
    _newResolver = function (this, serviceType)
      if serviceType:getIsGenericTypeDefinition() then
        System.throw(System.Exception("type Is GenericTypeDefinition"))
      end
      
      local match =Linq.LastOrDefault(this._services, function (x)
        return x.ServiceType == serviceType
      end)
      
      if match == nil and serviceType:getIsGenericType() then
        local genericDefine =serviceType:GetGenericTypeDefinition()
        match = Linq.LastOrDefault(this._services, function (x)
          return x.ServiceType == genericDefine
        end)
      end
      
      
      if match ~= nil then
        return System.new(wdmDependencyInjection.ServiceResolver, 2, match)
      elseif System.typeof(System.IEnumerable):IsAssignableFrom(serviceType) and serviceType:getIsGenericType() then
        local genericType =serviceType:GetGenericArguments():get(0)
        local matches =Linq.ToArray(Linq.Where(this._services, function (x)
          if genericType:getIsGenericType() then
            local result =x.ServiceType == genericType or x.ServiceType == genericType:GetGenericTypeDefinition()
            return result
          else
            return x.ServiceType == genericType
          end
        end))
        
        
        return wdmDependencyInjection.ServiceResolver(serviceType, matches)
      end
      
      return nil
    end
    Dispose = function (this)
      this._resolvers:Clear()
    end
    return {
      base = function (out)
        return {
          out.System.IDisposable
        }
      end,
      Resolve = Resolve,
      Resolve1 = Resolve1,
      Dispose = Dispose,
      __ctor__ = __ctor__,
      __metadata__ = function (out)
        return {
          fields = {
            { "_resolvers", 0x1, System.Dictionary(System.Type, out.wdm.Extensions.DependencyInjection.ServiceResolver) },
            { "_services", 0x1, out.wdm.Extensions.DependencyInjection.IServiceCollection }
          },
          methods = {
            { ".ctor", 0x106, nil, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "Dispose", 0x6, Dispose },
            { "Resolve", 0x486, Resolve, out.wdm.Extensions.DependencyInjection.ServiceProvider, System.Type, out.wdm.Extensions.DependencyInjection.ServiceDescriptor, System.List(System.Type), System.Object },
            { "Resolve", 0x386, Resolve1, out.wdm.Extensions.DependencyInjection.ServiceProvider, System.Type, System.List(System.Type), System.Object },
            { "_getOrAddServiceResolver", 0x181, _getOrAddServiceResolver, System.Type, out.wdm.Extensions.DependencyInjection.ServiceResolver },
            { "_newResolver", 0x181, _newResolver, System.Type, out.wdm.Extensions.DependencyInjection.ServiceResolver }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}