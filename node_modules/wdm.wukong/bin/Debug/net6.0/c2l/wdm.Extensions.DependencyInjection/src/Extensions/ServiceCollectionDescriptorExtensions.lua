-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmDependencyInjection
local wdmToolkitDiagnostics
local __importGlobal =function (out)
  wdmDependencyInjection = out.wdm.Extensions.DependencyInjection
  wdmToolkitDiagnostics = out.wdm.Toolkit.Diagnostics
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.DependencyInjection.Extensions", function (namespace)
  -- <summary>
  -- Extension methods for adding and removing services to an <see cref="IServiceCollection" />.
  -- </summary>
  namespace.class("ServiceCollectionDescriptorExtensions", function (namespace)
    local Add, Add1, TryAdd, TryAdd1, TryAddTransient, TryAddTransient1, TryAddTransient2, TryAddTransient3, 
    TryAddTransient4, TryAddTransient5, TryAddScoped, TryAddScoped1, TryAddScoped2, TryAddScoped3, TryAddScoped4, TryAddScoped5, 
    TryAddSingleton, TryAddSingleton1, TryAddSingleton2, TryAddSingleton3, TryAddSingleton4, TryAddSingleton5, TryAddSingleton6, TryAddEnumerable, 
    TryAddEnumerable1, Replace, RemoveAll, RemoveAll1
    -- <summary>
    -- Adds the specified <paramref name="descriptor"/> to the <paramref name="collection"/>.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="descriptor">The <see cref="ServiceDescriptor"/> to add.</param>
    -- <returns>A reference to the current instance of <see cref="IServiceCollection"/>.</returns>
    Add = function (collection, descriptor)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(descriptor)
      
      collection:Add(descriptor)
      return collection
    end
    -- <summary>
    -- Adds a sequence of <see cref="ServiceDescriptor"/> to the <paramref name="collection"/>.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="descriptors">The <see cref="ServiceDescriptor"/>s to add.</param>
    -- <returns>A reference to the current instance of <see cref="IServiceCollection"/>.</returns>
    Add1 = function (collection, descriptors)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(descriptors)
      
      for _, descriptor in System.each(descriptors) do
        collection:Add(descriptor)
      end
      
      return collection
    end
    -- <summary>
    -- Adds the specified <paramref name="descriptor"/> to the <paramref name="collection"/> if the
    -- service type hasn't already been registered.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="descriptor">The <see cref="ServiceDescriptor"/> to add.</param>
    TryAdd = function (collection, descriptor)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(descriptor)
      
      local count =collection:getCount()
      for i = 0, count - 1 do
        if collection:get(i).ServiceType == descriptor.ServiceType then
          -- Already added
          return
        end
      end
      
      collection:Add(descriptor)
    end
    -- <summary>
    -- Adds the specified <paramref name="descriptors"/> to the <paramref name="collection"/> if the
    -- service type hasn't already been registered.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="descriptors">The <see cref="ServiceDescriptor"/>s to add.</param>
    TryAdd1 = function (collection, descriptors)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(descriptors)
      
      for _, d in System.each(descriptors) do
        TryAdd(collection, d)
      end
    end
    -- <summary>
    -- Adds the specified <paramref name="service"/> as a <see cref="ServiceLifetime.Transient"/> service
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="service">The type of the service to register.</param>
    TryAddTransient = function (collection, service)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(service)
      
      local descriptor =wdmDependencyInjection.ServiceDescriptor.Transient1(service, service)
      TryAdd(collection, descriptor)
    end
    -- <summary>
    -- Adds the specified <paramref name="service"/> as a <see cref="ServiceLifetime.Transient"/> service
    -- with the <paramref name="implementationType"/> implementation
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="service">The type of the service to register.</param>
    -- <param name="implementationType">The implementation type of the service.</param>
    TryAddTransient1 = function (collection, service, implementationType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(service)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationType)
      
      local descriptor =wdmDependencyInjection.ServiceDescriptor.Transient1(service, implementationType)
      TryAdd(collection, descriptor)
    end
    -- <summary>
    -- Adds the specified <paramref name="service"/> as a <see cref="ServiceLifetime.Transient"/> service
    -- using the factory specified in <paramref name="implementationFactory"/>
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="service">The type of the service to register.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    TryAddTransient2 = function (collection, service, implementationFactory)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(service)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      local descriptor =wdmDependencyInjection.ServiceDescriptor.Transient4(service, implementationFactory)
      TryAdd(collection, descriptor)
    end
    -- <summary>
    -- Adds the specified <typeparamref name="TService"/> as a <see cref="ServiceLifetime.Transient"/> service
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    TryAddTransient3 = function (collection, TService)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      
      TryAddTransient1(collection, System.typeof(TService), System.typeof(TService))
    end
    -- <summary>
    -- Adds the specified <typeparamref name="TService"/> as a <see cref="ServiceLifetime.Transient"/> service
    -- implementation type specified in <typeparamref name="TImplementation"/>
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <typeparam name="TImplementation">The type of the implementation to use.</typeparam>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    TryAddTransient4 = function (collection, TService, TImplementation)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      
      TryAddTransient1(collection, System.typeof(TService), System.typeof(TImplementation))
    end
    -- <summary>
    -- Adds the specified <typeparamref name="TService"/> as a <see cref="ServiceLifetime.Transient"/> service
    -- using the factory specified in <paramref name="implementationFactory"/>
    -- to the <paramref name="services"/> if the service type hasn't already been registered.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/>.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    TryAddTransient5 = function (services, implementationFactory, TService)
      TryAdd(services, wdmDependencyInjection.ServiceDescriptor.Transient3(implementationFactory, TService))
    end
    -- <summary>
    -- Adds the specified <paramref name="service"/> as a <see cref="ServiceLifetime.Scoped"/> service
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="service">The type of the service to register.</param>
    TryAddScoped = function (collection, service)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(service)
      
      local descriptor =wdmDependencyInjection.ServiceDescriptor.Scoped1(service, service)
      TryAdd(collection, descriptor)
    end
    -- <summary>
    -- Adds the specified <paramref name="service"/> as a <see cref="ServiceLifetime.Scoped"/> service
    -- with the <paramref name="implementationType"/> implementation
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="service">The type of the service to register.</param>
    -- <param name="implementationType">The implementation type of the service.</param>
    TryAddScoped1 = function (collection, service, implementationType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(service)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationType)
      
      local descriptor =wdmDependencyInjection.ServiceDescriptor.Scoped1(service, implementationType)
      TryAdd(collection, descriptor)
    end
    -- <summary>
    -- Adds the specified <paramref name="service"/> as a <see cref="ServiceLifetime.Scoped"/> service
    -- using the factory specified in <paramref name="implementationFactory"/>
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="service">The type of the service to register.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    TryAddScoped2 = function (collection, service, implementationFactory)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(service)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      local descriptor =wdmDependencyInjection.ServiceDescriptor.Scoped4(service, implementationFactory)
      TryAdd(collection, descriptor)
    end
    -- <summary>
    -- Adds the specified <typeparamref name="TService"/> as a <see cref="ServiceLifetime.Scoped"/> service
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    TryAddScoped3 = function (collection, TService)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      
      TryAddScoped1(collection, System.typeof(TService), System.typeof(TService))
    end
    -- <summary>
    -- Adds the specified <typeparamref name="TService"/> as a <see cref="ServiceLifetime.Scoped"/> service
    -- implementation type specified in <typeparamref name="TImplementation"/>
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <typeparam name="TImplementation">The type of the implementation to use.</typeparam>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    TryAddScoped4 = function (collection, TService, TImplementation)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      
      TryAddScoped1(collection, System.typeof(TService), System.typeof(TImplementation))
    end
    -- <summary>
    -- Adds the specified <typeparamref name="TService"/> as a <see cref="ServiceLifetime.Scoped"/> service
    -- using the factory specified in <paramref name="implementationFactory"/>
    -- to the <paramref name="services"/> if the service type hasn't already been registered.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/>.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    TryAddScoped5 = function (services, implementationFactory, TService)
      TryAdd(services, wdmDependencyInjection.ServiceDescriptor.Scoped3(implementationFactory, TService))
    end
    -- <summary>
    -- Adds the specified <paramref name="service"/> as a <see cref="ServiceLifetime.Singleton"/> service
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="service">The type of the service to register.</param>
    TryAddSingleton = function (collection, service)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(service)
      
      local descriptor =wdmDependencyInjection.ServiceDescriptor.Singleton1(service, service)
      TryAdd(collection, descriptor)
    end
    -- <summary>
    -- Adds the specified <paramref name="service"/> as a <see cref="ServiceLifetime.Singleton"/> service
    -- with the <paramref name="implementationType"/> implementation
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="service">The type of the service to register.</param>
    -- <param name="implementationType">The implementation type of the service.</param>
    TryAddSingleton1 = function (collection, service, implementationType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(service)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationType)
      
      local descriptor =wdmDependencyInjection.ServiceDescriptor.Singleton1(service, implementationType)
      TryAdd(collection, descriptor)
    end
    -- <summary>
    -- Adds the specified <paramref name="service"/> as a <see cref="ServiceLifetime.Singleton"/> service
    -- using the factory specified in <paramref name="implementationFactory"/>
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="service">The type of the service to register.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    TryAddSingleton2 = function (collection, service, implementationFactory)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(service)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(implementationFactory)
      
      local descriptor =wdmDependencyInjection.ServiceDescriptor.Singleton4(service, implementationFactory)
      TryAdd(collection, descriptor)
    end
    -- <summary>
    -- Adds the specified <typeparamref name="TService"/> as a <see cref="ServiceLifetime.Singleton"/> service
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    TryAddSingleton3 = function (collection, TService)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      
      TryAddSingleton1(collection, System.typeof(TService), System.typeof(TService))
    end
    -- <summary>
    -- Adds the specified <typeparamref name="TService"/> as a <see cref="ServiceLifetime.Singleton"/> service
    -- implementation type specified in <typeparamref name="TImplementation"/>
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <typeparam name="TImplementation">The type of the implementation to use.</typeparam>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    TryAddSingleton4 = function (collection, TService, TImplementation)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      
      TryAddSingleton1(collection, System.typeof(TService), System.typeof(TImplementation))
    end
    -- <summary>
    -- Adds the specified <typeparamref name="TService"/> as a <see cref="ServiceLifetime.Singleton"/> service
    -- with an instance specified in <paramref name="instance"/>
    -- to the <paramref name="collection"/> if the service type hasn't already been registered.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="instance">The instance of the service to add.</param>
    TryAddSingleton5 = function (collection, instance, TService)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(instance)
      
      local descriptor =wdmDependencyInjection.ServiceDescriptor.Singleton6(System.typeof(TService), instance)
      TryAdd(collection, descriptor)
    end
    -- <summary>
    -- Adds the specified <typeparamref name="TService"/> as a <see cref="ServiceLifetime.Singleton"/> service
    -- using the factory specified in <paramref name="implementationFactory"/>
    -- to the <paramref name="services"/> if the service type hasn't already been registered.
    -- </summary>
    -- <typeparam name="TService">The type of the service to add.</typeparam>
    -- <param name="services">The <see cref="IServiceCollection"/>.</param>
    -- <param name="implementationFactory">The factory that creates the service.</param>
    TryAddSingleton6 = function (services, implementationFactory, TService)
      TryAdd(services, wdmDependencyInjection.ServiceDescriptor.Singleton3(implementationFactory, TService))
    end
    -- <summary>
    -- Adds a <see cref="ServiceDescriptor"/> if an existing descriptor with the same
    -- <see cref="ServiceDescriptor.ServiceType"/> and an implementation that does not already exist
    -- in <paramref name="services."/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/>.</param>
    -- <param name="descriptor">The <see cref="ServiceDescriptor"/>.</param>
    -- <remarks>
    -- Use <see cref="TryAddEnumerable(IServiceCollection, ServiceDescriptor)"/> when registering a service implementation of a
    -- service type that
    -- supports multiple registrations of the same service type. Using
    -- <see cref="Add(IServiceCollection, ServiceDescriptor)"/> is not idempotent and can add
    -- duplicate
    -- <see cref="ServiceDescriptor"/> instances if called twice. Using
    -- <see cref="TryAddEnumerable(IServiceCollection, ServiceDescriptor)"/> will prevent registration
    -- of multiple implementation types.
    -- </remarks>
    TryAddEnumerable = function (services, descriptor)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(descriptor)
      
      local implementationType =descriptor:GetImplementationType()
      
      -- kk modify,�ų�ImplementationFactory
      if descriptor.ImplementationFactory == nil then
        if implementationType == System.typeof(System.Object) or implementationType == descriptor.ServiceType then
          System.throw(System.ArgumentException(System.toString(System.toString(System.toString(System.toString("Implementation type cannot be '") .. System.toString(System.toString(implementationType))) .. System.toString("' because it is indistinguishable from other services registered for '")) .. System.toString(System.toString(descriptor.ServiceType))) .. System.toString("'.")))
        end
      end
      
      
      local count =services:getCount()
      for i = 0, count - 1 do
        local service =services:get(i)
        if service.ServiceType == descriptor.ServiceType and (service:GetImplementationType()) == implementationType then
          -- Already added
          return
        end
      end
      
      services:Add(descriptor)
    end
    -- <summary>
    -- Adds the specified <see cref="ServiceDescriptor"/>s if an existing descriptor with the same
    -- <see cref="ServiceDescriptor.ServiceType"/> and an implementation that does not already exist
    -- in <paramref name="services."/>.
    -- </summary>
    -- <param name="services">The <see cref="IServiceCollection"/>.</param>
    -- <param name="descriptors">The <see cref="ServiceDescriptor"/>s.</param>
    -- <remarks>
    -- Use <see cref="TryAddEnumerable(IServiceCollection, ServiceDescriptor)"/> when registering a service
    -- implementation of a service type that
    -- supports multiple registrations of the same service type. Using
    -- <see cref="Add(IServiceCollection, ServiceDescriptor)"/> is not idempotent and can add
    -- duplicate
    -- <see cref="ServiceDescriptor"/> instances if called twice. Using
    -- <see cref="TryAddEnumerable(IServiceCollection, ServiceDescriptor)"/> will prevent registration
    -- of multiple implementation types.
    -- </remarks>
    TryAddEnumerable1 = function (services, descriptors)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(services)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(descriptors)
      
      for _, d in System.each(descriptors) do
        TryAddEnumerable(services, d)
      end
    end
    -- <summary>
    -- Removes the first service in <see cref="IServiceCollection"/> with the same service type
    -- as <paramref name="descriptor"/> and adds <paramref name="descriptor"/> to the collection.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="descriptor">The <see cref="ServiceDescriptor"/> to replace with.</param>
    -- <returns>The <see cref="IServiceCollection"/> for chaining.</returns>
    Replace = function (collection, descriptor)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(collection)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(descriptor)
      
      -- Remove existing
      local count =collection:getCount()
      for i = 0, count - 1 do
        if collection:get(i).ServiceType == descriptor.ServiceType then
          collection:RemoveAt(i)
          break
        end
      end
      
      collection:Add(descriptor)
      return collection
    end
    -- <summary>
    -- Removes all services of type <typeparamref name="T"/> in <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <returns>The <see cref="IServiceCollection"/> for chaining.</returns>
    RemoveAll = function (collection, T)
      return RemoveAll1(collection, System.typeof(T))
    end
    -- <summary>
    -- Removes all services of type <paramref name="serviceType"/> in <see cref="IServiceCollection"/>.
    -- </summary>
    -- <param name="collection">The <see cref="IServiceCollection"/>.</param>
    -- <param name="serviceType">The service type to remove.</param>
    -- <returns>The <see cref="IServiceCollection"/> for chaining.</returns>
    RemoveAll1 = function (collection, serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      
      for i = collection:getCount() - 1, 0, -1 do
        local descriptor =collection:get(i)
        if descriptor.ServiceType == serviceType then
          collection:RemoveAt(i)
        end
      end
      
      return collection
    end
    return {
      Add = Add,
      Add1 = Add1,
      TryAdd = TryAdd,
      TryAdd1 = TryAdd1,
      TryAddTransient = TryAddTransient,
      TryAddTransient1 = TryAddTransient1,
      TryAddTransient2 = TryAddTransient2,
      TryAddTransient3 = TryAddTransient3,
      TryAddTransient4 = TryAddTransient4,
      TryAddTransient5 = TryAddTransient5,
      TryAddScoped = TryAddScoped,
      TryAddScoped1 = TryAddScoped1,
      TryAddScoped2 = TryAddScoped2,
      TryAddScoped3 = TryAddScoped3,
      TryAddScoped4 = TryAddScoped4,
      TryAddScoped5 = TryAddScoped5,
      TryAddSingleton = TryAddSingleton,
      TryAddSingleton1 = TryAddSingleton1,
      TryAddSingleton2 = TryAddSingleton2,
      TryAddSingleton3 = TryAddSingleton3,
      TryAddSingleton4 = TryAddSingleton4,
      TryAddSingleton5 = TryAddSingleton5,
      TryAddSingleton6 = TryAddSingleton6,
      TryAddEnumerable = TryAddEnumerable,
      TryAddEnumerable1 = TryAddEnumerable1,
      Replace = Replace,
      RemoveAll = RemoveAll,
      RemoveAll1 = RemoveAll1,
      __metadata__ = function (out)
        return {
          methods = {
            { "Add", 0x28E, Add, out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.ServiceDescriptor, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "Add", 0x28E, Add1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.IEnumerable_1(out.wdm.Extensions.DependencyInjection.ServiceDescriptor), out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "RemoveAll", 0x1018E, RemoveAll, function (T) return out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "RemoveAll", 0x28E, RemoveAll1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "Replace", 0x28E, Replace, out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.ServiceDescriptor, out.wdm.Extensions.DependencyInjection.IServiceCollection },
            { "TryAdd", 0x20E, TryAdd1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.IEnumerable_1(out.wdm.Extensions.DependencyInjection.ServiceDescriptor) },
            { "TryAdd", 0x20E, TryAdd, out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.ServiceDescriptor },
            { "TryAddEnumerable", 0x20E, TryAddEnumerable1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.IEnumerable_1(out.wdm.Extensions.DependencyInjection.ServiceDescriptor) },
            { "TryAddEnumerable", 0x20E, TryAddEnumerable, out.wdm.Extensions.DependencyInjection.IServiceCollection, out.wdm.Extensions.DependencyInjection.ServiceDescriptor },
            { "TryAddScoped", 0x1020E, TryAddScoped5, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Delegate(System.IServiceProvider, TService) end },
            { "TryAddScoped", 0x2010E, TryAddScoped4, function (TService, TImplementation) return out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "TryAddScoped", 0x30E, TryAddScoped2, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Delegate(System.IServiceProvider, System.Object) },
            { "TryAddScoped", 0x1010E, TryAddScoped3, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "TryAddScoped", 0x20E, TryAddScoped, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type },
            { "TryAddScoped", 0x30E, TryAddScoped1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Type },
            { "TryAddSingleton", 0x20E, TryAddSingleton, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type },
            { "TryAddSingleton", 0x30E, TryAddSingleton1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Type },
            { "TryAddSingleton", 0x30E, TryAddSingleton2, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Delegate(System.IServiceProvider, System.Object) },
            { "TryAddSingleton", 0x1010E, TryAddSingleton3, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "TryAddSingleton", 0x2010E, TryAddSingleton4, function (TService, TImplementation) return out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "TryAddSingleton", 0x1020E, TryAddSingleton5, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection, TService end },
            { "TryAddSingleton", 0x1020E, TryAddSingleton6, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Delegate(System.IServiceProvider, TService) end },
            { "TryAddTransient", 0x1020E, TryAddTransient5, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Delegate(System.IServiceProvider, TService) end },
            { "TryAddTransient", 0x2010E, TryAddTransient4, function (TService, TImplementation) return out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "TryAddTransient", 0x1010E, TryAddTransient3, function (TService) return out.wdm.Extensions.DependencyInjection.IServiceCollection end },
            { "TryAddTransient", 0x30E, TryAddTransient2, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Delegate(System.IServiceProvider, System.Object) },
            { "TryAddTransient", 0x30E, TryAddTransient1, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type, System.Type },
            { "TryAddTransient", 0x20E, TryAddTransient, out.wdm.Extensions.DependencyInjection.IServiceCollection, System.Type }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}