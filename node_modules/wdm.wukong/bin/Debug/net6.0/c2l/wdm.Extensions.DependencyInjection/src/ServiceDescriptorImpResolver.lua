-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
local ListType =System.List(System.Type)
local wdmExtensions
local __importGlobal =function (out)
  wdmExtensions = out.wdm.Extensions.DependencyInjection.Extensions
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.DependencyInjection", function (namespace)
  namespace.class("ServiceDescriptorImplementationTypeResolver", function (namespace)
    local ResolveFromImplementationType, _resolveGeneric, _getArgs, _updateUnsolvedService, _getConstructor
    ResolveFromImplementationType = function (serviceProvider, service, descriptor, unsolved)
      local default, finalImplType, isConstraintError = _getConstructor(service, descriptor)
      local constructor =default
      if isConstraintError then
        return nil
      end
      local args
      if System.Object.EqualsStatic(constructor, nil) then
        args = nil
      else
        args = _getArgs(descriptor, serviceProvider, constructor, unsolved)
      end
      if finalImplType:getIsGenericTypeDefinition() then
      end
      
      local extern, ref = System.try(function ()
        if not descriptor.ServiceType:getIsGenericTypeDefinition() then
          return true, System.Activator.CreateInstance(finalImplType, args)
        else
          return true, _resolveGeneric(finalImplType, descriptor, args, false)
        end
      end, function (default)
        local e =default
        System.Console.WriteLine(e)
        System.throw(default)
      end)
      if extern then
        return ref
      end
    end
    _resolveGeneric = function (type, _descriptor, args, isEnumerableGeneric)
      local configurationInstance =System.Activator.CreateInstance(type, args)
      return configurationInstance
    end
    _getArgs = function (_descriptor, serviceProvider, constructor, unsolved)
      local args =nil
      local implementationType =_descriptor.ImplementationType
      local serviceType =_descriptor.ServiceType
      if not System.Object.EqualsStatic(constructor, nil) then
        local constructorParams =constructor:GetParameters()
        args = Linq.ToArray(Linq.Select(constructorParams, function (param)
          local default, extern = System.try(function ()
            unsolved = _updateUnsolvedService(_descriptor, unsolved, serviceType)
            if unsolved:Contains(param:getParameterType()) then
              System.throw(wdmExtensions.CircularDependencyException(param:getParameterType()))
            end
            
            local resolved =serviceProvider:GetService1(unsolved, param:getParameterType())
            
            if resolved == nil then
              System.throw(System.InvalidOperationException(System.toString(System.toString(System.toString(System.toString("Unable to resolve service for type '") .. System.toString(param:getParameterType():getFullName())) .. System.toString("' while attempting to activate '")) .. System.toString(System.toString(_descriptor.ImplementationType))) .. System.toString("'")))
            end
            
            unsolved:Remove(serviceType)
            
            return true, resolved
          end, function (default)
            if System.is(default, wdmExtensions.CircularDependencyException) then
              local e =default
              System.Console.WriteLine(System.String.JoinEnumerable("\n", unsolved))
              e:Add(serviceType)
              if Linq.FirstOrDefault(unsolved) == serviceType then
                local sb =System.StringBuilder()
                e.Services:Reverse()
                sb:AppendLine(System.toString(System.toString("A circular dependency was detected for the service of type '") .. System.toString(System.toString(Linq.Last(e.Services)))) .. System.toString("'."))
                sb:Append(System.String.JoinEnumerable(" -> ", e.Services))
                System.throw(System.InvalidOperationException(sb:ToString()))
              end
              
              System.throw(default)
            else
              return 1, default
            end
          end)
          if default then
            return extern
          end
        end, System.Object))
      end
      return args
    end
    _updateUnsolvedService = function (_descriptor, unsolved, service)
      if unsolved == nil then
        unsolved = ListType()
      end
      
      if not unsolved:Contains(service) then
        unsolved:Add(service)
      end
      return unsolved
    end
    _getConstructor = function (service, descriptor, finalImplType, isConstraintError)
      local implementationType =descriptor.ImplementationType
      finalImplType = implementationType
      local serviceType =descriptor.ServiceType
      local constructors =nil
      isConstraintError = false
      if implementationType:getIsGenericType() then
        local genericArgs =service:GetGenericArguments()
        
        local tmp =genericArgs:get(0):GetGenericArguments()
        if Linq.Any(tmp) then
          genericArgs = tmp
        end
        
        if implementationType:getIsGenericTypeDefinition() then
          local genericType =nil
          
          local default, extern = System.try(function ()
            genericType = implementationType:MakeGenericType(genericArgs)
          end, function (default)
            -- 如果约束冲突( constraint ),会报错,此处忽略错误
            isConstraintError = true
            return true, nil
          end)
          if default then
            return extern, finalImplType, isConstraintError
          end
          
          finalImplType = genericType
          
          constructors = genericType:GetConstructors()
        else
          -- Get first constructor for the type
          constructors = implementationType:GetConstructors()
        end
      else
        -- Get first constructor for the type
        constructors = implementationType:GetConstructors()
      end
      
      if #constructors == 0 then
        -- If no public constructor found, search for an internal constructor
        constructors = implementationType:GetConstructors(36 --[[BindingFlags.Instance | BindingFlags.NonPublic]])
      end
      
      local constructor =Linq.FirstOrDefault(constructors)
      
      
      if finalImplType:getIsGenericTypeDefinition() then
      end
      
      return constructor, finalImplType, isConstraintError
    end
    return {
      ResolveFromImplementationType = ResolveFromImplementationType,
      __metadata__ = function (out)
        return {
          methods = {
            { "ResolveFromImplementationType", 0x48E, ResolveFromImplementationType, out.wdm.Extensions.DependencyInjection.ServiceProvider, System.Type, out.wdm.Extensions.DependencyInjection.ServiceDescriptor, System.List(System.Type), System.Object },
            { "_getArgs", 0x489, _getArgs, out.wdm.Extensions.DependencyInjection.ServiceDescriptor, out.wdm.Extensions.DependencyInjection.ServiceProvider, System.Reflection.ConstructorInfo, System.List(System.Type), System.Array(System.Object) },
            { "_getConstructor", 0x489, _getConstructor, System.Type, out.wdm.Extensions.DependencyInjection.ServiceDescriptor, System.Type, System.Boolean, System.Reflection.ConstructorInfo },
            { "_resolveGeneric", 0x489, _resolveGeneric, System.Type, out.wdm.Extensions.DependencyInjection.ServiceDescriptor, System.Array(System.Object), System.Boolean, System.Object },
            { "_updateUnsolvedService", 0x389, _updateUnsolvedService, out.wdm.Extensions.DependencyInjection.ServiceDescriptor, System.List(System.Type), System.Type, System.List(System.Type) }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}