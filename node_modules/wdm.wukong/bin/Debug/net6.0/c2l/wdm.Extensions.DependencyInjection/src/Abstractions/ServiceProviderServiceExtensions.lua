-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmDependencyInjection
local wdmToolkitDiagnostics
local __importGlobal =function (out)
  wdmDependencyInjection = out.wdm.Extensions.DependencyInjection
  wdmToolkitDiagnostics = out.wdm.Toolkit.Diagnostics
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.DependencyInjection", function (namespace)
  namespace.class("ServiceProviderServiceExtensions", function (namespace)
    local GetRequiredService, GetRequiredService1, GetService, CreateScope, GetServices, GetServices1
    -- <summary>
    -- 获取指定类型服务
    -- </summary>
    -- <param name="provider"></param>
    -- <param name="serviceType">服务类型</param>
    -- <returns></returns>
    -- <exception cref="InvalidOperationException">未找到指定服务</exception>
    GetRequiredService = function (provider, serviceType)
      local service =provider:GetService(serviceType)
      if service == nil then
        System.throw(System.InvalidOperationException(System.toString(System.toString("No service for type '") .. System.toString(System.toString(serviceType))) .. System.toString("' has been registered.")))
      end
      return service
    end
    -- <summary>
    -- 获取指定类型服务
    -- </summary>
    -- <param name="provider"></param>
    -- <typeparam name="T"></typeparam>
    -- <returns></returns>
    -- <exception cref="InvalidOperationException">未找到指定服务</exception>
    GetRequiredService1 = function (provider, T)
      local default, extern = System.try(function ()
        local service =provider:GetService(System.typeof(T))
        if service == nil then
          System.throw(System.InvalidOperationException(System.toString(System.toString("No service for type '") .. System.toString(System.toString(System.typeof(T)))) .. System.toString("' has been registered.")))
        end
        return true, service
      end, function (default)
        if System.is(default, System.KeyNotFoundException) then
          local e =default
          System.throw(System.InvalidOperationException(System.toString(System.toString(e:getMessage()) .. System.toString(",while resolving ")) .. System.toString(System.typeof(T):getFullName())))
        else
          return 1, default
        end
      end)
      if default then
        return extern
      end
    end
    -- <summary>
    -- 获取指定类型服务
    -- </summary>
    -- <typeparam name="T"></typeparam>
    -- <param name="provider"></param>
    -- <returns>未找到指定类型,则返回null</returns>
    GetService = function (provider, T)
      return provider:GetService(System.typeof(T))
    end
    -- <summary>
    -- Creates a new <see cref="IServiceScope"/> that can be used to resolve scoped services.
    -- </summary>
    -- <param name="provider">The <see cref="IServiceProvider"/> to create the scope from.</param>
    -- <returns>A <see cref="IServiceScope"/> that can be used to resolve scoped services.</returns>
    CreateScope = function (provider)
      return GetRequiredService1(provider, wdmDependencyInjection.IServiceScopeFactory):CreateScope()
    end
    -- <summary>
    -- Get an enumeration of services of type <typeparamref name="T"/> from the <see cref="IServiceProvider"/>.
    -- </summary>
    -- <typeparam name="T">The type of service object to get.</typeparam>
    -- <param name="provider">The <see cref="IServiceProvider"/> to retrieve the services from.</param>
    -- <returns>An enumeration of services of type <typeparamref name="T"/>.</returns>
    GetServices = function (provider, T)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(provider)
      
      return GetRequiredService1(provider, System.IEnumerable_1(T))
    end
    -- <summary>
    -- Get an enumeration of services of type <paramref name="serviceType"/> from the <see cref="IServiceProvider"/>.
    -- </summary>
    -- <param name="provider">The <see cref="IServiceProvider"/> to retrieve the services from.</param>
    -- <param name="serviceType">An object that specifies the type of service object to get.</param>
    -- <returns>An enumeration of services of type <paramref name="serviceType"/>.</returns>
    GetServices1 = function (provider, serviceType)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(provider)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(serviceType)
      
      local genericEnumerable =System.typeof(System.IEnumerable_1):MakeGenericType(serviceType)
      local result =GetRequiredService(provider, genericEnumerable)
      return result
    end
    return {
      GetRequiredService = GetRequiredService,
      GetRequiredService1 = GetRequiredService1,
      GetService = GetService,
      CreateScope = CreateScope,
      GetServices = GetServices,
      GetServices1 = GetServices1,
      __metadata__ = function (out)
        return {
          methods = {
            { "CreateScope", 0x18E, CreateScope, System.IServiceProvider, out.wdm.Extensions.DependencyInjection.IServiceScope },
            { "GetRequiredService", 0x28E, GetRequiredService, System.IServiceProvider, System.Type, System.Object },
            { "GetRequiredService", 0x1018E, GetRequiredService1, function (T) return System.IServiceProvider, T end },
            { "GetService", 0x1018E, GetService, function (T) return System.IServiceProvider, T end },
            { "GetServices", 0x1018E, GetServices, function (T) return System.IServiceProvider, System.IEnumerable_1(T) end },
            { "GetServices", 0x28E, GetServices1, System.IServiceProvider, System.Type, System.IEnumerable_1(System.Object) }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}