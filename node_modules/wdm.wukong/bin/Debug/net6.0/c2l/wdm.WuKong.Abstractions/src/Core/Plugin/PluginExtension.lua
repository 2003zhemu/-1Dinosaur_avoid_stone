-- Generated by CSharp.lua Compiler
local System =_G.System
System.namespace("wdm.WuKong.Abstractions.Core", function (namespace)
  namespace.class("PluginExtension", function (namespace)
    local IsLegal
    -- <summary>
    -- 插件是否合法
    -- </summary>
    IsLegal = function (plugin, owner)
      -- // 没有验证器的话,就直接通过
      -- if (plugin.ValidatorBase == null) return true;
      --
      -- // 使用插件的验证器,传入插件的拥有者作为参数,进行验证
      -- var validation = plugin.Validator.Validate(owner);
      --
      -- return validation.HasError;
      System.throw(System.NotImplementedException())
    end
    return {
      IsLegal = IsLegal,
      __metadata__ = function (out)
        return {
          methods = {
            { "IsLegal", 0x28E, IsLegal, out.wdm.WuKong.Abstractions.Core.IPlugin, out.wdm.WuKong.Abstractions.Core.IPluginOwner, System.Boolean }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}