-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmConfiguration
local wdmToolkitDiagnostics
local __importGlobal =function (out)
  wdmConfiguration = out.wdm.Extensions.Configuration
  wdmToolkitDiagnostics = out.wdm.Toolkit.Diagnostics
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Configuration", function (namespace)
  -- <summary>
  -- Represents a section of application configuration values.
  -- </summary>
  namespace.class("ConfigurationSection", function (namespace)
    local getPath, getKey, getValue, setValue, get, set, GetSection, GetChildren, 
    GetReloadToken, __ctor__
    -- <summary>
    -- Initializes a new instance.
    -- </summary>
    -- <param name="root">The configuration root.</param>
    -- <param name="path">The path to this section.</param>
    __ctor__ = function (this, root, path)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(root)
      wdmToolkitDiagnostics.ThrowHelper.ThrowIfNull(path)
      
      this._root = root
      this._path = path
    end
    getPath = function (this)
      return this._path
    end
    getKey = function (this)
      if this._key == nil then
        this._key = wdmConfiguration.ConfigurationPath.GetSectionKey(this._path)
      end
      
      return this._key
    end
    getValue = function (this)
      return this._root:get(getPath(this))
    end
    setValue = function (this, value)
      this._root:set(getPath(this), value)
    end
    get = function (this, key)
      return this._root:get(wdmConfiguration.ConfigurationPath.Combine(getPath(this), key))
    end
    set = function (this, key, value)
      this._root:set(wdmConfiguration.ConfigurationPath.Combine(getPath(this), key), value)
    end
    -- <summary>
    -- Gets a configuration sub-section with the specified key.
    -- </summary>
    -- <param name="key">The key of the configuration section.</param>
    -- <returns>The <see cref="IConfigurationSection"/>.</returns>
    -- <remarks>
    -- This method will never return <c>null</c>. If no matching sub-section is found with the specified key,
    -- an empty <see cref="IConfigurationSection"/> will be returned.
    -- </remarks>
    GetSection = function (this, key)
      return this._root:GetSection(wdmConfiguration.ConfigurationPath.Combine(getPath(this), key))
    end
    -- <summary>
    -- Gets the immediate descendant configuration sub-sections.
    -- </summary>
    -- <returns>The configuration sub-sections.</returns>
    GetChildren = function (this)
      return wdmConfiguration.InternalConfigurationRootExtensions.GetChildrenImplementation(this._root, getPath(this))
    end
    -- <summary>
    -- Returns a <see cref="IChangeToken"/> that can be used to observe when this configuration is reloaded.
    -- </summary>
    -- <returns>The <see cref="IChangeToken"/>.</returns>
    GetReloadToken = function (this)
      return this._root:GetReloadToken()
    end
    return {
      base = function (out)
        return {
          out.wdm.Extensions.Configuration.IConfigurationSection
        }
      end,
      getPath = getPath,
      getKey = getKey,
      getValue = getValue,
      setValue = setValue,
      get = get,
      set = set,
      GetSection = GetSection,
      GetChildren = GetChildren,
      GetReloadToken = GetReloadToken,
      __ctor__ = __ctor__,
      __metadata__ = function (out)
        return {
          fields = {
            { "_key", 0x1, System.String },
            { "_path", 0x1, System.String },
            { "_root", 0x1, out.wdm.Extensions.Configuration.IConfigurationRoot }
          },
          methods = {
            { ".ctor", 0x206, nil, out.wdm.Extensions.Configuration.IConfigurationRoot, System.String },
            { "GetChildren", 0x86, GetChildren, System.IEnumerable_1(out.wdm.Extensions.Configuration.IConfigurationSection) },
            { "GetReloadToken", 0x86, GetReloadToken, out.wdm.Extensions.Primitives.IChangeToken },
            { "GetSection", 0x186, GetSection, System.String, out.wdm.Extensions.Configuration.IConfigurationSection }
          },
          properties = {
            { "Key", 0x206, System.String, getKey },
            { "Path", 0x206, System.String, getPath },
            { "Value", 0x106, System.String, getValue, setValue }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}