-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmConfiguration
local __importGlobal =function (out)
  wdmConfiguration = out.wdm.Extensions.Configuration
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Configuration", function (namespace)
  -- <summary>
  -- Implements <see cref="IChangeToken"/>
  -- </summary>
  namespace.class("ConfigurationReloadToken", function (namespace)
    local getActiveChangeCallbacks, setActiveChangeCallbacks, getHasChanged, RegisterChangeCallback, OnReload, class, __ctor__
    __ctor__ = function (this)
      this._cts = System.CancellationTokenSource()
    end
    getActiveChangeCallbacks, setActiveChangeCallbacks = System.property("ActiveChangeCallbacks")
    getHasChanged = function (this)
      return this._cts:getIsCancellationRequested()
    end
    -- <summary>
    -- Registers for a callback that will be invoked when the entry has changed. <see cref="IChangeToken.HasChanged"/>
    -- MUST be set before the callback is invoked.
    -- </summary>
    -- <param name="callback">The callback to invoke.</param>
    -- <param name="state">State to be passed into the callback.</param>
    -- <returns>The <see cref="CancellationToken"/> registration.</returns>
    RegisterChangeCallback = function (this, callback, state)
      return wdmConfiguration.ChangeCallbackRegistrar.UnsafeRegisterChangeCallback(callback, state, this._cts:getToken(), function (s)
        setActiveChangeCallbacks(s, false)
      end, this, class)
    end
    -- <summary>
    -- Used to trigger the change token when a reload occurs.
    -- </summary>
    OnReload = function (this)
      this._cts:Cancel()
    end
    class = {
      base = function (out)
        return {
          out.wdm.Extensions.Primitives.IChangeToken
        }
      end,
      ActiveChangeCallbacks = true,
      getActiveChangeCallbacks = getActiveChangeCallbacks,
      setActiveChangeCallbacks = setActiveChangeCallbacks,
      getHasChanged = getHasChanged,
      RegisterChangeCallback = RegisterChangeCallback,
      OnReload = OnReload,
      __ctor__ = __ctor__,
      __metadata__ = function (out)
        return {
          fields = {
            { "_cts", 0x1, System.CancellationTokenSource }
          },
          properties = {
            { "ActiveChangeCallbacks", 0x106, System.Boolean, getActiveChangeCallbacks, setActiveChangeCallbacks },
            { "HasChanged", 0x206, System.Boolean, getHasChanged }
          },
          methods = {
            { "OnReload", 0x6, OnReload },
            { "RegisterChangeCallback", 0x286, RegisterChangeCallback, System.Delegate(System.Object, System.Void), System.Object, System.IDisposable }
          },
          class = { 0x6 }
        }
      end
    }
    return class
  end)
end)
return {}