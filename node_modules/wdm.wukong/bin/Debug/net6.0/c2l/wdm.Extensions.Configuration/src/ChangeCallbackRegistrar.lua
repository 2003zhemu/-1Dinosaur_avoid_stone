-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmConfiguration
local __importGlobal =function (out)
  wdmConfiguration = out.wdm.Extensions.Configuration
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Configuration", function (namespace)
  namespace.class("ChangeCallbackRegistrar", function (namespace)
    local UnsafeRegisterChangeCallback
    -- <summary>
    -- Registers for a callback that will be invoked when the entry has changed. <see cref="Primitives.IChangeToken.HasChanged"/>
    -- MUST be set before the callback is invoked.
    -- </summary>
    -- <param name="callback">The callback to invoke.</param>
    -- <param name="state">State to be passed into the callback.</param>
    -- <param name="token">The <see cref="CancellationToken"/> to invoke the callback with.</param>
    -- <param name="onFailure">The action to execute when an <see cref="ObjectDisposedException"/> is thrown. Should be used to set the IChangeToken's ActiveChangeCallbacks property to false.</param>
    -- <param name="onFailureState">The state to be passed into the <paramref name="onFailure"/> action.</param>
    -- <returns>The <see cref="CancellationToken"/> registration.</returns>
    UnsafeRegisterChangeCallback = function (callback, state, token, onFailure, onFailureState, T)
      local default, extern = System.try(function ()
        return true, token:Register(callback, state)
      end, function (default)
        if System.is(default, System.ObjectDisposedException) then
          onFailure(onFailureState, T)
        else
          return 1, default
        end
      end)
      if default then
        return extern
      end
      return wdmConfiguration.EmptyDisposable.Instance
    end
    return {
      UnsafeRegisterChangeCallback = UnsafeRegisterChangeCallback,
      __metadata__ = function (out)
        return {
          methods = {
            { "UnsafeRegisterChangeCallback", 0x1058C, UnsafeRegisterChangeCallback, function (T) return System.Delegate(System.Object, System.Void), System.Object, System.CancellationToken, System.Delegate(T, System.Void), T, System.IDisposable end }
          },
          class = { 0xC }
        }
      end
    }
  end)
end)
return {}