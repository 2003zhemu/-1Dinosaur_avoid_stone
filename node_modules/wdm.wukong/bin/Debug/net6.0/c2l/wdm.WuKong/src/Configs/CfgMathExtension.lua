-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongConfigs
local __importGlobal =function (out)
  wdmWuKongConfigs = out.wdm.WuKong.Configs
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Configs", function (namespace)
  namespace.class("CfgMathExtension", function (namespace)
    local MathOperate, _calculateFormula, Calculate
    MathOperate = function (mathOperator, left, right)
      repeat
        local default =mathOperator.Symbol
        if default == "+" or default == "加" then
          return left + right
        elseif default == "-" or default == "减" then
          return left - right
        elseif default == "*" or default == "乘" then
          return left * right
        elseif default == "/" or default == "除" then
          return left / right
        elseif default == "零则" or default == "0or" then
          if left == 0 then
            return right
          end
          return left
        else
          System.throw(System.NotImplementedException(System.toString("未支持的数学操作符号:") .. System.toString(mathOperator.Symbol)))
        end
      until 1
    end
    _calculateFormula = function (formula, container, left)
      local right =wdmWuKongConfigs.CfgValueExtension.GetValue(formula.Value, container)
      return MathOperate(formula.MathOperator, left, right)
    end
    Calculate = function (chainFormula, container)
      local result =0
      for _, formula in System.each(chainFormula.Chains) do
        result = _calculateFormula(formula, container, result)
      end
      
      return result
    end
    return {
      MathOperate = MathOperate,
      Calculate = Calculate,
      __metadata__ = function (out)
        return {
          methods = {
            { "Calculate", 0x28E, Calculate, out.wdm.WuKong.Configs.Core.CfgChainMathFormula, out.wdm.WuKong.Abstractions.Container.IContainer, System.Single },
            { "MathOperate", 0x38E, MathOperate, out.wdm.WuKong.Configs.Core.CfgMathOperator, System.Single, System.Single, System.Single },
            { "_calculateFormula", 0x389, _calculateFormula, out.wdm.WuKong.Configs.Core.CfgMathFormula, out.wdm.WuKong.Abstractions.Container.IContainer, System.Single, System.Single }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}