-- Generated by CSharp.lua Compiler
local System =_G.System
System.namespace("wdm.WuKong.Core", function (namespace)
  namespace.class("RandomHelper", function (namespace)
    local RollCheck, Roll, Roll1
    -- <summary>
    -- 传入一个0到100之间的数字
    -- 必须大于等于0,小于等于100
    -- 传0, 代表永远不可能发生
    -- 传100, 代表100%发生
    -- 也就是说,传入的数字等于发生概率
    -- 
    -- 程序将同样在该范围内进行随机,如果随机数小于等于传入参数,则返回 true
    -- </summary>
    -- <param name="number"></param>
    -- <returns></returns>
    RollCheck = function (number)
      local random =System.Random()
      local result =random:Next(1, 101)
      return (result <= number)
    end
    -- <summary>
    -- 在指定范围内获取随机整数
    -- 包括0
    -- </summary>
    Roll = function (max)
      local random =System.Random()
      local result =random:Next(max)
      return result
    end
    -- <summary>
    -- 在指定范围内获取随机整数
    -- 大于等于最小值,小于最大值
    -- </summary>
    Roll1 = function (min, max)
      local random =System.Random()
      local result =random:Next(min, max)
      return result
    end
    return {
      RollCheck = RollCheck,
      Roll = Roll,
      Roll1 = Roll1,
      __metadata__ = function (out)
        return {
          methods = {
            { "Roll", 0x18E, Roll, System.Int32, System.Int32 },
            { "Roll", 0x28E, Roll1, System.Int32, System.Int32, System.Int32 },
            { "RollCheck", 0x18E, RollCheck, System.Int32, System.Boolean }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}