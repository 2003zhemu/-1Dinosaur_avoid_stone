-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongLuaHelper
local __importGlobal =function (out)
  wdmWuKongLuaHelper = out.wdm.WuKong.LuaHelper
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Vendor", function (namespace)
  -- <summary>
  -- 商人钩子插件
  -- </summary>
  namespace.class("VendorHooksPlugin", function (namespace)
    local _buildConext, HookExtraLogic, HookReplaceCost, HookReplaceReward, HookPurchased
    _buildConext = function (this, slot)
      local context =System.Object()
      for _, pair in System.each(slot:getContext()) do
        local key = pair[1]
        local value = pair[2].Handler()
        context[key] = value
      end
      return context
    end
    -- <summary>
    -- 钩子,额外逻辑验证, 将在验证完普通逻辑后执行
    -- 需要目标模块提供函数 "HookExtraLogic"
    -- 返回true, 说明验证通过
    -- 返回false, 说明验证不通过
    -- </summary>
    HookExtraLogic = function (this, slot, target, accessorySlots, multipleCount, args)
      if not wdmWuKongLuaHelper.LuaExecutor.HasMethod(this.Cfg.ModulePath, "HookExtraLogic") then
        return nil
      end
      
      local context =_buildConext(this, slot)
      
      return wdmWuKongLuaHelper.LuaExecutor.TryExecuteLua(slot, this.Cfg.ModulePath, "HookExtraLogic", context, target, accessorySlots, args)
    end
    -- <summary>
    -- 钩子,替换消耗函数
    -- 需要目标模块提供函数 "HookReplaceCost"
    -- 如果返回null,则不替换
    -- </summary>
    HookReplaceCost = function (this, slot, target, accessorySlots, multipleCount, args, purchaseResult)
      if not wdmWuKongLuaHelper.LuaExecutor.HasMethod(this.Cfg.ModulePath, "HookReplaceCost") then
        return nil
      end
      
      local context =_buildConext(this, slot)
      
      return wdmWuKongLuaHelper.LuaExecutor.TryExecuteLua(slot, this.Cfg.ModulePath, "HookReplaceCost", context, target, accessorySlots, args, purchaseResult)
    end
    -- <summary>
    -- 钩子,替换奖励函数
    -- 需要目标模块提供函数 "HookReplaceReward"
    -- 如果返回null,则不替换
    -- </summary>
    HookReplaceReward = function (this, slot, target, accessorySlots, multipleCount, args)
      if not wdmWuKongLuaHelper.LuaExecutor.HasMethod(this.Cfg.ModulePath, "HookReplaceReward") then
        return nil
      end
      
      local context =_buildConext(this, slot)
      
      return wdmWuKongLuaHelper.LuaExecutor.TryExecuteLua(slot, this.Cfg.ModulePath, "HookReplaceReward", context, target, accessorySlots, args)
    end
    -- <summary>
    -- 钩子,在购买后执行
    -- 需要目标模块提供函数 "HookPurchased"
    -- 如果返回null,则不替换
    -- </summary>
    HookPurchased = function (this, slot, target, accessorySlots, multipleCount, args, purchaseResult)
      local context =_buildConext(this, slot)
      
      wdmWuKongLuaHelper.LuaExecutor.TryExecuteLua(slot, this.Cfg.ModulePath, "HookPurchased", context, target, accessorySlots, args, purchaseResult)
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.Configs.OperatorBase_1(out.wdm.WuKong.Configs.Vender.CfgVendorHooks)
        }
      end,
      HookExtraLogic = HookExtraLogic,
      HookReplaceCost = HookReplaceCost,
      HookReplaceReward = HookReplaceReward,
      HookPurchased = HookPurchased,
      __metadata__ = function (out)
        return {
          methods = {
            { "HookExtraLogic", 0x586, HookExtraLogic, out.wdm.WuKong.Container.Slot, out.wdm.WuKong.Container.Slot, System.Array(out.wdm.WuKong.Container.Slot), System.Int64, System.Array(System.Object), System.Object },
            { "HookPurchased", 0x606, HookPurchased, out.wdm.WuKong.Container.Slot, out.wdm.WuKong.Container.Slot, System.Array(out.wdm.WuKong.Container.Slot), System.Int64, System.Array(System.Object), out.wdm.WuKong.Vendor.PurchaseResult },
            { "HookReplaceCost", 0x686, HookReplaceCost, out.wdm.WuKong.Container.Slot, out.wdm.WuKong.Container.Slot, System.Array(out.wdm.WuKong.Container.Slot), System.Int64, System.Array(System.Object), out.wdm.WuKong.Vendor.PurchaseResult, System.Object },
            { "HookReplaceReward", 0x586, HookReplaceReward, out.wdm.WuKong.Container.Slot, out.wdm.WuKong.Container.Slot, System.Array(out.wdm.WuKong.Container.Slot), System.Int64, System.Array(System.Object), System.Object },
            { "_buildConext", 0x181, _buildConext, out.wdm.WuKong.Container.Slot, System.Object }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}