-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
local wdmWuKongAbstractions
local wdmWuKongConfigsCore
local wdmWuKongConfigsVendor
local wdmWuKongCore
local wdmWuKongVendor
local ArrayIndexCost
local ArrayIndexLogic
local ArrayIndexProduct
local ArrayCfgActionBase
local __importGlobal =function (out)
  wdmWuKongAbstractions = out.wdm.WuKong.Abstractions
  wdmWuKongConfigsCore = out.wdm.WuKong.Configs.Core
  wdmWuKongConfigsVendor = out.wdm.WuKong.Configs.Vendor
  wdmWuKongCore = out.wdm.WuKong.Core
  wdmWuKongVendor = out.wdm.WuKong.Vendor
  ArrayIndexCost = System.Array(wdmWuKongConfigsVendor.IndexCost)
  ArrayIndexLogic = System.Array(wdmWuKongConfigsVendor.IndexLogic)
  ArrayIndexProduct = System.Array(wdmWuKongConfigsVendor.IndexProduct)
  ArrayCfgActionBase = System.Array(wdmWuKongConfigsCore.CfgActionBase)
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Vendor", function (namespace)
  -- <summary>
  -- 当商人为普通,或者进度商人时,分别返回不同的成员
  -- </summary>
  namespace.class("VendorPluginProgressHelper", function (namespace)
    local _assertGroupLegal, _assertLegal, AssertLegalIfProgressVendor, GetIndexLogics, GetIndexProducts, GetIndexCosts, GetPostActions
    _assertGroupLegal = function (e, id, T)
      if e ~= nil and Linq.Count(e) ~= 0 then
        System.throw(System.new(wdmWuKongAbstractions.WuKongException, 2, System.toString(System.toString("不得为进度商人`") .. System.toString(id)) .. System.toString("`的分组配置任何组件")))
      end
    end
    _assertLegal = function (id, ...)
      local var= {...} 
      local arr=System.arrayFromTable(var,System.Object)
      local count =0
      for _, e in System.each(arr) do
        if e ~= nil and Linq.Count(e) ~= 0 then
          if count == 0 then
            count = Linq.Count(e)
          else
            if count ~= Linq.Count(e) then
              System.throw(System.new(wdmWuKongAbstractions.WuKongException, 2, System.toString(System.toString("进度商人`") .. System.toString(id)) .. System.toString("`的每个数组成员的数量,要求为零或者相等")))
            end
          end
        end
      end
    end
    -- <summary>
    -- 如果是进度商人,确保合法性
    -- </summary>
    AssertLegalIfProgressVendor = function (slot)
      local _, progressVendorPlugin = wdmWuKongCore.PluginsExtension.TryGetPlugin(slot, nil, wdmWuKongVendor.ProgressVendorPlugin)
      if progressVendorPlugin == nil then
        return
      end
      
      local vendorPlugin =wdmWuKongCore.PluginsExtension.GetPlugin(slot, wdmWuKongVendor.VendorPlugin)
      
      -- 确保分组未配置
      _assertGroupLegal(vendorPlugin.Cfg.Group_Ref.Logics, slot:getId(), wdmWuKongConfigsVendor.IndexLogic)
      _assertGroupLegal(vendorPlugin.Cfg.Group_Ref.PostActions, slot:getId(), wdmWuKongConfigsCore.CfgActionBase)
      _assertGroupLegal(vendorPlugin.Cfg.Group_Ref.Products, slot:getId(), wdmWuKongConfigsVendor.IndexProduct)
      _assertGroupLegal(vendorPlugin.Cfg.Group_Ref.Costs, slot:getId(), wdmWuKongConfigsVendor.IndexCost)
      
      -- 确保组件数量相等
      local cfg =vendorPlugin.Cfg
      _assertLegal(slot:getId(), cfg.Costs, cfg.Products, cfg.Logics, cfg.PostActions)
    end
    -- <summary>
    -- 获取所有产品或下个进度所需逻辑
    -- </summary>
    GetIndexLogics = function (indexLogics, slot)
      local arr =indexLogics
      if arr == nil or #arr == 0 then
        return arr
      end
      local _, plugin = wdmWuKongCore.PluginsExtension.TryGetPlugin(slot, nil, wdmWuKongVendor.ProgressVendorPlugin)
      if plugin == nil then
        return arr
      end
      local curProgress =plugin:GetCurrentProgress(slot)
      if curProgress >= #arr then
        System.throw(System.new(wdmWuKongAbstractions.WuKongException, 2, "在进度商品中,当前进度大于逻辑数量"))
      end
      return ArrayIndexLogic(1, { arr:get(curProgress) })
    end
    -- <summary>
    -- 获取所有产品或下个进度所需产品
    -- </summary>
    GetIndexProducts = function (indexProducts, slot)
      local arr =indexProducts
      if arr == nil or #arr == 0 then
        return arr
      end
      local _, plugin = wdmWuKongCore.PluginsExtension.TryGetPlugin(slot, nil, wdmWuKongVendor.ProgressVendorPlugin)
      if plugin == nil then
        return arr
      end
      local curProgress =plugin:GetCurrentProgress(slot)
      if curProgress >= #arr then
        System.throw(System.new(wdmWuKongAbstractions.WuKongException, 2, "在进度商品中,当前进度大于产品数量"))
      end
      return ArrayIndexProduct(1, { arr:get(curProgress) })
    end
    -- <summary>
    -- 获取所有产品或下个进度所需定价
    -- </summary>
    GetIndexCosts = function (indexCosts, slot)
      local arr =indexCosts
      if arr == nil or #arr == 0 then
        return arr
      end
      local _, plugin = wdmWuKongCore.PluginsExtension.TryGetPlugin(slot, nil, wdmWuKongVendor.ProgressVendorPlugin)
      if plugin == nil then
        return arr
      end
      local curProgress =plugin:GetCurrentProgress(slot)
      if curProgress >= #arr then
        System.throw(System.new(wdmWuKongAbstractions.WuKongException, 2, "在进度商品中,当前进度大于定价数量"))
      end
      return ArrayIndexCost(1, { arr:get(curProgress) })
    end
    -- <summary>
    -- 获取所有产品或下个进度所需后置行动
    -- </summary>
    GetPostActions = function (postActions, slot)
      local arr =postActions
      if arr == nil or #arr == 0 then
        return arr
      end
      local _, plugin = wdmWuKongCore.PluginsExtension.TryGetPlugin(slot, nil, wdmWuKongVendor.ProgressVendorPlugin)
      if plugin == nil then
        return arr
      end
      local curProgress =plugin:GetCurrentProgress(slot)
      if curProgress >= #arr then
        System.throw(System.new(wdmWuKongAbstractions.WuKongException, 2, "在进度商品中,当前进度大于后续行动数量"))
      end
      return ArrayCfgActionBase(1, { arr:get(curProgress) })
    end
    return {
      AssertLegalIfProgressVendor = AssertLegalIfProgressVendor,
      GetIndexLogics = GetIndexLogics,
      GetIndexProducts = GetIndexProducts,
      GetIndexCosts = GetIndexCosts,
      GetPostActions = GetPostActions,
      __metadata__ = function (out)
        return {
          methods = {
            { "AssertLegalIfProgressVendor", 0x10E, AssertLegalIfProgressVendor, out.wdm.WuKong.Container.Slot },
            { "GetIndexCosts", 0x28E, GetIndexCosts, System.Array(out.wdm.WuKong.Configs.Vendor.IndexCost), out.wdm.WuKong.Container.Slot, System.Array(out.wdm.WuKong.Configs.Vendor.IndexCost) },
            { "GetIndexLogics", 0x28E, GetIndexLogics, System.Array(out.wdm.WuKong.Configs.Vendor.IndexLogic), out.wdm.WuKong.Container.Slot, System.Array(out.wdm.WuKong.Configs.Vendor.IndexLogic) },
            { "GetIndexProducts", 0x28E, GetIndexProducts, System.Array(out.wdm.WuKong.Configs.Vendor.IndexProduct), out.wdm.WuKong.Container.Slot, System.Array(out.wdm.WuKong.Configs.Vendor.IndexProduct) },
            { "GetPostActions", 0x28E, GetPostActions, System.Array(out.wdm.WuKong.Configs.Core.CfgActionBase), out.wdm.WuKong.Container.Slot, System.Array(out.wdm.WuKong.Configs.Core.CfgActionBase) },
            { "_assertGroupLegal", 0x10209, _assertGroupLegal, function (T) return System.IEnumerable_1(T), System.String end },
            { "_assertLegal", 0x209, _assertLegal, System.String, System.Array(System.IEnumerable_1(out.wdm.WuKong.Configs.BeanBase)) }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}