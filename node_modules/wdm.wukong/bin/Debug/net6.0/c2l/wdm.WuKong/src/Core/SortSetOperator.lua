-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
local wdmWuKongAbstractions
local __importGlobal =function (out)
  wdmWuKongAbstractions = out.wdm.WuKong.Abstractions
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Core", function (namespace)
  namespace.class("SortSetOperator", function (namespace)
    local CanAddOne, GetPropertiesCustomOptionByValue, HasPropertiesOption, GetItemsOption, HasItemOption, GetCustomOptionByValue, GetAllValuesOfCustomOption, HasCustomOption, 
    IsValueInRange, GetValueConfigByValue, GetValueConfig, GetValue, AddValue, DeductValue, SetValue, IsLast, 
    IsFirst, GetFirst, GetLast, GetAll, __ctor__
    __ctor__ = function (this, cfg)
      this._cfg = cfg
      
      this._allValues = Linq.ToArray(Linq.OrderBy(Linq.Select(this._cfg.Values, function (x)
        return x.Value
      end, System.Int32), function (x)
        return x
      end, nil, System.Int32))
    end
    -- <summary>
    -- 能否加一
    -- </summary>
    CanAddOne = function (this, slot)
      local cur =GetValue(this, slot)
      return IsValueInRange(this, cur + 1)
    end
    -- <summary>
    -- 获取属性参数
    -- </summary>
    GetPropertiesCustomOptionByValue = function (this, optionName, index)
      local member =GetValueConfigByValue(this, index)
      local default, value = member.Properties:TryGetValue(optionName, nil)
      if not default then
        System.throw(System.ArgumentException(System.toString("未找到属性参数:") .. System.toString(optionName)))
      else
        return value
      end
    end
    -- <summary>
    -- 是否包含属性参数
    -- </summary>
    HasPropertiesOption = function (this, optionName, index)
      local member =GetValueConfigByValue(this, index)
      return member.Properties:ContainsKey(optionName)
    end
    -- <summary>
    -- 获取物品参数
    -- </summary>
    GetItemsOption = function (this, optionName, index)
      local member =GetValueConfigByValue(this, index)
      local default, value = member.Items:TryGetValue(optionName, nil)
      if not default then
        System.throw(System.ArgumentException(System.toString("未找到物品参数:") .. System.toString(optionName)))
      else
        return value
      end
    end
    -- <summary>
    -- 是否包含物品参数
    -- </summary>
    HasItemOption = function (this, optionName, index)
      local member =GetValueConfigByValue(this, index)
      return member.Items:ContainsKey(optionName)
    end
    -- <summary>
    -- 获取自定义参数
    -- </summary>
    -- <returns>
    -- 返回当前有序集值
    -- </returns>
    GetCustomOptionByValue = function (this, optionName, index)
      local member =GetValueConfigByValue(this, index)
      local default, value = member.CustomValues:TryGetValue(optionName, nil)
      if not default then
        System.throw(System.ArgumentException(System.toString("未找到自定义参数:") .. System.toString(optionName)))
      else
        return value
      end
    end
    -- <summary>
    -- 获取指定自定义参数的所有值
    -- </summary>
    GetAllValuesOfCustomOption = function (this, optionName)
      return Linq.ToArray(Linq.Select(this._cfg.Values, function (x)
        local default, value = x.CustomValues:TryGetValue(optionName, nil)
        if default then
          return value
        end
        System.throw(System.ArgumentException(System.toString(System.toString(System.toString(System.toString(System.toString("指定有续集成员 ") .. System.toString(this._cfg.Name)) .. System.toString("#")) .. System.toString(x.Value)) .. System.toString(",未包含自定义参数 ")) .. System.toString(optionName)))
      end, System.Int64))
    end
    -- <summary>
    -- 是否包含自定义参数
    -- </summary>
    HasCustomOption = function (this, optionName, index)
      local member =GetValueConfigByValue(this, index)
      return member.CustomValues:ContainsKey(optionName)
    end
    -- <summary>
    -- 给定值在不在有序集范围内
    -- </summary>
    IsValueInRange = function (this, value)
      return value >= GetFirst(this) and value <= GetLast(this)
    end
    -- <summary>
    -- 根据值获取对应有序集配置
    -- </summary>
    GetValueConfigByValue = function (this, value)
      if not IsValueInRange(this, value) then
        System.throw(wdmWuKongAbstractions.OutOfSortSetRangeException(this._cfg.Id, value))
      end
      return Linq.First(this._cfg.Values, function (x)
        return x.Value == value
      end)
    end
    -- <summary>
    -- 获取当前有序集值
    -- </summary>
    -- <returns>
    -- 返回当前有序集值
    -- </returns>
    GetValueConfig = function (this, slot)
      local cur =GetValue(this, slot)
      return GetValueConfigByValue(this, cur)
    end
    -- <summary>
    -- 获取当前有序集值
    -- </summary>
    -- <returns>
    -- 返回当前有序集值
    -- </returns>
    GetValue = function (this, slot)
      local cur =slot:getElement():getCount()
      if not IsValueInRange(this, cur) then
        System.throw(wdmWuKongAbstractions.OutOfSortSetRangeException(this._cfg.Id, cur))
      end
      return cur
    end
    -- <summary>
    -- 增加有序集值
    -- </summary>
    -- <returns>
    -- 返回变更后的值
    -- </returns>
    AddValue = function (this, slot, value)
      local cur =GetValue(this, slot)
      local result =value + cur
      if not IsValueInRange(this, result) then
        System.throw(wdmWuKongAbstractions.OutOfSortSetRangeException(this._cfg.Id, value))
      end
      return SetValue(this, slot, result)
    end
    -- <summary>
    -- 减少有序集值
    -- </summary>
    -- <returns>
    -- 返回变更后的值
    -- </returns>
    DeductValue = function (this, slot, value)
      local cur =GetValue(this, slot)
      local result =cur - value
      if not IsValueInRange(this, result) then
        System.throw(wdmWuKongAbstractions.OutOfSortSetRangeException(this._cfg.Id, value))
      end
      return SetValue(this, slot, result)
    end
    -- <summary>
    -- 设置有序集值
    -- </summary>
    -- <returns>
    -- 返回变更后的值
    -- </returns>
    SetValue = function (this, slot, value)
      if not IsValueInRange(this, value) then
        System.throw(wdmWuKongAbstractions.OutOfSortSetRangeException(this._cfg.Id, value))
      end
      slot:getElement():setCount(value)
      return value
    end
    IsLast = function (this, value)
      if not IsValueInRange(this, value) then
        System.throw(wdmWuKongAbstractions.OutOfSortSetRangeException(this._cfg.Id, value))
      end
      return value > this._cfg.EndIndex
    end
    IsFirst = function (this, value)
      if not IsValueInRange(this, value) then
        System.throw(wdmWuKongAbstractions.OutOfSortSetRangeException(this._cfg.Id, value))
      end
      return value > this._cfg.StartIndex
    end
    GetFirst = function (this)
      return this._cfg.StartIndex
    end
    GetLast = function (this)
      return this._cfg.EndIndex
    end
    GetAll = function (this)
      return this._allValues
    end
    return {
      CanAddOne = CanAddOne,
      GetPropertiesCustomOptionByValue = GetPropertiesCustomOptionByValue,
      HasPropertiesOption = HasPropertiesOption,
      GetItemsOption = GetItemsOption,
      HasItemOption = HasItemOption,
      GetCustomOptionByValue = GetCustomOptionByValue,
      GetAllValuesOfCustomOption = GetAllValuesOfCustomOption,
      HasCustomOption = HasCustomOption,
      IsValueInRange = IsValueInRange,
      GetValueConfigByValue = GetValueConfigByValue,
      GetValueConfig = GetValueConfig,
      GetValue = GetValue,
      AddValue = AddValue,
      DeductValue = DeductValue,
      SetValue = SetValue,
      IsLast = IsLast,
      IsFirst = IsFirst,
      GetFirst = GetFirst,
      GetLast = GetLast,
      GetAll = GetAll,
      __ctor__ = __ctor__,
      __metadata__ = function (out)
        return {
          fields = {
            { "_allValues", 0x1, System.Array(System.Int32) },
            { "_cfg", 0x1, out.wdm.WuKong.Configs.Core.CfgSortSet }
          },
          methods = {
            { ".ctor", 0x106, nil, out.wdm.WuKong.Configs.Core.CfgSortSet },
            { "AddValue", 0x286, AddValue, out.wdm.WuKong.Container.Slot, System.Int64, System.Int64 },
            { "CanAddOne", 0x186, CanAddOne, out.wdm.WuKong.Container.Slot, System.Boolean },
            { "DeductValue", 0x286, DeductValue, out.wdm.WuKong.Container.Slot, System.Int64, System.Int64 },
            { "GetAll", 0x86, GetAll, System.Array(System.Int32) },
            { "GetAllValuesOfCustomOption", 0x186, GetAllValuesOfCustomOption, System.String, System.Array(System.Int64) },
            { "GetCustomOptionByValue", 0x286, GetCustomOptionByValue, System.String, System.Int32, System.Int64 },
            { "GetFirst", 0x86, GetFirst, System.Int32 },
            { "GetItemsOption", 0x286, GetItemsOption, System.String, System.Int32, out.wdm.WuKong.Configs.Item.CfgItemStackGroupBase },
            { "GetLast", 0x86, GetLast, System.Int32 },
            { "GetPropertiesCustomOptionByValue", 0x286, GetPropertiesCustomOptionByValue, System.String, System.Int32, out.wdm.WuKong.Configs.Property.CfgPropertyStackGroupBase },
            { "GetValue", 0x186, GetValue, out.wdm.WuKong.Container.Slot, System.Int32 },
            { "GetValueConfig", 0x186, GetValueConfig, out.wdm.WuKong.Container.Slot, out.wdm.WuKong.Configs.Core.SortSetMember },
            { "GetValueConfigByValue", 0x186, GetValueConfigByValue, System.Int64, out.wdm.WuKong.Configs.Core.SortSetMember },
            { "HasCustomOption", 0x286, HasCustomOption, System.String, System.Int32, System.Boolean },
            { "HasItemOption", 0x286, HasItemOption, System.String, System.Int32, System.Boolean },
            { "HasPropertiesOption", 0x286, HasPropertiesOption, System.String, System.Int32, System.Boolean },
            { "IsFirst", 0x186, IsFirst, System.Int64, System.Boolean },
            { "IsLast", 0x186, IsLast, System.Int64, System.Boolean },
            { "IsValueInRange", 0x186, IsValueInRange, System.Int64, System.Boolean },
            { "SetValue", 0x286, SetValue, out.wdm.WuKong.Container.Slot, System.Int64, System.Int64 }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}