-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongConfigsCore
local __importGlobal =function (out)
  wdmWuKongConfigsCore = out.wdm.WuKong.Configs.Core
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Configs", function (namespace)
  namespace.class("EComparerExtension", function (namespace)
    local Compare
    Compare = function (comparer, left, right)
      repeat
        local default =comparer
        if default == wdmWuKongConfigsCore.EComparer.Bigger then
          return left > right
        elseif default == wdmWuKongConfigsCore.EComparer.BiggerEqual then
          return left >= right
        elseif default == wdmWuKongConfigsCore.EComparer.Smaller then
          return left < right
        elseif default == wdmWuKongConfigsCore.EComparer.SmallerEqual then
          return left <= right
        elseif default == wdmWuKongConfigsCore.EComparer.Equal or default == wdmWuKongConfigsCore.EComparer.Equal2 then
          return left == right
        elseif default == wdmWuKongConfigsCore.EComparer.NotEqual or default == wdmWuKongConfigsCore.EComparer.NotEqual2 then
          return left ~= right
        else
          System.throw(System.ArgumentOutOfRangeException("comparer" --[[nameof(comparer)]], "未知的比较器类型"))
        end
      until 1
    end
    return {
      Compare = Compare,
      __metadata__ = function (out)
        return {
          methods = {
            { "Compare", 0x38E, Compare, out.wdm.WuKong.Configs.Core.EComparer, System.Int64, System.Int64, System.Boolean }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}