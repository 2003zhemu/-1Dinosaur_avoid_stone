-- Generated by CSharp.lua Compiler
local System =_G.System
local ArrayString =System.Array(System.String)
local wdmWuKongAbstractionsCore
local wdmWuKongContainer
local wdmWuKongCore
local wdmWuKongItem
local wdmWuKongVendor
local __importGlobal =function (out)
  wdmWuKongAbstractionsCore = out.wdm.WuKong.Abstractions.Core
  wdmWuKongContainer = out.wdm.WuKong.Container
  wdmWuKongCore = out.wdm.WuKong.Core
  wdmWuKongItem = out.wdm.WuKong.Item
  wdmWuKongVendor = out.wdm.WuKong.Vendor
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Vendor", function (namespace)
  namespace.class("CostAllAccessoryPlugin", function (namespace)
    local Validate, Cost
    -- <summary>
    -- 一定成功
    -- </summary>
    Validate = function (this, container, extra)
      local payload =System.as(extra, wdmWuKongVendor.AccessoryPayload)
      assert(payload ~= nil)
      local target =payload.Target
      local accessory =payload.Accessory
      
      if accessory == nil then
        System.throw(System.ArgumentNullException("必须提供附属物"))
      end
      
      for _, slot in System.each(accessory) do
        if slot:getElement() == nil then
          System.throw(System.Exception(System.toString("提供的附属物为空:") .. System.toString(slot:getId())))
        end
      end
      
      -- 附属物中,不能包含标的物
      for _, slot in System.each(accessory) do
        if slot == target then
          wdmWuKongAbstractionsCore.Validation.Fail("消耗附属物验证", "附属物中包含标的物")
        end
      end
      
      return wdmWuKongAbstractionsCore.Validation.Pass("消耗附属物验证")
    end
    Cost = function (this, container, extra)
      Validate(this, container, extra):AssertPass()
      local payload =System.as(extra, wdmWuKongVendor.AccessoryPayload)
      assert(payload ~= nil)
      local accessory =payload.Accessory
      
      local result =System.new(wdmWuKongVendor.CostResult, 3, "消耗所有附属物", ArrayString { "消耗所有附属物" }, 1)
      for _, slot in System.each(accessory) do
        local ec =slot:getParent()
        local plugin =wdmWuKongCore.PluginsExtension.GetPlugin(ec, wdmWuKongItem.IItemSlotProviderPlugin)
        plugin:ClearSlots(ec, ArrayString(1, { slot:getId() }))
      end
      return result
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.Configs.OperatorBase_1(out.wdm.WuKong.Configs.Vendor.CfgCostAllAccessory),
          out.wdm.WuKong.Vendor.IAccessoryCostPlugin
        }
      end,
      Validate = Validate,
      Cost = Cost,
      __metadata__ = function (out)
        return {
          methods = {
            { "Cost", 0x286, Cost, out.wdm.WuKong.Abstractions.Container.IContainer, System.Object, out.wdm.WuKong.Vendor.CostResult },
            { "Validate", 0x286, Validate, out.wdm.WuKong.Abstractions.Container.IContainer, System.Object, out.wdm.WuKong.Abstractions.Core.Validation }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}