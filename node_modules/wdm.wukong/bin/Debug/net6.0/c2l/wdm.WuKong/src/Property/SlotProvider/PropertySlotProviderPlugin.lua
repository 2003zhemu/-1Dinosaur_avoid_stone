-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
local wdmContainer
local wdmWuKongConfigs
local wdmWuKongConfigsContainer
local wdmWuKongContainer
local __importGlobal =function (out)
  wdmContainer = out.wdm.WuKong.Abstractions.Container
  wdmWuKongConfigs = out.wdm.WuKong.Configs
  wdmWuKongConfigsContainer = out.wdm.WuKong.Configs.Container
  wdmWuKongContainer = out.wdm.WuKong.Container
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Property", function (namespace)
  -- <summary>
  -- 属性格子提供者
  -- </summary>
  namespace.class("PropertySlotProviderPlugin", function (namespace)
    local BuildSlots
    BuildSlots = function (this, container, tables)
      local ec =System.as(container, wdmWuKongContainer.ElementContainer)
      assert(ec ~= nil)
      local properties =Linq.ToArray(Linq.Where(tables.TbProperty:getDataList(), function (x)
        return this.Cfg.PropertyGroups:Contains(x.Group)
      end))
      if Linq.Count(properties) > 1024 --[[Consts.MaxSlotCountPerContainer]] then
        System.throw(wdmContainer.MaxSlotsCountException())
      end
      return Linq.Select(properties, function (x)
        local slot =System.new(wdmWuKongContainer.Slot, 2, x.Id, ec)
        slot:setOriginConfig(x.OriginConfig)
        slot:setElement(this:BuildElementInSlot(x.Id, tables))
        assert(ec:getConfig() ~= nil, "元素容器的配置为空")
        local cfgElementContainer =wdmWuKongConfigs.BeanBaseExtension.CfgAssertAs(ec:getConfig().Type, wdmWuKongConfigsContainer.CfgElementContainerType)
        slot:setPlugins(wdmWuKongContainer.CachedPluginsProviderExtension.BuildPlugins(slot, function ()
          return this:BuildPlugins(x, tables, System.toLuaTuple(cfgElementContainer.SlotPlugins))
        end))
        slot:setIsActive(true)
        return slot
      end, wdmWuKongContainer.Slot)
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.Property.PropertySlotProviderBase_1(out.wdm.WuKong.Configs.Property.CfgPropertySlotProviderPlugin),
          out.wdm.WuKong.Property.IPropertySlotProvider
        }
      end,
      BuildSlots = BuildSlots,
      __metadata__ = function (out)
        return {
          methods = {
            { "BuildSlots", 0x286, BuildSlots, out.wdm.WuKong.Abstractions.Container.IContainer, out.wdm.WuKong.Configs.Tables, System.IEnumerable_1(out.wdm.WuKong.Container.Slot) }
          },
          class = { 0x6, out.wdm.WuKong.Abstractions.Core.PluginAttribute("属性格子提供者"), out.wdm.WuKong.Abstractions.Core.PluginDescriptionAttribute("为属性容器提供格子,将根据配置中的属性分组,为分组中每个元素创建对应的格子"), out.wdm.WuKong.Abstractions.Core.PluginTagAttribute("容器", "属性", "格子提供者") }
        }
      end
    }
  end)
end)
return {}