-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
System.namespace("wdm.WuKong.Configs", function (namespace)
  namespace.class("CfgRouteExtension", function (namespace)
    local IsPass, IsPatternMatch
    IsPass = function (patterns, text)
      local isPass =false
      -- 先判断白名单
      for _, pattern in System.each(Linq.Where(patterns:getDataList(), function (x)
        return x.IsAllow
      end)) do
        if IsPatternMatch(pattern, text) then
          isPass = true
          break
        end
      end
      
      -- 再判断黑名单
      if isPass then
        for _, pattern in System.each(Linq.Where(patterns:getDataList(), function (x)
          return not x.IsAllow
        end)) do
          if IsPatternMatch(pattern, text) then
            isPass = false
            break
          end
        end
      end
      
      
      return isPass
    end
    IsPatternMatch = function (pattern, text)
      local result =true
      
      
      local match = string.match(text,pattern.Pattern)
      result = match ~= nil
      
      return result
    end
    return {
      IsPass = IsPass,
      IsPatternMatch = IsPatternMatch,
      __metadata__ = function (out)
        return {
          methods = {
            { "IsPass", 0x28E, IsPass, out.wdm.WuKong.Configs.Route.TbPattern, System.String, System.Boolean },
            { "IsPatternMatch", 0x28E, IsPatternMatch, out.wdm.WuKong.Configs.Route.CfgPattern, System.String, System.Boolean }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}