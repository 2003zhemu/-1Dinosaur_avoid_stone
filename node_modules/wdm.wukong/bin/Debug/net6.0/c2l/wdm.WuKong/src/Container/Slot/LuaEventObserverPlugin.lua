-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongAbstractionsCore
local wdmWuKongContainer
local __importGlobal =function (out)
  wdmWuKongAbstractionsCore = out.wdm.WuKong.Abstractions.Core
  wdmWuKongContainer = out.wdm.WuKong.Container
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Container", function (namespace)
  -- <summary>
  -- Lua事件监听插件
  -- 可以挂载在容器,slot上
  -- </summary>
  namespace.class("LuaEventObserverPlugin", function (namespace)
    local GetCount, ResetCount, _addElementCount, _addPluginCount, OnEvent
    GetCount = function (this, container)
      local isElementCount =this.Cfg.UseElementCount
      if isElementCount then
        local slot =wdmWuKongContainer.ContainerExtension.AssertAsSlot(container)
        return slot:getElement():getCount()
      else
        local _, count = wdmWuKongAbstractionsCore.PluginDataExtension.TryGetPluginValue1(container, "LEOC" --[[LuaEventObserverPlugin.CountKey]], nil, System.Int64)
        return count
      end
    end
    ResetCount = function (this, container)
      local isElementCount =this.Cfg.UseElementCount
      if isElementCount then
        local slot =wdmWuKongContainer.ContainerExtension.AssertAsSlot(container)
        slot:getElement():setCount(0)
      else
        wdmWuKongAbstractionsCore.PluginDataExtension.SetPluginValue(container, "LEOC" --[[LuaEventObserverPlugin.CountKey]], 0 --[[(long)0]], System.Int64)
      end
    end
    _addElementCount = function (this, container, count)
      local slot =wdmWuKongContainer.ContainerExtension.AssertAsSlot(container)
      slot:getElement():setCount(slot:getElement():getCount() + count)
    end
    _addPluginCount = function (this, container, count)
      local _, origin = wdmWuKongAbstractionsCore.PluginDataExtension.TryGetPluginValue1(container, "LEOC" --[[LuaEventObserverPlugin.CountKey]], nil, System.Int64)
      origin = origin + count
      wdmWuKongAbstractionsCore.PluginDataExtension.SetPluginValue(container, "LEOC" --[[LuaEventObserverPlugin.CountKey]], origin, System.Int64)
    end
    -- <summary>
    -- 由 UserContainerTaskLuaEventObserverPlugins 调用
    -- </summary>
    OnEvent = function (this, container, count)
      local isElementCount =this.Cfg.UseElementCount
      if isElementCount then
        _addElementCount(this, container, count)
      else
        _addPluginCount(this, container, count)
      end
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.Configs.OperatorBase_1(out.wdm.WuKong.Configs.Container.CfgLuaEventObserverPlugin)
        }
      end,
      GetCount = GetCount,
      ResetCount = ResetCount,
      OnEvent = OnEvent,
      __metadata__ = function (out)
        return {
          methods = {
            { "GetCount", 0x186, GetCount, out.wdm.WuKong.Abstractions.Container.IContainer, System.Int64, out.wdm.WuKong.Abstractions.Core.QueryAttribute("获取监听事件计数") },
            { "OnEvent", 0x206, OnEvent, out.wdm.WuKong.Abstractions.Container.IContainer, System.Int64 },
            { "ResetCount", 0x106, ResetCount, out.wdm.WuKong.Abstractions.Container.IContainer, out.wdm.WuKong.Abstractions.Core.ActionAttribute("重置监听事件计数") },
            { "_addElementCount", 0x201, _addElementCount, out.wdm.WuKong.Abstractions.Container.IContainer, System.Int64 },
            { "_addPluginCount", 0x201, _addPluginCount, out.wdm.WuKong.Abstractions.Container.IContainer, System.Int64 }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}