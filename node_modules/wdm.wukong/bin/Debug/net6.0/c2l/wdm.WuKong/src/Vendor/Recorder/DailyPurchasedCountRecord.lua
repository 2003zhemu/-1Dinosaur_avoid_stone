-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
local ListInt64 =System.List(System.Int64)
local ArrayInt64 =System.Array(System.Int64)
local wdmWuKongAbstractionsCore
local wdmWuKongCore
local __importGlobal =function (out)
  wdmWuKongAbstractionsCore = out.wdm.WuKong.Abstractions.Core
  wdmWuKongCore = out.wdm.WuKong.Core
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Vendor", function (namespace)
  -- <summary>
  -- 每日购买次数记录
  -- 该类将记录最近30天内每日购买次数.
  -- </summary>
  namespace.class("DailyPurchasedCountRecord", function (namespace)
    local _getDays, _resetHistory, AddCount, GetDaysCount, UpdateData, _getTodayLong, _takeLast, __ctor__
    -- <summary>
    -- 构造函数
    -- </summary>
    -- <param name="daysCount">
    -- 一份历史购买记录,该记录可能不完整,也可能过期
    -- </param>
    -- <param name="endDate"></param>
    __ctor__ = function (this, daysCount, endDate)
      this._daysCount = Linq.ToList(daysCount)
      this._endDate = endDate
      _resetHistory(this)
    end
    _getDays = function (this, timestamp)
      local result =System.div(timestamp, (86400 --[[24 * 3600]]))
      
      result = math.floor(result)
      
      return result
    end
    -- <summary>
    -- 更新每日购买次数数组和最近一次购买日期.
    -- 因为以上两个数据可能过时了,所以要根据当前日期,重新更新.
    -- </summary>
    _resetHistory = function (this)
      local today =_getTodayLong(this)
      local daysPast =(_getDays(this, today) - _getDays(this, this._endDate))
      if daysPast > 30 --[[DailyPurchasedCountRecord.MaxDays]] then
        daysPast = 30 --[[DailyPurchasedCountRecord.MaxDays]]
      end
      if daysPast > 0 then
        local list =ListInt64()
        for i = 0, daysPast - 1 do
          list:Add(0)
        end
        this._daysCount:AddRange(list)
        this._daysCount = Linq.ToList(_takeLast(this, this._daysCount, 30 --[[DailyPurchasedCountRecord.MaxDays]]))
        this._endDate = today
      end
    end
    -- <summary>
    -- 增加当日购买次数
    -- </summary>
    AddCount = function (this)
      _resetHistory(this)
      local default =this._daysCount
      default:set(#this._daysCount - 1, default:get(#this._daysCount - 1) + 1)
    end
    -- <summary>
    -- 获取每日购买次数数组
    -- </summary>
    -- <returns></returns>
    GetDaysCount = function (this, lastNDays)
      return _takeLast(this, this._daysCount, lastNDays):ToArray()
    end
    -- <summary>
    -- 写入数据
    -- </summary>
    UpdateData = function (this, owner)
      wdmWuKongAbstractionsCore.PluginDataExtension.SetPluginValue(owner, "最近一次购物时间" --[[RecordLevel.LastDate]], this._endDate, System.Int64)
      wdmWuKongAbstractionsCore.PluginDataExtension.SetPluginValue(owner, "本日购物次数" --[[RecordLevel.DailyCount]], this._daysCount:ToArray(), ArrayInt64)
    end
    _getTodayLong = function (this)
      return wdmWuKongCore.WuKongTimer.GetTimeStamp()
    end
    _takeLast = function (this, collection, count)
      local collectionCount =#collection
      if collectionCount <= count then
        return collection
      end
      for i = 0, collectionCount - count - 1 do
        collection:RemoveAt(0)
      end
      
      return collection
    end
    return {
      _endDate = 0,
      AddCount = AddCount,
      GetDaysCount = GetDaysCount,
      UpdateData = UpdateData,
      __ctor__ = __ctor__,
      __metadata__ = function (out)
        return {
          fields = {
            { "_daysCount", 0x1, System.List(System.Int64) },
            { "_endDate", 0x1, System.Int64 }
          },
          methods = {
            { ".ctor", 0x206, nil, System.Array(System.Int64), System.Int64 },
            { "AddCount", 0x6, AddCount },
            { "GetDaysCount", 0x186, GetDaysCount, System.Int64, System.Array(System.Int64) },
            { "UpdateData", 0x106, UpdateData, out.wdm.WuKong.Abstractions.Core.IHasPluginData },
            { "_getDays", 0x181, _getDays, System.Int64, System.Int32 },
            { "_getTodayLong", 0x81, _getTodayLong, System.Int64 },
            { "_resetHistory", 0x1, _resetHistory },
            { "_takeLast", 0x281, _takeLast, System.List(System.Int64), System.Int64, System.List(System.Int64) }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}