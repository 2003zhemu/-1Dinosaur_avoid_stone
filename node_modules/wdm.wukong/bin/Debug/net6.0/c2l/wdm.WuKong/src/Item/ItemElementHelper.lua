-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmContainer
local wdmWuKongConfigsContainer
local wdmWuKongConfigsItem
local wdmWuKongItem
local wdmWuKongProperty
local __importGlobal =function (out)
  wdmContainer = out.wdm.WuKong.Abstractions.Container
  wdmWuKongConfigsContainer = out.wdm.WuKong.Configs.Container
  wdmWuKongConfigsItem = out.wdm.WuKong.Configs.Item
  wdmWuKongItem = out.wdm.WuKong.Item
  wdmWuKongProperty = out.wdm.WuKong.Property
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Item", function (namespace)
  namespace.class("ItemElementHelper", function (namespace)
    local GetItemElement
    GetItemElement = function (itemId, tables, slot, count)
      local e
      local config =tables.TbItem:Get(itemId)
      local p =config.Type
      if System.is(p, wdmWuKongConfigsItem.CfgPropertyItem) then
        local containerProvider =wdmWuKongProperty.PropertyContainerProvider(tables)
        local propertyContainer =containerProvider:BuildPropertyContainer(p.Template_Ref)
        
        -- 使用slot作为该属性的父级
        propertyContainer:setParent(slot)
        
        local propertyItem =System.new(wdmWuKongItem.PropertyItem, 2, propertyContainer)
        e = propertyItem
        
        -- set default value
        for _, pair in System.each(p.Defaults) do
          local property =propertyItem.PropertyContainer:GetSlot(pair[1])
          property:getElement():setCount(pair[2])
        end
      else
        e = System.new(wdmContainer.Element, 2, itemId, wdmWuKongConfigsContainer.EElementType.Item)
      end
      
      e:setId(itemId)
      e:setType(wdmWuKongConfigsContainer.EElementType.Item)
      e:setConfig(config)
      e:setCount(count)
      return e
    end
    return {
      GetItemElement = GetItemElement,
      __metadata__ = function (out)
        return {
          methods = {
            { "GetItemElement", 0x48E, GetItemElement, System.String, out.wdm.WuKong.Configs.Tables, out.wdm.WuKong.Container.Slot, System.Int32, out.wdm.WuKong.Abstractions.Container.Element }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}