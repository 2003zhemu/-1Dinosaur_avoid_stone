-- Generated by CSharp.lua Compiler
-- Licensed to the .NET Foundation under one or more agreements.
-- The .NET Foundation licenses this file to you under the MIT license.
local System =_G.System
local wdmDependencyInjection
local wdmExtensions
local wdmExtensionsLogging
local __importGlobal =function (out)
  wdmDependencyInjection = out.wdm.Extensions.DependencyInjection
  wdmExtensions = out.wdm.Extensions.DependencyInjection.Extensions
  wdmExtensionsLogging = out.wdm.Extensions.Logging
end
System.import(__importGlobal)
System.namespace("wdm.Extensions.Logging", function (namespace)
  -- <summary>
  -- Extension methods for setting up logging services in an <see cref="ILoggingBuilder" />.
  -- </summary>
  namespace.class("LoggingBuilderExtensions", function (namespace)
    local AddProvider, ClearProviders
    -- <summary>
    -- Adds the given <see cref="ILoggerProvider"/> to the <see cref="ILoggingBuilder"/>
    -- </summary>
    -- <param name="builder">The <see cref="ILoggingBuilder"/> to add the <paramref name="provider"/> to.</param>
    -- <param name="provider">The <see cref="ILoggerProvider"/> to add to the <paramref name="builder"/>.</param>
    -- <returns>The <see cref="ILoggingBuilder"/> so that additional calls can be chained.</returns>
    AddProvider = function (builder, provider)
      wdmDependencyInjection.ServiceCollectionServiceExtensions.AddSingleton8(builder:getServices(), provider, wdmExtensionsLogging.ILoggerProvider)
      return builder
    end
    -- <summary>
    -- Removes all <see cref="ILoggerProvider"/>s from <paramref name="builder"/>.
    -- </summary>
    -- <param name="builder">The <see cref="ILoggingBuilder"/> to remove <see cref="ILoggerProvider"/>s from.</param>
    -- <returns>The <see cref="ILoggingBuilder"/> so that additional calls can be chained.</returns>
    ClearProviders = function (builder)
      wdmExtensions.ServiceCollectionDescriptorExtensions.RemoveAll(builder:getServices(), wdmExtensionsLogging.ILoggerProvider)
      return builder
    end
    return {
      AddProvider = AddProvider,
      ClearProviders = ClearProviders,
      __metadata__ = function (out)
        return {
          methods = {
            { "AddProvider", 0x28E, AddProvider, out.wdm.Extensions.Logging.ILoggingBuilder, out.wdm.Extensions.Logging.ILoggerProvider, out.wdm.Extensions.Logging.ILoggingBuilder },
            { "ClearProviders", 0x18E, ClearProviders, out.wdm.Extensions.Logging.ILoggingBuilder, out.wdm.Extensions.Logging.ILoggingBuilder }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}