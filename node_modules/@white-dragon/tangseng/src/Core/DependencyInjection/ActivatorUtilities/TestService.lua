local module = {}
module.__index = module

function module.new(testValue: string, serviceProvider: IServiceProvider)
	local self = setmetatable({}, module)
	self.testValue = testValue
	self.ServiceProvider = serviceProvider
	return self
end

return module
