--[=[
    @class IActivatorUtilities
    实例激活工具接口
]=]

type ImplementationType = string | ModuleScript

export type IActivatorUtilities = {
    --- 创建实例
    --- @param serviceProvider IServiceProvider 服务提供者
    --- @param targetType ImplementationType 目标类型
    --- @param ... any 额外参数
    --- @return any 创建的实例
    CreateInstance: (serviceProvider: IServiceProvider, targetType: ImplementationType, ...any) -> any
}

return {}
