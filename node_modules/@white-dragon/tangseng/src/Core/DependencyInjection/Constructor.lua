-- create constructor class
export type Type = {
	GetParameters: () -> { { ParameterType: { FullName: string } } },
	luaCtor: (...any) -> (),
}

local Constructor = {}
Constructor.__index = Constructor

function Constructor.new(): Type
	local self = setmetatable({}, Constructor)
	return self
end

return Constructor
