return function(source: IConfigurationSource)
	if source.Type == "Module" then
		assert(source.Instance, "ModuleScript is required for Module source type")
		return require(source.Instance)
	elseif source.Type == "Factory" then
		return source.Factory(source.Instance)
	else
		error("Invalid source type: " .. source.Type)
	end
end
