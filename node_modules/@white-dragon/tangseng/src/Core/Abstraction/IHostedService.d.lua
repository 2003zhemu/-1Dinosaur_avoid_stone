--[=[
    @class IHostedService
    托管服务接口
]=]

export type IHostedService = {
    --- 启动服务
    Start: (self: IHostedService) -> (),

    --- 停止服务
    Stop: (self: IHostedService) -> (),

    --- 销毁服务
    Dispose: (self: IHostedService) -> ()
}

return {}
