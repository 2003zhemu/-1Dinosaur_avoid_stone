-- 添加 Roblox 服务
return function(services: IServiceCollection)
	for k, v in pairs(game:GetChildren()) do
		local name = v.Name
		-- 去掉空格(比如 "Run Service" -> "RunService")
		name = name:gsub(" ", "")
		services:AddSingleton(name, function()
			return v
		end)
	end
end
