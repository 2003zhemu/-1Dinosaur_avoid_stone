local RunService = game:GetService("RunService")
return function(services: IServiceCollection, handler: (services: IServiceCollection) -> ())
	if RunService:IsServer() then
		handler(services)
	end
end
