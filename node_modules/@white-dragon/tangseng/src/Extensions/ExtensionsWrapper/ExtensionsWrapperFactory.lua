return function(source: ModuleScript)
	return function(object: {}): ITangSengHostBuilder
		local extensions = require(source)
		for k, v in extensions do
			assert(not object[k])
			object[k] = v
		end
		return object
	end
end
