--[=[
    @class IPlayerHostedService
    玩家上线后执行的后台服务接口
]=]

export type IPlayerHostedService = {
    --- 启动服务
    Start: (self: IPlayerHostedService) -> (),

    --- 停止服务
    Stop: (self: IPlayerHostedService) -> (),

    --- 销毁服务
    Dispose: (self: IPlayerHostedService) -> ()
}

return {}