--[=[
    @class IPlayerScopeCancellationTokenProvider
    玩家作用域取消令牌提供者接口
]=]

export type IPlayerScopeCancellationTokenProvider = {
    --- 获取取消令牌
    GetCancellationToken: (self: IPlayerScopeCancellationTokenProvider, userId: number) -> IPlayerScopeCancellationToken,

    --- 移除取消令牌
    RemoveCancellationToken: (self: IPlayerScopeCancellationTokenProvider, userId: number) -> ()
}

return {}