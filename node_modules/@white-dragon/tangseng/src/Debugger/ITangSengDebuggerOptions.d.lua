export type ITangSengDebuggerOptions = {
	-- 验证器, 返回 true 表示可以使用 Debugger
	-- 如果为空, 则仅在 stuido 内可以使用 Debugger
	Authorize: ((userId: number) -> boolean)?,

	-- 开启 Debugger 界面的快捷键
	KeyCode: Enum.KeyCode?,
}
return {}
