--[=[
    @class ITangSengHostBuilder
    唐僧主机构建器接口
]=]

export type ITangSengHostBuilder = {
    --- 配置应用配置文件夹，将该文件夹内所有的实例作为配置源，可以多次调用。可以指定一个反序列化函数，将实例反序列化为配置
    --- @param folder Instance 配置文件夹实例
    --- @param deserializer ((data: any) -> {})? 可选的反序列化函数
    ConfigureAppConfigurationFolder: (self: ITangSengHostBuilder, folder: Instance, deserializer: ((data: any) -> {})?) -> ITangSengHostBuilder
} & IHostBuilder

return {}