--[=[
    @class IProtocolEmiter
    协议发射器接口
]=]

export type IProtocolEmiter = {
    --- 发送协议
    --- @param protocol string 协议名
    --- @param dto table 协议数据
    --- @param specificPlayer Player? 指定玩家，可选，默认发送给当前作用域玩家
    EmitProtocol: (self: IProtocolEmiter, protocol: string, dto: {}, specificPlayer: Player?) -> ()
}

return {}