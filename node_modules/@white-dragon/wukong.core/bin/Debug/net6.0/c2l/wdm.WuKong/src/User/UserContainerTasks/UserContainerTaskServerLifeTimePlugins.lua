-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongContainer
local __importGlobal =function (out)
  wdmWuKongContainer = out.wdm.WuKong.Container
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.User.UserContainerTasks", function (namespace)
  namespace.class("UserContainerTaskServerLifeTimePlugins", function (namespace)
    local _updateLifeTimePlugins, Dispose, Start, __ctor__
    __ctor__ = function (this, userContainer)
      this._userContainer = userContainer
    end
    _updateLifeTimePlugins = function (this, container, isStart)
      if container:getPlugins() ~= nil then
        for _, plugin in System.each(container:getPlugins()) do
          local p =plugin
          if System.is(p, wdmWuKongContainer.IServerLifeTimePlugin) then
            if isStart then
              p:Start(container)
            else
              p:Stop(container)
            end
          end
        end
      end
      
      
      if container:getChildren() == nil then
        return
      end
      
      for _, child in System.each(container:getChildren()) do
        _updateLifeTimePlugins(this, child, isStart)
      end
    end
    Dispose = function (this)
      _updateLifeTimePlugins(this, this._userContainer, false)
    end
    Start = function (this)
      _updateLifeTimePlugins(this, this._userContainer, true)
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.User.UserContainerTasks.IUserContainerTasks
        }
      end,
      Dispose = Dispose,
      Start = Start,
      __ctor__ = __ctor__,
      __metadata__ = function (out)
        return {
          fields = {
            { "_userContainer", 0x1, out.wdm.WuKong.User.UserContainer }
          },
          methods = {
            { ".ctor", 0x106, nil, out.wdm.WuKong.User.UserContainer },
            { "Dispose", 0x6, Dispose },
            { "Start", 0x6, Start },
            { "_updateLifeTimePlugins", 0x201, _updateLifeTimePlugins, out.wdm.WuKong.Abstractions.Container.IContainer, System.Boolean }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}