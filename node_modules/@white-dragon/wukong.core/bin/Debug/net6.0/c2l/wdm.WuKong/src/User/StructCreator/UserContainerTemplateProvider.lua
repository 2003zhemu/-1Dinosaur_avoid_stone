-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongConfigsContainer
local wdmWuKongContainer
local wdmWuKongProperty
local wdmWuKongUser
local ArrayComputePropertyPlugin
local __importGlobal =function (out)
  wdmWuKongConfigsContainer = out.wdm.WuKong.Configs.Container
  wdmWuKongContainer = out.wdm.WuKong.Container
  wdmWuKongProperty = out.wdm.WuKong.Property
  wdmWuKongUser = out.wdm.WuKong.User
  ArrayComputePropertyPlugin = System.Array(wdmWuKongProperty.ComputePropertyPlugin)
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.User", function (namespace)
  -- <summary>
  -- 用户容器结构创建
  -- 创建的容器不包括数据
  -- </summary>
  namespace.class("UserContainerTemplateProvider", function (namespace)
    local CreateUserContainer, getOnCreateUserContainer, setOnCreateUserContainer, _configRootPlugins, _buildContainers, __ctor__
    __ctor__ = function (this, tables, aggregateContainerStructCreator, elementContainerStructCreator, isDisableTicket)
      this._tables = tables
      this._aggregateContainerStructCreator = aggregateContainerStructCreator
      this._elementContainerStructCreator = elementContainerStructCreator
      this._isDisableTicket = isDisableTicket
    end
    -- <summary>
    -- 创建UserContainer
    -- </summary>
    -- <returns></returns>
    CreateUserContainer = function (this, userId)
      -- 新建根节点容器,根节点一定是 AggregateContainer
      
      local root =wdmWuKongUser.UserContainer(userId, this._isDisableTicket)
      root:setIsActive(true)
      
      -- 遍历容器配置表,找出所有的 Primary为 true的配置.这些节点,我们称之为骨干节点.
      _buildContainers(this, root)
      
      -- 为根节点配置插件
      _configRootPlugins(this, root)
      
      if this.OnCreateUserContainer ~= nil then
        this.OnCreateUserContainer(root)
      end
      
      -- 返回根节点容器
      return root
    end
    getOnCreateUserContainer, setOnCreateUserContainer = System.property("OnCreateUserContainer")
    _configRootPlugins = function (this, root)
      -- 为根容器配置属性查询插件
      root:setPlugins(wdmWuKongContainer.CachedPluginsProviderExtension.BuildPlugins(root, function ()
        return ArrayComputePropertyPlugin(1, { wdmWuKongProperty.ComputePropertyPlugin() })
      end))
    end
    _buildContainers = function (this, root)
      for _, cfgContainer in System.each(this._tables.TbContainer:getDataList()) do
        local continue_lua
        repeat
          if not cfgContainer.Primary then
            continue_lua = true
            break
          end
          
          -- 创建骨干节点并加入到根节点容器中
          
          local primaryContainer
          local a =cfgContainer.Type
          if System.is(a, wdmWuKongConfigsContainer.CfgAggregateContainerType) then
            primaryContainer = this._aggregateContainerStructCreator:CreateContainer(cfgContainer, true)
          else
            primaryContainer = this._elementContainerStructCreator:CreateContainer(cfgContainer, true)
          end
          
          -- 主节点一定是激活的
          primaryContainer:Activate(true)
          
          root:AddChild(primaryContainer)
          continue_lua = true
        until 1
        if not continue_lua then
          break
        end
      end
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.User.IUserContainerTemplateProvider
        }
      end,
      _isDisableTicket = false,
      CreateUserContainer = CreateUserContainer,
      getOnCreateUserContainer = getOnCreateUserContainer,
      setOnCreateUserContainer = setOnCreateUserContainer,
      __ctor__ = __ctor__,
      __metadata__ = function (out)
        return {
          fields = {
            { "_aggregateContainerStructCreator", 0x1, out.wdm.WuKong.User.AggregateContainerStructCreator },
            { "_elementContainerStructCreator", 0x1, out.wdm.WuKong.User.ElementContainerStructCreator },
            { "_isDisableTicket", 0x1, System.Boolean },
            { "_tables", 0x1, out.wdm.WuKong.Configs.Tables }
          },
          methods = {
            { ".ctor", 0x406, nil, out.wdm.WuKong.Configs.Tables, out.wdm.WuKong.User.AggregateContainerStructCreator, out.wdm.WuKong.User.ElementContainerStructCreator, System.Boolean },
            { "CreateUserContainer", 0x186, CreateUserContainer, System.Int64, out.wdm.WuKong.Abstractions.IUserContainer },
            { "_buildContainers", 0x101, _buildContainers, out.wdm.WuKong.Container.AggregateContainer },
            { "_configRootPlugins", 0x101, _configRootPlugins, out.wdm.WuKong.Container.AggregateContainer }
          },
          properties = {
            { "OnCreateUserContainer", 0x106, System.Delegate(out.wdm.WuKong.Abstractions.IUserContainer, System.Void), getOnCreateUserContainer, setOnCreateUserContainer }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}