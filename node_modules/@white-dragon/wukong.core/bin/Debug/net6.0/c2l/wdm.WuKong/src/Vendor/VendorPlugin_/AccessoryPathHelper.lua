-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
local wdmContainer
local wdmWuKongContainer
local wdmWuKongCore
local ListSlot
local __importGlobal =function (out)
  wdmContainer = out.wdm.WuKong.Abstractions.Container
  wdmWuKongContainer = out.wdm.WuKong.Container
  wdmWuKongCore = out.wdm.WuKong.Core
  ListSlot = System.List(_G.wdm.WuKong.Container.Slot)
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Vendor", function (namespace)
  namespace.class("AccessoryPathHelper", function (namespace)
    local GetAccessorySlots
    GetAccessorySlots = function (slot, extraCostPath)
      if extraCostPath == nil or not Linq.Any(extraCostPath) then
        return nil
      end
      local list =ListSlot()
      for _, path in System.each(extraCostPath) do
        local root =slot:GetRootContainer()
        local container =wdmWuKongCore.PluginOwnerPathExtension.FindRequired2(root, path, wdmContainer.IContainer)
        local newSlot =wdmWuKongContainer.ContainerExtension.AssertAsSlot(container)
        list:Add(newSlot)
      end
      
      return list:ToArray()
    end
    return {
      GetAccessorySlots = GetAccessorySlots,
      __metadata__ = function (out)
        return {
          methods = {
            { "GetAccessorySlots", 0x28E, GetAccessorySlots, out.wdm.WuKong.Container.Slot, System.Array(System.String), System.Array(out.wdm.WuKong.Container.Slot) }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}