-- Generated by CSharp.lua Compiler
local System =_G.System
local ArrayString =System.Array(System.String)
local wdmWuKongAbstractionsCore
local wdmWuKongContainer
local wdmWuKongCore
local wdmWuKongVendor
local __importGlobal =function (out)
  wdmWuKongAbstractionsCore = out.wdm.WuKong.Abstractions.Core
  wdmWuKongContainer = out.wdm.WuKong.Container
  wdmWuKongCore = out.wdm.WuKong.Core
  wdmWuKongVendor = out.wdm.WuKong.Vendor
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Vendor", function (namespace)
  namespace.class("ChancesByDaysCostPlugin", function (namespace)
    local getRecordLevels, Validate, Cost, Record
    getRecordLevels = function (this)
      return ArrayString { "本日购物次数" --[[RecordLevel.DailyCount]] }
    end
    Validate = function (this, container, extra)
      local stop =System.Stopwatch()
      local slot =wdmWuKongContainer.ContainerExtension.AssertAsSlot(container)
      local owner =slot:getElement()
      
      local recorder =wdmWuKongCore.PluginsExtension.GetPlugin(container, wdmWuKongVendor.IVenderRecorderPlugin)
      
      local purchasedToday =recorder:SumPurchasedCountInLastNDays(owner, this.Cfg.Days)
      stop:Stop()
      if purchasedToday >= this.Cfg.Chances then
        return wdmWuKongAbstractionsCore.Validation.Fail("购物记录检查", "N日N次")
      end
      return wdmWuKongAbstractionsCore.Validation.Pass()
    end
    -- <summary>
    -- 仅作验证,没有实际消耗任何元素
    -- </summary>
    Cost = function (this, container, extra)
      local validation =Validate(this, container, extra)
      validation:AssertPass()
      local result =System.new(wdmWuKongVendor.CostResult, 4, 0)
      return result
    end
    -- <summary>
    -- 购物成功后,被调用,进行记录.
    -- </summary>
    Record = function (this, container, purchaseResult)
      local slot =wdmWuKongContainer.ContainerExtension.AssertAsSlot(container)
      local owner =slot:getElement()
      
      local recorder =wdmWuKongCore.PluginsExtension.GetPlugin(container, wdmWuKongVendor.IVenderRecorderPlugin)
      recorder:Record(owner, purchaseResult, getRecordLevels(this))
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.Configs.OperatorBase_1(out.wdm.WuKong.Configs.Vendor.CfgChancesByDaysCost),
          out.wdm.WuKong.Vendor.ICostPlugin,
          out.wdm.WuKong.Vendor.INeedRecorder
        }
      end,
      getRecordLevels = getRecordLevels,
      Validate = Validate,
      Cost = Cost,
      Record = Record,
      __metadata__ = function (out)
        return {
          properties = {
            { "RecordLevels", 0x206, System.Array(System.String), getRecordLevels }
          },
          methods = {
            { "Cost", 0x286, Cost, out.wdm.WuKong.Abstractions.Container.IContainer, System.Object, out.wdm.WuKong.Vendor.CostResult },
            { "Record", 0x206, Record, out.wdm.WuKong.Abstractions.Container.IContainer, out.wdm.WuKong.Vendor.PurchaseResult },
            { "Validate", 0x286, Validate, out.wdm.WuKong.Abstractions.Container.IContainer, System.Object, out.wdm.WuKong.Abstractions.Core.Validation }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}