-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmContainer
local wdmWuKongConfigsContainer
local wdmWuKongContainer
local __importGlobal =function (out)
  wdmContainer = out.wdm.WuKong.Abstractions.Container
  wdmWuKongConfigsContainer = out.wdm.WuKong.Configs.Container
  wdmWuKongContainer = out.wdm.WuKong.Container
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Property", function (namespace)
  -- <summary>
  -- 属性定位扩展
  -- </summary>
  namespace.class("PropertyLocationExtension", function (namespace)
    local FindProperty, GetRequiredProperty
    -- <summary>
    -- 获取属性
    -- </summary>
    -- <param name="container"></param>
    -- <param name="path"></param>
    -- <returns></returns>
    -- <exception cref="ArgumentException"></exception>
    FindProperty = function (container, path)
      local obj =wdmWuKongContainer.ContainerLocationExtension.Find(container, path)
      if obj ~= nil then
        local o =obj
        if System.is(o, wdmContainer.Element) and o:getType() == wdmWuKongConfigsContainer.EElementType.Property then
          return o
        else
          System.throw(System.ArgumentException(System.toString(System.toString("指定路径 '") .. System.toString(path)) .. System.toString("' 并不是 'Property' 对象 ")))
        end
      end
      
      return nil
    end
    -- <summary>
    -- 确保获取到属性
    -- </summary>
    -- <param name="container"></param>
    -- <param name="path"></param>
    -- <returns></returns>
    GetRequiredProperty = function (container, path)
      local p =FindProperty(container, path)
      if p == nil then
        if System.String.IsNullOrEmpty(path) then
          System.throw(System.KeyNotFoundException("can't find path :(path is null)"))
        else
          System.throw(System.KeyNotFoundException(System.toString("can't find path:") .. System.toString(path)))
        end
      end
      return p
    end
    return {
      FindProperty = FindProperty,
      GetRequiredProperty = GetRequiredProperty,
      __metadata__ = function (out)
        return {
          methods = {
            { "FindProperty", 0x28E, FindProperty, out.wdm.WuKong.Abstractions.Container.IContainer, System.String, out.wdm.WuKong.Abstractions.Container.Element },
            { "GetRequiredProperty", 0x28E, GetRequiredProperty, out.wdm.WuKong.Abstractions.Container.IContainer, System.String, out.wdm.WuKong.Abstractions.Container.Element }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}