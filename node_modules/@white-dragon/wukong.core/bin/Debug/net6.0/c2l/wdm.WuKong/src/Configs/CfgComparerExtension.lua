-- Generated by CSharp.lua Compiler
local System =_G.System
System.namespace("wdm.WuKong.Configs", function (namespace)
  namespace.class("CfgComparerExtension", function (namespace)
    local Compare, CompareSymbol
    Compare = function (comparer, left, right)
      return CompareSymbol(comparer.Symbol, left, right)
    end
    CompareSymbol = function (comparer, left, right)
      repeat
        local default =comparer
        if default == ">" then
          return left > right
        elseif default == ">=" then
          return left >= right
        elseif default == "<" then
          return left < right
        elseif default == "<=" then
          return left <= right
        elseif default == "==" or default == "等于" then
          return left == right
        elseif default == "~=" or default == "!=" then
          return left ~= right
        else
          System.throw(System.ArgumentOutOfRangeException("comparer" --[[nameof(comparer)]], "未知的比较器类型"))
        end
      until 1
    end
    return {
      Compare = Compare,
      CompareSymbol = CompareSymbol,
      __metadata__ = function (out)
        return {
          methods = {
            { "Compare", 0x38E, Compare, out.wdm.WuKong.Configs.Core.CfgComparer, System.Int64, System.Int64, System.Boolean },
            { "CompareSymbol", 0x38E, CompareSymbol, System.String, System.Int64, System.Int64, System.Boolean }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}