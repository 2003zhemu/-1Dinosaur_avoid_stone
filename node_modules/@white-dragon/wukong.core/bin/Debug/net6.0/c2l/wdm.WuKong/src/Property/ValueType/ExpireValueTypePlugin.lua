-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongContainer
local wdmWuKongCore
local __importGlobal =function (out)
  wdmWuKongContainer = out.wdm.WuKong.Container
  wdmWuKongCore = out.wdm.WuKong.Core
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Property", function (namespace)
  -- <summary>
  -- 有效期值类型插件
  -- </summary>
  namespace.class("ExpireValueTypePlugin", function (namespace)
    local IsExpired, IsNotExpired, AddExpireMinutes, SetExpireDate, GetExpireDateString, GetExpireDate, GetExpireSeconds, GetExpireMinutes, 
    IncreaseValue, TickClient, Tick
    -- <summary>
    -- 判断元素已过期
    -- </summary>
    IsExpired = function (this, slot)
      -- 获取当前Unix时间戳
      local currentTimestamp =wdmWuKongCore.WuKongTimer.GetTimeStamp()
      
      -- 如果元素的过期时间小于当前时间戳，则已过期
      return slot:getElement():getCount() < currentTimestamp
    end
    -- <summary>
    -- 判断元素未过期
    -- </summary>
    IsNotExpired = function (this, slot)
      return not IsExpired(this, slot)
    end
    -- <summary>
    -- 增加元素过期时间
    -- 如果元素已过期,则以当前时间点开始计算.
    -- </summary>
    -- <returns>Unix 到期时间</returns>
    AddExpireMinutes = function (this, slot, minutes)
      local currentTimestamp =wdmWuKongCore.WuKongTimer.GetTimeStamp()
      local newExpireTimestamp =currentTimestamp + (minutes * 60)
      
      -- 如果元素已过期，则以当前时间为基准增加过期时间
      if IsExpired(this, slot) then
        slot:getElement():setCount(newExpireTimestamp)
      else
        -- 如果元素未过期，增加其过期时间
        slot:getElement():setCount(slot:getElement():getCount() + (minutes * 60))
      end
      
      return slot:getElement():getCount()
    end
    -- <summary>
    -- 设置属性到期时间
    -- </summary>
    -- <returns>Unix 到期时间</returns>
    SetExpireDate = function (this, slot, date)
      slot:getElement():setCount(date)
      return date
    end
    -- <summary>
    -- 属性到期时间字符串查询
    -- </summary>
    -- <returns>Unix 到期时间</returns>
    GetExpireDateString = function (this, slot)
      return "未实现日期字符串"
    end
    -- <summary>
    -- 属性到期时间查询
    -- </summary>
    -- <returns>Unix 到期时间</returns>
    GetExpireDate = function (this, slot)
      return slot:getElement():getCount()
    end
    -- <summary>
    -- 获取属性到期(秒)
    -- </summary>
    GetExpireSeconds = function (this, slot)
      local currentTimestamp =wdmWuKongCore.WuKongTimer.GetTimeStamp()
      local expireSec =slot:getElement():getCount() - currentTimestamp
      if expireSec < 0 then
        return 0
      end
      return expireSec
    end
    -- <summary>
    -- 获取属性到期(分钟)
    -- </summary>
    GetExpireMinutes = function (this, slot)
      return System.div(GetExpireSeconds(this, slot), 60)
    end
    IncreaseValue = function (this, slot, value)
      return AddExpireMinutes(this, slot, value)
    end
    TickClient = function (this, container)
      -- 抛出查询事件
      local sec =GetExpireSeconds(this, wdmWuKongContainer.ContainerExtension.AssertAsSlot(container))
      container:DispatchQueryChangedEvent("获取锁到期秒" --[[ExpireValueTypePlugin.ABILITY_Expiration]], sec)
    end
    Tick = function (this, container)
      -- 抛出查询事件
      local sec =GetExpireSeconds(this, wdmWuKongContainer.ContainerExtension.AssertAsSlot(container))
      container:DispatchQueryChangedEvent("获取锁到期秒" --[[ExpireValueTypePlugin.ABILITY_Expiration]], sec)
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.Configs.OperatorBase_1(out.wdm.WuKong.Configs.Property.CfgExpireValueType),
          out.wdm.WuKong.Property.IValueTypePlugin,
          out.wdm.WuKong.Property.IAddableValueTypePlugin,
          out.wdm.WuKong.Container.ITickablePlugin,
          out.wdm.WuKong.Container.IClientTickablePlugin
        }
      end,
      IsExpired = IsExpired,
      IsNotExpired = IsNotExpired,
      AddExpireMinutes = AddExpireMinutes,
      SetExpireDate = SetExpireDate,
      GetExpireDateString = GetExpireDateString,
      GetExpireDate = GetExpireDate,
      GetExpireSeconds = GetExpireSeconds,
      GetExpireMinutes = GetExpireMinutes,
      IncreaseValue = IncreaseValue,
      TickClient = TickClient,
      Tick = Tick,
      __metadata__ = function (out)
        return {
          methods = {
            { "AddExpireMinutes", 0x286, AddExpireMinutes, out.wdm.WuKong.Container.Slot, System.Int64, System.Int64, out.wdm.WuKong.Abstractions.Core.ActionAttribute("属性增加到期分钟", "minutes") },
            { "GetExpireDate", 0x186, GetExpireDate, out.wdm.WuKong.Container.Slot, System.Int64, out.wdm.WuKong.Abstractions.Core.QueryAttribute("属性到期时间") },
            { "GetExpireDateString", 0x186, GetExpireDateString, out.wdm.WuKong.Container.Slot, System.String, out.wdm.WuKong.Abstractions.Core.QueryAttribute("属性到期时间字符串") },
            { "GetExpireMinutes", 0x186, GetExpireMinutes, out.wdm.WuKong.Container.Slot, System.Int64, out.wdm.WuKong.Abstractions.Core.QueryAttribute("属性到期分钟") },
            { "GetExpireSeconds", 0x186, GetExpireSeconds, out.wdm.WuKong.Container.Slot, System.Int64, out.wdm.WuKong.Abstractions.Core.QueryAttribute("属性到期秒") },
            { "IncreaseValue", 0x286, IncreaseValue, out.wdm.WuKong.Container.Slot, System.Int64, System.Int64 },
            { "IsExpired", 0x186, IsExpired, out.wdm.WuKong.Container.Slot, System.Boolean, out.wdm.WuKong.Abstractions.Core.QueryAttribute("属性已过期") },
            { "IsNotExpired", 0x186, IsNotExpired, out.wdm.WuKong.Container.Slot, System.Boolean, out.wdm.WuKong.Abstractions.Core.QueryAttribute("属性未过期") },
            { "SetExpireDate", 0x286, SetExpireDate, out.wdm.WuKong.Container.Slot, System.Int64, System.Int64, out.wdm.WuKong.Abstractions.Core.ActionAttribute("属性设为到期时间", "date") },
            { "Tick", 0x106, Tick, out.wdm.WuKong.Abstractions.Container.IContainer },
            { "TickClient", 0x106, TickClient, out.wdm.WuKong.Abstractions.Container.IContainer }
          },
          class = { 0x6, out.wdm.WuKong.Abstractions.Core.PluginAttribute("有效期值类型插件"), out.wdm.WuKong.Abstractions.Core.PluginDescriptionAttribute("设置属性类型为有效期类型"), out.wdm.WuKong.Abstractions.Core.PluginTagAttribute("格子", "属性", "值类型") }
        }
      end
    }
  end)
end)
return {}