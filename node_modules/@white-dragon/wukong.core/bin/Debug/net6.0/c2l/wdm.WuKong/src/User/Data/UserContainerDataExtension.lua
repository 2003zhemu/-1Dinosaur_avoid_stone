-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongConfigs
local wdmWuKongContainer
local wdmWuKongUser
local wdmWuKongUserSerialize
local __importGlobal =function (out)
  wdmWuKongConfigs = out.wdm.WuKong.Configs
  wdmWuKongContainer = out.wdm.WuKong.Container
  wdmWuKongUser = out.wdm.WuKong.User
  wdmWuKongUserSerialize = out.wdm.WuKong.User.Serialize
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.User", function (namespace)
  namespace.class("UserContainerDataExtension", function (namespace)
    local FulfillContainer, _executeActions, _fulfillElements
    -- <summary>
    -- 使用数据填充 UserContainer
    -- </summary>
    -- <param name="userContainer"></param>
    -- <param name="data"></param>
    -- <param name="tables"></param>
    -- <returns></returns>
    FulfillContainer = function (data, userContainer, tables)
      _fulfillElements(data.Elements, userContainer, tables)
      
      
      -- 触发插件反序列化
      wdmWuKongUserSerialize.PluginSerializeHelper.OnDeserializeContainer(userContainer)
      
      -- 执行配置行动
      _executeActions(data.Actions, userContainer, tables)
    end
    _executeActions = function (actions, userContainer, tables)
      for _, action in System.each(actions) do
        System.try(function ()
          local _, target = wdmWuKongConfigs.CfgActionExtension.ExecuteAction(action, userContainer)
        end, function (default)
          local ex =default
          System.throw(System.Exception(System.toString(System.toString(System.toString("用户数据初始化,在执行行动时出现错误! \r 路径: ") .. System.toString(action.Location.Path)) .. System.toString(" \r 错误:")) .. System.toString(ex:getMessage())))
        end)
      end
    end
    _fulfillElements = function (dataElements, container, tables)
      if dataElements == nil then
        return
      end
      for _, pair in System.each(dataElements) do
        local parentContainer =wdmWuKongContainer.ContainerLocationExtension.FindRequired(container, pair[1], wdmWuKongContainer.ElementContainer)
        wdmWuKongUser.ElementFulfillHelper.Fulfill(parentContainer, pair[2], tables)
      end
    end
    return {
      FulfillContainer = FulfillContainer,
      __metadata__ = function (out)
        return {
          methods = {
            { "FulfillContainer", 0x30E, FulfillContainer, out.wdm.WuKong.User.UserContainerInitialData, out.wdm.WuKong.Abstractions.Container.IContainer, out.wdm.WuKong.Configs.Tables },
            { "_executeActions", 0x309, _executeActions, System.Array(out.wdm.WuKong.Configs.Core.CfgAction), out.wdm.WuKong.Abstractions.Container.IContainer, out.wdm.WuKong.Configs.Tables },
            { "_fulfillElements", 0x309, _fulfillElements, System.Dictionary(System.String, out.wdm.WuKong.User.UserElementCollection), out.wdm.WuKong.Abstractions.Container.IContainer, out.wdm.WuKong.Configs.Tables }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}