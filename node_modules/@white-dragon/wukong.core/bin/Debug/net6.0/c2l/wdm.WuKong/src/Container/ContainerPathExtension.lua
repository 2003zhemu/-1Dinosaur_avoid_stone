-- Generated by CSharp.lua Compiler
local System =_G.System
local ListString =System.List(System.String)
local wdmWuKongContainer
local __importGlobal =function (out)
  wdmWuKongContainer = out.wdm.WuKong.Container
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Container", function (namespace)
  namespace.class("ContainerPathExtension", function (namespace)
    local GetPath, GetPath1, _getPath
    -- <summary>
    -- 获取聚合容器路径
    -- </summary>
    -- <param name="container"></param>
    -- <returns></returns>
    GetPath = function (container)
      local list =ListString()
      list:Add(container:getId())
      return _getPath(System.as(container:getParent(), wdmWuKongContainer.AggregateContainer), list)
    end
    -- <summary>
    -- 获取元素容器路径
    -- </summary>
    -- <param name="container"></param>
    -- <returns></returns>
    GetPath1 = function (container)
      local list =ListString()
      list:Add(container:getId())
      return _getPath(System.as(container:getParent(), wdmWuKongContainer.AggregateContainer), list)
    end
    _getPath = function (container, ids)
      if container ~= nil then
        ids:Insert(0, container:getId())
        return _getPath(System.as(container:getParent(), wdmWuKongContainer.AggregateContainer), ids)
      end
      
      -- 根部的container不算进路由内
      ids:RemoveAt(0)
      
      return System.String.JoinEnumerable("/", ids)
      
      -- 根部的container算进路由内
      -- return string.Join("/", ids);
    end
    return {
      GetPath = GetPath,
      GetPath1 = GetPath1,
      __metadata__ = function (out)
        return {
          methods = {
            { "GetPath", 0x18E, GetPath, out.wdm.WuKong.Container.AggregateContainer, System.String },
            { "GetPath", 0x18E, GetPath1, out.wdm.WuKong.Container.ElementContainer, System.String },
            { "_getPath", 0x289, _getPath, out.wdm.WuKong.Container.AggregateContainer, System.List(System.String), System.String }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}