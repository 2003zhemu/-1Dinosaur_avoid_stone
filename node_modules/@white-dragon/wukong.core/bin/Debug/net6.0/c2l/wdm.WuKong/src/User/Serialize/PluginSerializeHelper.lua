-- Generated by CSharp.lua Compiler
local System =_G.System
local wdmWuKongAbstractionsCore
local __importGlobal =function (out)
  wdmWuKongAbstractionsCore = out.wdm.WuKong.Abstractions.Core
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.User.Serialize", function (namespace)
  namespace.class("PluginSerializeHelper", function (namespace)
    local OnSerializeContainer, _onSerializeContainer, _onInitContainer, OnDeserializeContainer, _onDeserializeContainer
    OnSerializeContainer = function (container)
      _onSerializeContainer(container)
      if container:getChildren() ~= nil then
        for _, child in System.each(container:getChildren()) do
          OnSerializeContainer(child)
        end
      end
    end
    _onSerializeContainer = function (container)
      if container:getPlugins() ~= nil then
        for _, plugin in System.each(container:getPlugins()) do
          local c =plugin
          if System.is(c, wdmWuKongAbstractionsCore.ISerializablePlugin) then
            local d =container
            if d ~= nil then
              c:OnSerialize(d)
            end
          end
        end
      end
    end
    _onInitContainer = function (container)
      local key ="IS_INIT"
      if container:getPlugins() ~= nil then
        for _, plugin in System.each(container:getPlugins()) do
          local c =plugin
          if System.is(c, wdmWuKongAbstractionsCore.IContainerInitializer) then
            local default, _ = wdmWuKongAbstractionsCore.PluginDataExtension.TryGetPluginValue1(container, key, nil, System.Boolean)
            if not default then
              c:OnInitContainer(container)
              wdmWuKongAbstractionsCore.PluginDataExtension.SetPluginValue(container, key, true, System.Boolean)
            end
          end
        end
      end
    end
    OnDeserializeContainer = function (container)
      _onInitContainer(container)
      _onDeserializeContainer(container)
      if container:getChildren() ~= nil then
        for _, child in System.each(container:getChildren()) do
          OnDeserializeContainer(child)
        end
      end
    end
    _onDeserializeContainer = function (container)
      if container:getPlugins() ~= nil then
        for _, plugin in System.each(container:getPlugins()) do
          local c =plugin
          if System.is(c, wdmWuKongAbstractionsCore.ISerializablePlugin) then
            local d =container
            if d ~= nil then
              c:OnDeserialize(d)
            end
          end
        end
      end
    end
    return {
      OnSerializeContainer = OnSerializeContainer,
      OnDeserializeContainer = OnDeserializeContainer,
      __metadata__ = function (out)
        return {
          methods = {
            { "OnDeserializeContainer", 0x10E, OnDeserializeContainer, out.wdm.WuKong.Abstractions.Container.IContainer },
            { "OnSerializeContainer", 0x10E, OnSerializeContainer, out.wdm.WuKong.Abstractions.Container.IContainer },
            { "_onDeserializeContainer", 0x109, _onDeserializeContainer, out.wdm.WuKong.Abstractions.Container.IContainer },
            { "_onInitContainer", 0x109, _onInitContainer, out.wdm.WuKong.Abstractions.Container.IContainer },
            { "_onSerializeContainer", 0x109, _onSerializeContainer, out.wdm.WuKong.Abstractions.Container.IContainer }
          },
          class = { 0xE }
        }
      end
    }
  end)
end)
return {}