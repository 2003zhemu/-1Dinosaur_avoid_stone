-- Generated by CSharp.lua Compiler
local System =_G.System
local Linq =System.Linq.Enumerable
local Roblox =_G.Roblox
local wdmWuKongAbstractions
local wdmContainer
local wdmWuKongContainer
local wdmWuKongCore
local wdmWuKongIoc
local wdmWuKongIocObservers
local wdmWuKongItem
local wdmWuKongSerializer
local wdmWuKongUserSerialize
local HashSetIWuKongObserver
local __importGlobal =function (out)
  wdmWuKongAbstractions = out.wdm.WuKong.Abstractions
  wdmContainer = out.wdm.WuKong.Abstractions.Container
  wdmWuKongContainer = out.wdm.WuKong.Container
  wdmWuKongCore = out.wdm.WuKong.Core
  wdmWuKongIoc = out.wdm.WuKong.Ioc
  wdmWuKongIocObservers = out.wdm.WuKong.Ioc.Observers
  wdmWuKongItem = out.wdm.WuKong.Item
  wdmWuKongSerializer = out.wdm.WuKong.Serializer
  wdmWuKongUserSerialize = out.wdm.WuKong.User.Serialize
  HashSetIWuKongObserver = System.HashSet(_G.wdm.WuKong.Ioc.Observers.IWuKongObserver)
end
System.import(__importGlobal)
System.namespace("wdm.WuKong.Ioc", function (namespace)
  -- <summary>
  -- 悟空For罗布乐思Lua门面
  -- </summary>
  namespace.class("WuKongRbxFacade", function (namespace)
    local SetRoot, DisposeRoot, getUserId, GetContainer, GetTables, GetChild, TryGetChild, ExecuteQuery, 
    ExecuteAction, ExecuteValidate, ExecuteCommand, GetCommandIds, GetUnixTime, GetRootContainer, RegisterContainerPropertyObserver, RegisterContainerPluginDataObserver, 
    RegisterContainerQueryObserver, RegisterContainerActionObserver, RegisterSlotObserver, RegisterActionObserver, IsContainer, IsElementContainer, IsAggregateContainer, IsElement, 
    IsPropertyItem, IsSlot, UpdateSlotCount, SerializeWuKongContainer, DeserializeWuKongContainer, GetElementExtraConfig, GetElementName, GetElementConfig, 
    Dispose, GetUserId, __ctor__
    __ctor__ = function (this, tables, templateProvider, actionCenter, commandCenter, elementConfigProvider, routeCenter)
      this._observers = HashSetIWuKongObserver()
      this._templateProvider = templateProvider
      this._tables = tables
      this._actionCenter = actionCenter
      this._commandCenter = commandCenter
      this._routeCenter = routeCenter
      this._argsHelper = wdmWuKongIoc.ArgsHelper()
      this._elementConfigProvider = elementConfigProvider
    end
    -- <summary>
    -- 设置根容器
    -- </summary>
    -- <param name="root"></param>
    SetRoot = function (this, root, disableAutoStart)
      this.Root = root
      if not disableAutoStart then
        root:Start()
      else
        root:StartWithClientEnable()
      end
    end
    DisposeRoot = function (this)
      if this.Root == nil then
        System.throw(System.new(wdmWuKongAbstractions.WuKongException, 2, "Root is null"))
      end
      this.Root:Stop()
      this.Root = nil
    end
    getUserId = function (this)
      local u =this.Root
      if System.is(u, wdmWuKongAbstractions.IUserContainer) then
        return u:getUserId()
      end
      return - 1
    end
    GetContainer = function (this)
      return this.Root
    end
    GetTables = function (this)
      return this._tables
    end
    -- <summary>
    -- 根据路径获取指定节点
    -- </summary>
    GetChild = function (this, location)
      assert(this.Root ~= nil, "必须设置根容器")
      return wdmWuKongCore.PluginOwnerPathExtension.FindRequired2(this.Root, location, wdmContainer.IContainer)
    end
    -- <summary>
    -- 根据路径获取指定节点
    -- </summary>
    TryGetChild = function (this, location, container)
      assert(this.Root ~= nil, "必须设置根容器")
      container = wdmWuKongCore.PluginOwnerPathExtension.Find2(this.Root, location)
      if container == nil then
        return false, container
      end
      return true, container
    end
    ExecuteQuery = function (this, location, ...)
      local var= {...} 
      local args=System.arrayFromTable(var,System.Object)
      assert(this.Root ~= nil, "必须设置根容器")
      return wdmWuKongIoc.WuKongHelper.ExecuteQuery(this.Root, location, System.toLuaTuple(args))
    end
    ExecuteAction = function (this, location, ...)
      local var= {...} 
      local args=System.arrayFromTable(var,System.Object)
      local default, extern = System.try(function ()
        -- 客户端不再检查路由合法性,由服务端检查
        --_routeCenter.AssertPass(location, args);
        local result =this._actionCenter:ExecuteAction(this.Root, location, System.toLuaTuple(args))
        
        local default =this.OnActionFinished
                            if default ~= nil then
         default(location, unpack(var))
                            end
        return true, result
      end, function (default)
        local e =default
        local i =e
        if System.is(i, wdmWuKongIoc.WuKongRbxFacadeInvokeException) then
          if i.ExceptionType ~= "wdm.WuKong.Abstractions.RobloxCancelPurchaseException" then
            warn(System.toString(System.toString(System.toString("调用行动错误:") .. System.toString(location)) .. System.toString("\n")) .. System.toString(e:ToString()))
          end
        end
        System.throw(default)
      end)
      if default then
        return extern
      end
    end
    ExecuteValidate = function (this, location, ...)
      local var= {...} 
      local args=System.arrayFromTable(var,System.Object)
      assert(this.Root ~= nil, "必须设置根容器")
      return wdmWuKongIoc.WuKongHelper.ExecuteValidator(this.Root, location, System.toLuaTuple(args))
    end
    ExecuteCommand = function (this, commandId, ...)
      local var= {...} 
      local inputArgs=System.arrayFromTable(var,System.Object)
      this._routeCenter:AssertAdmin(commandId, inputArgs)
      this._commandCenter:ExecuteCommand(this.Root, commandId, inputArgs)
    end
    GetCommandIds = function (this)
      return this._commandCenter:GetCommandIds()
    end
    -- <summary>
    -- 获取Unix时间戳
    -- </summary>
    -- <returns></returns>
    GetUnixTime = function (this)
      return wdmWuKongCore.WuKongTimer.GetTimeStamp()
    end
    GetRootContainer = function (this)
      return this.Root
    end
    -- <summary>
    -- 注册容器属性观察者
    -- path: 监听的容器的路径, 可以为空, 使用container作为目标
    -- handler: 事件回调
    -- container: 路径的起始容器, 可以为空, 使用root作为目标
    -- </summary>
    RegisterContainerPropertyObserver = function (this, path, handler, container)
      if this.Root == nil then
        System.throw(System.NullReferenceException("Root container is null"))
      end
      if container == nil then
        container = this.Root
      end
      local observer =wdmWuKongIocObservers.ContainerPropertyObserver(path, handler, container, wdmWuKongIocObservers.CancelToken(this._observers))
      this._observers:Add(observer)
    end
    -- <summary>
    -- 注册容器插件数据观察者
    -- path: 监听的容器的路径, 可以为空, 使用container作为目标
    -- handler: 事件回调
    -- container: 路径的起始容器, 可以为空, 使用root作为目标
    -- </summary>
    RegisterContainerPluginDataObserver = function (this, path, handler, container)
      if this.Root == nil then
        System.throw(System.NullReferenceException("Root container is null"))
      end
      if container == nil then
        container = this.Root
      end
      this._observers:Add(wdmWuKongIocObservers.ContainerPluginDataObserver(path, handler, container, wdmWuKongIocObservers.CancelToken(this._observers)))
    end
    -- <summary>
    -- 注册容器查询观察者
    -- path: 监听的容器的路径, 可以为空, 使用container作为目标
    -- handler: 事件回调
    -- container: 路径的起始容器, 可以为空, 使用root作为目标
    -- </summary>
    RegisterContainerQueryObserver = function (this, path, handler, container)
      if this.Root == nil then
        System.throw(System.NullReferenceException("Root container is null"))
      end
      if container == nil then
        container = this.Root
      end
      this._observers:Add(wdmWuKongIocObservers.ContainerQueryObserver(path, handler, container, wdmWuKongIocObservers.CancelToken(this._observers)))
    end
    -- <summary>
    -- 注册容器行动观察者
    -- path: 监听的容器的路径, 可以为空, 使用container作为目标
    -- handler: 事件回调
    -- container: 路径的起始容器, 可以为空, 使用root作为目标
    -- </summary>
    RegisterContainerActionObserver = function (this, path, handler, container)
      if this.Root == nil then
        System.throw(System.NullReferenceException("Root container is null"))
      end
      if container == nil then
        container = this.Root
      end
      this._observers:Add(wdmWuKongIocObservers.ContainerActionObserver(path, handler, container, wdmWuKongIocObservers.CancelToken(this._observers)))
    end
    -- <summary>
    -- 注册格子观察者
    -- </summary>
    RegisterSlotObserver = function (this, slotPath, handler)
      if this.Root == nil then
        System.throw(System.NullReferenceException("Root container is null"))
      end
      this._observers:Add(System.new(wdmWuKongIocObservers.SlotObserver, 2, this.Root, slotPath, handler, wdmWuKongIocObservers.CancelToken(this._observers)))
    end
    -- <summary>
    -- 注册行动观察者
    -- </summary>
    -- <param name="handler"></param>
    -- <returns></returns>
    RegisterActionObserver = function (this, handler)
      this._observers:Add(wdmWuKongIocObservers.ActionObserver(this, handler))
    end
    IsContainer = function (this, obj)
      return System.is(obj, wdmContainer.IContainer)
    end
    IsElementContainer = function (this, obj)
      return System.is(obj, wdmWuKongContainer.ElementContainer)
    end
    IsAggregateContainer = function (this, obj)
      return System.is(obj, wdmWuKongContainer.AggregateContainer)
    end
    IsElement = function (this, obj)
      return System.is(obj, wdmContainer.Element)
    end
    IsPropertyItem = function (this, obj)
      return System.is(obj, wdmWuKongItem.PropertyItem)
    end
    IsSlot = function (this, obj)
      return System.is(obj, wdmWuKongContainer.Slot)
    end
    -- <summary>
    -- 修改格子内部元素的数量
    -- </summary>
    -- <param name="obj"></param>
    UpdateSlotCount = function (this, slot, count)
      if slot:getElement() == nil then
        System.throw(System.new(wdmWuKongAbstractions.WuKongException, 2, "Slot is null"))
      end
      slot:getElement():setCount(count)
    end
    SerializeWuKongContainer = function (this, container)
      wdmWuKongUserSerialize.PluginSerializeHelper.OnSerializeContainer(container)
      return wdmWuKongSerializer.ContainerSerializerExtension.Serialize(container, this._tables)
    end
    DeserializeWuKongContainer = function (this, userId, staff)
      local user =this._templateProvider:CreateUserContainer(userId)
      wdmWuKongSerializer.ContainerSerializerExtension.DeserializeToStructure(staff, user, this._tables, "")
      wdmWuKongUserSerialize.PluginSerializeHelper.OnDeserializeContainer(user)
      return user
    end
    -- <summary>
    -- 获取元素额外配置, 如果无, 则返回空
    -- </summary>
    -- <param name="id"></param>
    -- <returns></returns>
    GetElementExtraConfig = function (this, id)
      local extra =this._tables.TbElementExtra:GetOrDefault(id)
      if extra ~= nil then
        return extra.Config
      end
      return nil
    end
    -- <summary>
    -- 获取元素名称
    -- </summary>
    -- <param name="id"></param>
    -- <returns></returns>
    GetElementName = function (this, id)
      local config =GetElementConfig(this, id)
      if config then
      		        return config.Name
      	        end
      return nil
    end
    -- <summary>
    -- 获取元素配置, 如果无, 则返回空
    -- </summary>
    -- <param name="id"></param>
    -- <returns></returns>
    GetElementConfig = function (this, id)
      return this._elementConfigProvider:GetElementConfig(id)
    end
    Dispose = function (this)
      local u =this.Root
      if System.is(u, wdmWuKongAbstractions.IUserContainer) then
        u:Stop()
      end
      
      local tmpList =Linq.ToList(this._observers)
      
      for _, observer in System.each(tmpList) do
        observer:Dispose()
      end
      this._observers:Clear()
      
      this.Root = nil
    end
    GetUserId = function (this)
      local u =this.Root
      if System.is(u, wdmWuKongAbstractions.IUserContainer) then
        return u:getUserId()
      end
      return 0
    end
    return {
      base = function (out)
        return {
          out.wdm.WuKong.Ioc.IWuKongRbxFacade,
          out.System.IDisposable
        }
      end,
      SetRoot = SetRoot,
      DisposeRoot = DisposeRoot,
      getUserId = getUserId,
      GetContainer = GetContainer,
      GetTables = GetTables,
      GetChild = GetChild,
      TryGetChild = TryGetChild,
      ExecuteQuery = ExecuteQuery,
      ExecuteAction = ExecuteAction,
      ExecuteValidate = ExecuteValidate,
      ExecuteCommand = ExecuteCommand,
      GetCommandIds = GetCommandIds,
      GetUnixTime = GetUnixTime,
      GetRootContainer = GetRootContainer,
      RegisterContainerPropertyObserver = RegisterContainerPropertyObserver,
      RegisterContainerPluginDataObserver = RegisterContainerPluginDataObserver,
      RegisterContainerQueryObserver = RegisterContainerQueryObserver,
      RegisterContainerActionObserver = RegisterContainerActionObserver,
      RegisterSlotObserver = RegisterSlotObserver,
      RegisterActionObserver = RegisterActionObserver,
      IsContainer = IsContainer,
      IsElementContainer = IsElementContainer,
      IsAggregateContainer = IsAggregateContainer,
      IsElement = IsElement,
      IsPropertyItem = IsPropertyItem,
      IsSlot = IsSlot,
      UpdateSlotCount = UpdateSlotCount,
      SerializeWuKongContainer = SerializeWuKongContainer,
      DeserializeWuKongContainer = DeserializeWuKongContainer,
      GetElementExtraConfig = GetElementExtraConfig,
      GetElementName = GetElementName,
      GetElementConfig = GetElementConfig,
      Dispose = Dispose,
      GetUserId = GetUserId,
      __ctor__ = __ctor__,
      __metadata__ = function (out)
        return {
          fields = {
            { "OnActionFinished", 0x6, System.Delegate(System.String, System.Object, System.Object, System.Object, System.Object, System.Object, System.Void) },
            { "_actionCenter", 0x1, out.wdm.WuKong.Ioc.IActionCenter },
            { "_argsHelper", 0x1, out.wdm.WuKong.Ioc.ArgsHelper },
            { "_commandCenter", 0x1, out.wdm.WuKong.Ioc.ICommandCenter },
            { "_elementConfigProvider", 0x1, out.wdm.WuKong.Ioc.ElementConfigProvider },
            { "_observers", 0x1, System.HashSet(out.wdm.WuKong.Ioc.Observers.IWuKongObserver) },
            { "_routeCenter", 0x1, out.wdm.WuKong.Ioc.IRouteCenter },
            { "_tables", 0x1, out.wdm.WuKong.Configs.Tables },
            { "_templateProvider", 0x1, out.wdm.WuKong.User.IUserContainerTemplateProvider }
          },
          properties = {
            { "Root", 0x6, out.wdm.WuKong.Abstractions.Container.IContainer },
            { "UserId", 0x206, System.Int64, getUserId }
          },
          methods = {
            { ".ctor", 0x606, nil, out.wdm.WuKong.Configs.Tables, out.wdm.WuKong.User.IUserContainerTemplateProvider, out.wdm.WuKong.Ioc.IActionCenter, out.wdm.WuKong.Ioc.ICommandCenter, out.wdm.WuKong.Ioc.ElementConfigProvider, out.wdm.WuKong.Ioc.IRouteCenter },
            { "DeserializeWuKongContainer", 0x286, DeserializeWuKongContainer, System.Int64, out.wdm.WuKong.Serializer.ContainerStaff, out.wdm.WuKong.Abstractions.IUserContainer },
            { "Dispose", 0x6, Dispose },
            { "DisposeRoot", 0x6, DisposeRoot },
            { "ExecuteAction", 0x286, ExecuteAction, System.String, System.Array(System.Object), System.Object },
            { "ExecuteCommand", 0x206, ExecuteCommand, System.String, System.Array(System.String) },
            { "ExecuteQuery", 0x286, ExecuteQuery, System.String, System.Array(System.Object), System.Object },
            { "ExecuteValidate", 0x286, ExecuteValidate, System.String, System.Array(System.Object), System.Object },
            { "GetChild", 0x186, GetChild, System.String, out.wdm.WuKong.Abstractions.Container.IContainer },
            { "GetCommandIds", 0x86, GetCommandIds, System.Array(System.String) },
            { "GetContainer", 0x86, GetContainer, out.wdm.WuKong.Abstractions.Container.IContainer },
            { "GetElementConfig", 0x186, GetElementConfig, System.String, System.Object },
            { "GetElementExtraConfig", 0x186, GetElementExtraConfig, System.String, System.Object },
            { "GetElementName", 0x186, GetElementName, System.String, System.Object },
            { "GetRootContainer", 0x86, GetRootContainer, out.wdm.WuKong.Abstractions.IUserContainer },
            { "GetTables", 0x86, GetTables, out.wdm.WuKong.Configs.Tables },
            { "GetUnixTime", 0x86, GetUnixTime, System.Int64 },
            { "GetUserId", 0x86, GetUserId, System.Int64 },
            { "IsAggregateContainer", 0x186, IsAggregateContainer, System.Object, System.Boolean },
            { "IsContainer", 0x186, IsContainer, System.Object, System.Boolean },
            { "IsElement", 0x186, IsElement, System.Object, System.Boolean },
            { "IsElementContainer", 0x186, IsElementContainer, System.Object, System.Boolean },
            { "IsPropertyItem", 0x186, IsPropertyItem, System.Object, System.Boolean },
            { "IsSlot", 0x186, IsSlot, System.Object, System.Boolean },
            { "RegisterActionObserver", 0x106, RegisterActionObserver, System.Delegate(System.String, System.Object, System.Object, System.Object, System.Object, System.Object, System.Void) },
            { "RegisterContainerActionObserver", 0x306, RegisterContainerActionObserver, System.String, System.Delegate(out.wdm.WuKong.Abstractions.Container.IContainer, System.String, System.Array(System.Object), System.Void), out.wdm.WuKong.Abstractions.Container.IContainer },
            { "RegisterContainerPluginDataObserver", 0x306, RegisterContainerPluginDataObserver, System.String, System.Delegate(out.wdm.WuKong.Abstractions.Container.IContainer, System.String, System.Object, System.Void), out.wdm.WuKong.Abstractions.Container.IContainer },
            { "RegisterContainerPropertyObserver", 0x306, RegisterContainerPropertyObserver, System.String, System.Delegate(out.wdm.WuKong.Abstractions.Container.IContainer, System.String, System.Void), out.wdm.WuKong.Abstractions.Container.IContainer },
            { "RegisterContainerQueryObserver", 0x306, RegisterContainerQueryObserver, System.String, System.Delegate(out.wdm.WuKong.Abstractions.Container.IContainer, System.String, System.Object, System.Void), out.wdm.WuKong.Abstractions.Container.IContainer },
            { "RegisterSlotObserver", 0x206, RegisterSlotObserver, System.String, System.Delegate(out.wdm.WuKong.Container.Slot, System.String, System.Void) },
            { "SerializeWuKongContainer", 0x186, SerializeWuKongContainer, out.wdm.WuKong.Abstractions.IUserContainer, out.wdm.WuKong.Serializer.ContainerStaff },
            { "SetRoot", 0x206, SetRoot, out.wdm.WuKong.Abstractions.IUserContainer, System.Boolean },
            { "TryGetChild", 0x286, TryGetChild, System.String, out.wdm.WuKong.Abstractions.Container.IContainer, System.Boolean },
            { "UpdateSlotCount", 0x206, UpdateSlotCount, out.wdm.WuKong.Container.Slot, System.Int64 }
          },
          class = { 0x6 }
        }
      end
    }
  end)
end)
return {}