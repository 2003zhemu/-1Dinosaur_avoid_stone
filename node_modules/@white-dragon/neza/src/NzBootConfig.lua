local defines = require(game.ReplicatedStorage.Packages.Neza.Defines)
local module = {} :: defines.Bootstarp

-- UI启动项
module.Starts = {
    -- "ReplicatedStorage.UI",
}

-- UI模块启动项
module.StartModules = {
    -- "ReplicatedStorage.UI2.Pet",
    -- "ReplicatedStorage.UI2.Shop",
}

-- 组件包
-- default has：NezaUI.components
module.Components = {
    -- "ReplicatedStorage.MyComponents",
}

-- 预制体包
-- default has：ReplicatedStorage.UIPrefabs
module.Prefabs = {
    -- "ReplicatedStorage.MyPrefabs",
}

-- 图集文件夹或文件
-- default has：ReplicatedStorage.SpriteSheets
module.SpriteSheets = {
    -- "ReplicatedStorage.UI2.Pet.Model.mysprites",
}

-- 启动时回调
module.OnBootstraped = function()
    print("Neza has been bootstrapped")
end

return module

-- local Neza = require(game.ReplicatedStorage.Packages.Neza)
-- local cfg = require(script.Parent.NzBootConfig)

-- Neza:Boot(cfg)