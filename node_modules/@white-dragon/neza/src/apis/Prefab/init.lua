local Pool = require(script.Pool)
local module = {}

function module.Get(prefabName, ...)
    return Pool.GetNode(prefabName, ...)
end

function module.Put(node, ...)
    Pool.PutNode(node, ...)
end

function module.New(prefabName)
    return Pool.CreateNode(prefabName)
end

return module